# Kickoff — about-me-short thesis-lines COLD-READ (residual R1, final step)

*Paste everything below the line into a fresh session.*

---

Run the **cold-read** that closes stress-test residual R1 for the about-me-short
("How to Solve a Problem"). The two thesis lines were locked in a dedicated
writers-room session on 2026-08-03; the only thing left is reading them to
**one working PM** and logging the reaction. This session facilitates that
read, captures the result verbatim, and closes the residual.

This is **not** a writing session. The lines are LOCKED — do not redraft them,
do not reopen the picks, do not re-litigate the contract. If the cold-read
FAILS, the outcome is a logged failure + a flag back to the room, never an
in-session rewrite.

## 1. Read first

1. `anima/briefs/2026-08-02-about-me-short/lines-v1.md` — the whole file: the
   locked pair, Sean's verbatim reasons, and especially the **"Cold-read (one
   working PM)"** section, which contains the ready-to-run script. That script
   is the protocol; follow it as written.
2. The R1 bar, for context (no need to reread the whole bundle): the question
   must *demonstrate method* — the failure test is "the question could have
   been asked by anyone who merely paused." The cold-read bar is **"which one
   made them trust the asker."**

## 2. The locked pair (for reference — the file is canonical)

- **The question:** "Can you show me what happened the last time you tried to
  check out?"
- **The button:** NARRATOR: "Which brings us back to where every problem
  begins: a quiet—" [PROBLEM! alarm blares, cutting him off. Smash to black.]

## 3. Run the read

First ask Sean how the read is happening — the protocol adapts, the script
doesn't:

- **PM is present / on a call with Sean relaying:** walk Sean through the
  script step by step, one step at a time; he reads to the PM and types back
  what they said. Capture the PM's words **verbatim** — typos and all, same
  discipline as the front-door sessions.
- **Read already happened:** take Sean's account, capture the PM's reaction
  as close to verbatim as he can recall, and note it's recalled, not live.

Script (from lines-v1.md — do not paraphrase it to the PM):

1. Setup: *"A checkout metric is tanking. A PM walks over to the user and
   gets to ask exactly one question. Here are three ways to ask it."*
2. Read all three, unlabeled, in this order:
   (a) "What were you trying to do?"
   (b) "Can you show me what happened the last time you tried to check out?"
   (c) "Where did it lose you?"
3. Ask: *"Which one makes you trust the asker most — and why?"*
4. Then the button beat: describe the ending (team fixed it, user smiling,
   serene narrator: "Which brings us back to where every problem begins: a
   quiet—" ALARM, black). Ask: *"Does that ending land?"*
5. **PASS** = they pick (b), or explain that (b) is the one that shows the
   asker knows what they're doing. **FAIL** = they pick (a) or (c) — R1 is
   telling us something; log it and flag, don't fix.

Also capture: the PM's name/role (or "anonymous, working PM" if Sean prefers),
and any unprompted reactions — those are often the best signal.

## 4. Deliverables + wrap

- Fill the **Result** line in `lines-v1.md` §Cold-read with the verbatim
  reaction, the reader identity, PASS/FAIL against the bar, and the date.
- **On PASS:** update the about-me-short ticket bullet in
  `vault/00_inbox/tickets.md` — residual (1)'s "cold-read pending" clause
  becomes "cold-read PASSED {date}"; residual (2) (stopwatch table-read, must
  absorb the F3 alarm-sting delta per lines-v1.md §Delta) stays the next
  recommended session.
- **On FAIL:** log the result honestly in both files, mark residual (1) as
  "lines need a room revisit — cold-read failed," and add a one-line ticket
  bullet under `## Todo` for the revisit session with the PM's objection
  quoted. Do not rewrite the lines.
- Capture any other call-outs Sean makes as ticket bullets (CLAUDE.md rule 8).
- Commit the vault change yourself (vault git ownership is yours on this
  machine — memory: vault-git-ownership-macbook-pro).
- Standing open item, offer once at wrap: the entire
  `anima/briefs/2026-08-02-about-me-short/` bundle (concept doc, studio brief,
  art-viz renders, lines-v1.md) is **untracked in the anima repo** — offer to
  commit it there (only that directory; the repo has unrelated uncommitted
  changes that are not this session's to touch).

## Context worth knowing

The fresh-context stress reviewer called these "the two most load-bearing
lines in the script." The question carries the film's entire judgement
payload; the cold-read is the cheapest external test of the whole piece's
claim ("this person's judgement is the product"). Sean chose a real PM over
playing the PM himself because fresh ears are truest to the test — protect
that blindness: the PM must not know which candidate is the pick until after
they answer step 3.
