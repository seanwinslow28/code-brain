# Claude Code Prompt: Build My Obsidian Vault

## Context

I'm building an Obsidian vault that serves as a headless, machine-readable second brain — optimized for Claude Code programmatic access first, visual Obsidian UI second. This vault lives inside my `claude-code-superuser-pack` project at `vault/`.

I already have:
- A `vault/` directory in this project (currently has a basic PARA skeleton — we're replacing it)
- An `apple-notes-import/` folder inside vault with my exported Apple Notes
- 102 Claude Code skills at `.claude/skills/`, 13 agents at `.claude/agents/`, 7 hooks
- Existing vault-related skills: `vault-architecture`, `vault-read-write`, `knowledge-graph-nav`, `vault-mcp-setup`, `semantic-search`, `daily-driver`
- A `daily-note-appender` hook that writes session summaries when Claude Code stops

## What I Need You To Build

Execute this in phases. After each phase, confirm what you built and tell me what I need to do manually before we continue.

---

### PHASE 1: Scaffold the Folder Structure

Replace the current vault structure with this shallow-domain model. Preserve the existing `apple-notes-import/` folder by moving it to its new location.

```
vault/
├── 00_inbox/                    # Raw captures, Zapier webhooks, quick ideas
├── 10_timeline/
│   ├── daily/                   # Daily notes (YYYY-MM-DD.md)
│   ├── weekly/                  # Weekly reviews (YYYY-Www.md)
│   └── monthly/                 # Monthly reviews (YYYY-MM.md)
├── 20_projects/                 # Active cross-domain work
│   ├── prj-16bitfit/
│   ├── prj-campus-201/
│   ├── prj-boston-move/
│   ├── prj-superuser-pack/
│   ├── prj-animation-pipeline/
│   └── prj-personal-finance/
├── 30_domains/                  # Mirrors my 6 Claude Code workspaces
│   ├── claude-mastery/
│   ├── product-management/
│   ├── creative-studio/
│   ├── life-systems/
│   ├── design-team/
│   └── vault/
├── 40_knowledge/                # Atomic notes, NotebookLM exports, reference
│   ├── concepts/                # Permanent Zettelkasten-style notes
│   ├── notebooklm-exports/      # Synthesized research from NotebookLM
│   └── references/              # API docs, guides, external material
├── 50_sources/                  # Raw data files
│   ├── finance/                 # Bank CSVs, expense exports
│   ├── assets/                  # Creative assets, sprite references
│   └── data/                    # Misc raw data
├── 60_archive/                  # Completed/paused projects and old notes
├── 70_apple-notes/              # Imported Apple Notes (triage over time)
├── 90_system/
│   ├── templates/               # Templater templates
│   ├── scripts/                 # Dataview scripts, automation helpers
│   ├── css-snippets/            # Custom CSS for callouts and layout
│   └── mcp-configs/             # MCP server configuration references
└── .obsidian/                   # Obsidian config (auto-generated by app)
```

**Rules:**
- Use underscores in top-level folder names (not hyphens) for numeric prefixes
- Use kebab-case for everything inside folders
- Create a `.gitignore` at `vault/` root that excludes: `.obsidian/workspace.json`, `.obsidian/workspace-mobile.json`, `.trash/`, `.DS_Store`, `*.tmp`, and `50_sources/finance/*.csv` (sensitive financial data)
- Create a `vault/.gitkeep` in every empty directory so Git tracks the structure
- Move `apple-notes-import/` contents to `70_apple-notes/`

---

### PHASE 2: Create Templates

All templates go in `vault/90_system/templates/`. Use Templater syntax (`<% %>`) where dynamic content is needed.

#### 2a. Daily Note Template (`tpl-daily.md`)

```markdown
---
type: daily
date: <% tp.file.title %>
energy-peak: null
mood: null
---
# <% tp.file.title %>

## Morning Focus (Manual — <2 min)
> What's the ONE thing that makes today successful?

**Focus:** 

## Tasks
```dataview
TASK FROM "20_projects"
WHERE status = "active" AND !completed
SORT file.name ASC
```

## Work Log
<!-- jira-log -->

## Claude Code Sessions
<!-- claude-sessions -->

## Side Project Notes
<!-- side-projects -->

## Evening Reflection (Manual — <2 min)
> What worked? What didn't? Carry forward?

**Win:** 
**Lesson:** 
**Carry forward:** 
```

#### 2b. Weekly Review Template (`tpl-weekly.md`)

```markdown
---
type: weekly
week: <% tp.file.title %>
date-start: <% tp.date.now("YYYY-MM-DD", 0, tp.file.title, "gggg-[W]ww") %>
date-end: <% tp.date.now("YYYY-MM-DD", 6, tp.file.title, "gggg-[W]ww") %>
---
# Weekly Review — <% tp.file.title %>

## Wins This Week
<!-- auto-wins -->

## Blockers & Open Loops
<!-- auto-blockers -->

## Claude Code Session Summary
```dataview
TABLE WITHOUT ID
  file.link AS "Day",
  length(file.tasks.completed) AS "Tasks Done"
FROM "10_timeline/daily"
WHERE date >= date(<% tp.date.now("YYYY-MM-DD", 0, tp.file.title, "gggg-[W]ww") %>) AND date <= date(<% tp.date.now("YYYY-MM-DD", 6, tp.file.title, "gggg-[W]ww") %>)
SORT date ASC
```

## Domain Progress
- **PM Work:**
- **16BitFit:**
- **Animation:**
- **Life Systems:**

## Next Week's Priorities
1. 
2. 
3. 

## Decisions Made
<!-- auto-decisions -->
```

#### 2c. Project Note Template (`tpl-project.md`)

```markdown
---
type: project
domain: []
status: active
context: <% tp.file.title.replace("prj-", "") %>
energy-level: medium
ai-context: ""
review-date: <% tp.date.now("YYYY-MM-DD", 14) %>
created: <% tp.date.now("YYYY-MM-DD") %>
---
# <% tp.file.title %>

## Overview
**Goal:** 
**Why it matters:** 
**Definition of Done:** 

## Current Status
<!-- status-update -->

## Latest Commits
<!-- git-commits -->

## Key Decisions
| Date | Decision | Rationale |
|------|----------|-----------|

## Blockers
- 

## Links
- **Repo:** 
- **Jira:** 
- **Figma:** 
- **Related notes:** 
```

#### 2d. Meeting Note Template (`tpl-meeting.md`)

```markdown
---
type: meeting
date: <% tp.date.now("YYYY-MM-DD") %>
domain: [product-management]
attendees: []
context: ""
---
# Meeting — <% tp.date.now("YYYY-MM-DD") %>

## Agenda


## Notes


## Action Items
- [ ] 

## Decisions Made
> #decision 

## Follow-ups
- [ ] 
```

#### 2e. Knowledge Note Template (`tpl-knowledge.md`)

```markdown
---
type: reference
domain: []
status: active
source: ""
ai-context: ""
created: <% tp.date.now("YYYY-MM-DD") %>
---
# <% tp.file.title %>

## Summary


## Key Takeaways


## How This Connects
- **Applies to:** 
- **Related notes:** 

## Source Material
```

#### 2f. Idea Capture Template (`tpl-idea.md`)

```markdown
---
type: idea
domain: []
status: backlog
energy-level: low
ai-context: ""
created: <% tp.date.now("YYYY-MM-DD") %>
---
# <% tp.file.title %>

## The Idea


## Why It's Interesting


## Next Step
- [ ] 
```

---

### PHASE 3: Create the 6 Domain MOCs + Home Dashboard

#### 3a. Domain MOCs

Create these in `vault/10_atlas/` (create this folder — I forgot it in the structure above, add it between 00_inbox and 10_timeline... actually, keep timeline at 10. Put atlas at `05_atlas/`).

**Update: Revised folder list — add `05_atlas/` for MOCs and dashboards.**

Each MOC follows this pattern. Create one for each of my 6 domains:
- `moc-claude-mastery.md`
- `moc-product-management.md`
- `moc-creative-studio.md`
- `moc-life-systems.md`
- `moc-design-team.md`
- `moc-vault.md`

**MOC template pattern:**

```markdown
---
type: moc
domain: [<domain-name>]
---
# <Domain Name>

## Active Projects
```dataview
TABLE status, ai-context
FROM "20_projects"
WHERE contains(domain, "<domain-name>")
AND status = "active"
SORT file.mtime DESC
```

## Knowledge Base
```dataview
TABLE ai-context, source
FROM "40_knowledge"
WHERE contains(domain, "<domain-name>")
SORT file.mtime DESC
LIMIT 20
```

## Recent Daily Mentions
```dataview
TABLE date
FROM "10_timeline/daily"
WHERE contains(file.outlinks, this.file.link)
SORT date DESC
LIMIT 7
```
```

#### 3b. Home Dashboard (`vault/Home.md`)

```markdown
---
type: dashboard
---
# Sean's Command Center

## Active Projects
```dataview
TABLE WITHOUT ID
  file.link AS "Project",
  status AS "Status",
  domain AS "Domain",
  ai-context AS "Current Focus"
FROM "20_projects"
WHERE status = "active"
SORT file.mtime DESC
```

## Today's Note
```dataview
LIST
FROM "10_timeline/daily"
WHERE date = date(today)
```

## This Week's Wins
```dataview
TASK
FROM "10_timeline/daily"
WHERE completed AND date >= date(today) - dur(7 days)
LIMIT 10
```

## Domain Quick Links
- [[moc-product-management|PM Work]]
- [[moc-creative-studio|Creative Studio]]
- [[moc-claude-mastery|Claude Mastery]]
- [[moc-life-systems|Life Systems]]
- [[moc-design-team|Design Team]]
- [[moc-vault|Vault Meta]]

## Blockers Across All Projects
```dataview
LIST ai-context
FROM "20_projects"
WHERE status = "blocked"
```
```

---

### PHASE 4: Create Starter Project Notes

Create these in `vault/20_projects/` using the project template's YAML schema. Pre-populate with what you know about each from my CLAUDE.md and personal context:

1. **`prj-16bitfit.md`** — Domain: `[creative-studio]`. AI-context: "Game Boy fitness RPG. ~50% complete. Blocked on sprite sheet consistency. Pipeline: anchor image → Claude Code → vision audit → post-process → TexturePacker."
2. **`prj-campus-201.md`** — Domain: `[product-management]`. AI-context: "Education platform feature development at The Block."
3. **`prj-boston-move.md`** — Domain: `[life-systems]`. AI-context: "Moving from NYC to Boston March 21, 2026. Checklist includes address changes, medical provider transition, rent setup."
4. **`prj-superuser-pack.md`** — Domain: `[claude-mastery]`. AI-context: "102 skills, 13 agents, 7 hooks. Ongoing expansion and audit."
5. **`prj-animation-pipeline.md`** — Domain: `[creative-studio]`. AI-context: "R&D: Transfer sprite automation → cartoon animation → festival submissions. ComfyUI + ControlNet + IP-Adapter."
6. **`prj-personal-finance.md`** — Domain: `[life-systems]`. AI-context: "Reduce credit card debt, audit subscriptions, replace Rocket Money. Chase + Bilt CSV parsing."

---

### PHASE 5: Create/Update Claude Code Skills

#### 5a. New Skill: `process-inbox`

Create at `.claude/skills/process-inbox.md`. This skill:
- Scans `vault/00_inbox/` for unprocessed files
- For each file: analyzes content, determines domain, applies the YAML frontmatter schema, generates a 1-sentence `ai-context`, renames with proper prefix (`ref-`, `prj-`, etc.)
- Moves processed files to the appropriate folder (`40_knowledge/`, `20_projects/`, etc.)
- Reports what was processed

#### 5b. New Skill: `preserve-session`

Create at `.claude/skills/preserve-session.md`. This skill:
- Reads the current project's CLAUDE.md
- Updates it with: current blockers, decisions made this session, next steps, and a timestamp
- Also appends a summary to the corresponding vault project note if one exists

#### 5c. New Skill: `resume-session`

Create at `.claude/skills/resume-session.md`. This skill:
- Reads the current project's CLAUDE.md
- Reads the corresponding vault project note
- Reads today's daily note for any related context
- Synthesizes a "here's where you left off" briefing

#### 5d. Update Existing Skill: `daily-driver`

Update `.claude/skills/daily-driver.md` to reference the new folder structure, the new daily note template path, and the HTML comment anchors for injection points.

#### 5e. Update Existing Skill: `vault-architecture`

Update `.claude/skills/vault-architecture.md` to document the new shallow-domain folder structure, naming conventions, YAML schema, and the rationale for each decision.

#### 5f. Update Existing Skill: `vault-read-write`

Update `.claude/skills/vault-read-write.md` to include:
- The HTML comment anchor pattern for PATCH-style writes
- The naming convention rules (kebab-case, prefixed)
- The YAML schema reference
- Instructions for targeting specific sections via anchors instead of full-file rewrites

---

### PHASE 6: Update the `daily-note-appender` Hook

Refactor the existing hook to write session summaries in this format, targeting the `<!-- claude-sessions -->` anchor:

```markdown
- [time:: HH:MM] | [domain:: <detected-domain>] | [context:: <project>] | **Outcomes:** <1-sentence summary>. Link: [[prj-<project>]]
```

This format makes entries queryable by Dataview (inline fields) while being human-readable.

---

### PHASE 7: Create the `.obsidian` Starter Config

Create minimal Obsidian configuration files so the vault opens correctly:

- `vault/.obsidian/app.json` — Set default new note location to `00_inbox/`, enable strict line breaks, set attachment folder to `50_sources/assets/`
- `vault/.obsidian/appearance.json` — Placeholder (theme set manually)
- `vault/.obsidian/community-plugins.json` — Empty array (plugins installed manually, but document which ones I need)
- `vault/.obsidian/hotkeys.json` — Empty object

---

### PHASE 8: Create a Setup Checklist

Create `vault/90_system/SETUP-CHECKLIST.md` documenting everything I need to do manually after Claude Code finishes scaffolding. This is critical — list every manual step with links to plugin pages and configuration instructions.

---

## Important Constraints

1. **Do NOT delete `apple-notes-import/`** — move its contents to `70_apple-notes/`
2. **Keep all existing `.claude/skills/` files intact** — only update the 3 vault-related skills listed above, create the 3 new ones
3. **Use kebab-case** for all filenames inside the vault
4. **Flat YAML only** — no nested objects in frontmatter
5. **Every template must have HTML comment anchors** where Claude Code will inject content programmatically
6. **Run `python3 scripts/validate.py` after all changes** to ensure nothing in the superuser pack is broken
7. Before configuring any MCP servers, **use the GitHub MCP to check the current status** (stars, last commit date, open issues) of these repos:
   - `iansinnott/obsidian-claude-code-mcp`
   - `aaronsb/obsidian-semantic-mcp`
   - `mauricio.wolff/mcp-obsidian` (or `bitbonsai/mcp-obsidian`)
   - `dan6684/smart-connections-mcp` (or `brianpetro/obsidian-smart-connections`)
   Report back what you find so I can make informed decisions about which to install.

---

## Execution Order

1. Phase 1 (folders + .gitignore) → **CHECKPOINT: confirm structure**
2. Phase 2 (templates) → **CHECKPOINT: confirm templates**
3. Phase 3 (MOCs + Home) → Phase 4 (project notes) → **CHECKPOINT: confirm content**
4. Phase 5 (skills) → Phase 6 (hook update) → **CHECKPOINT: confirm automation layer**
5. Phase 7 (.obsidian config) → Phase 8 (setup checklist) → **DONE**

At each checkpoint, show me what you created and ask if I want adjustments before continuing.
