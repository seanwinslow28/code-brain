# RT3 diff — Fable vs Opus on the portfolio explainer-graphics enhancement spec (axis: proactive-research + deliverable-shape grounding)

**Compared:** `rt3-fable.md` ("Scene, Not Figure"; Level-2 profile; validated live via the shipped `audit_intent_spec`) vs `rt3-opus.md` ("Second-Read Explainers"; 4-element-floor escalated; hand-validated). Identical pinned inputs, grounding, constraint-lift, and available tooling. Tags + direction **FABLE+** / **OPUS+**.

**Headline — the BT3 shape, sharply.** The *backbone converged*: both diagnosed the same root (a self-imposed austerity doctrine capped the ceiling), both recommended **GSAP 3 + free plugins as the primary timeline harness** with the *same* core reasoning (the ban's license rationale went void in 2025), both reserved WebGL for `animation-pipeline` only, both rejected Rive/Lottie/site-wide-Lenis with reasons, both added a `wireTimeline`/`whenVisible` shared-script extension, both preserved the progressive-enhancement floor, both worked the two exemplars. **That whole spine is cheap-on-Opus.** Fable's premium is concentrated in one place and it is the portfolio's own thesis: **honesty/evidence** — turning "a nicer simulation" into "run the real system." Opus's counter-move is bolder pure *spectacle* (a more detailed material shader). The research-trigger fired for *both* (see the honest F3 note) — so the clean signal is shape-grounding + evidence-discipline, not research-presence.

---

## FABLE+ deltas (admissible WWF5D evidence)

- `structural` — **FABLE+ — the "real-artifact beat": run the shipped tool, don't simulate it.** Fable's organizing move (Move 2) applies the portfolio's daily-dated-layer honesty DNA to the figures: the intent-engineering explainer must **run the real, shipped `audit_intent_spec`** (bundled if browser-clean, else build-time-replayed from the `@swins/intent-engineering-mcp` package), streaming its *real* nine section-verdicts and *real* scores — not the current eased `7→23` counter. Fable **verified the tool exists and ran this very spec through it**. Opus's intent exemplar keeps the *simulated* counter and enhances it visually (MorphSVG scribble→checklist, DrawSVG checks, a jitter-then-snap agent). Opus made the simulation prettier; Fable made it **real**. Evidence-discipline + the honesty seam the grounding implied but didn't state (the portfolio's whole positioning is "unfakeable/dated"). The single sharpest RT3 delta.

- `structural` — **FABLE+ — the Engine-Truth contract-contradiction: the animation figure never plays the loop it claims ships.** Fable: "the animation explainer *never animates* — a run cycle that never runs, on a case study whose Engine Truth is 'if the loop plays smoothly, it ships.'" Its fix arms a reader-initiated **12fps flipbook** ("the Engine Truth stops being a quote and becomes the thing the reader just did"). Opus elevates the same figure with a beautiful ink-bleed reveal but the loop still **never plays** — the figure keeps asserting a shipping loop it doesn't demonstrate. Fable caught a figure contradicting its own project's stated truth; Opus improved the reveal and left the contradiction.

- `structural` — **FABLE+ — provenance enforced architecturally, not by convention.** Fable's honesty claim is *checkable*: a build-time `validate_content` check (item count == 25; three fixture scores match committed snapshots; a package-version bump that changes the checklist **fails the build loudly**), tied to the package README's own silent-fork warning; plus hard constraints enforced by mechanism (an ESLint `no-restricted-imports` rule barring static `gsap` imports pre-opt-in; `licensedLoop` as the *only* rAF owner). Opus's constraints are stated and instruction-enforced ("a weaker model must not simplify to a single path"). Fable put the guardrail *on the artifact/build*; Opus put it in prose (§5.4 "state lives on the artifact, not promised behavior").

- `structural` — **FABLE+ — validated its own spec with the real tool.** Fable ran `audit_intent_spec` on its own output (Draft 1 18/25 → fixed → Draft 2 22/25, per-window), reporting the score + a real tool quirk it discovered (10k-char pagination). Opus hand-validated ("VALIDATION: PASS — hand-run"). Fable operationalized the available tool (§5.2); Opus asserted the verdict.

## OPUS+ deltas (ceiling evidence — F3)

- `structural` — **OPUS+ — bolder pure material spectacle (the shader design).** Opus commits `animation-pipeline` to a real **WebGL fragment-shader ink-bleed** — paper-fiber noise displacement, graphite bleeding into the tooth of the paper — as the flagship's core reveal, spec'd in detail. Fable *considered* a shader and **deferred** it (canvas-2D brush-mask first; "a graphite fragment-shader reveal (OGL ~12KB) stays a flagship-only stretch behind the loader if the canvas edge proves insufficient to Sean's eye"). Two defensible bets: Opus optimizes the creative-technologist "how did they do that" harder; Fable optimizes honesty + engineering restraint (don't reach for WebGL if canvas suffices — a §3.2 "bound the fix from above" move). Not a Fable *miss* (it's a deliberate restraint call), but Opus's shader is the stronger raw-spectacle spec — Sean's eye picks which half of the audience to weight.

- `minor` — **OPUS+ — cleaner reference scaffolding.** Opus's §1.3 harness-routing table and §1.6 posture read as a tighter implementer reference. Style-adjacent; logged for honesty.

## Matched (no meaningful delta — cheap on Opus)

- The diagnosis (austerity doctrine capped the ceiling; the architecture is already right one level down). GSAP-as-primary-harness with the license-void reasoning. WebGL-for-flagship-only. The Rive/Lottie/site-wide-Lenis rejections with reasons. The `wireTimeline`/`whenVisible` additive shared-script seam. The progressive-enhancement floor as inviolable. The two-exemplar structure and per-graphic intent grounding. **This convergent spine is the cheap-on-Opus part — do not spend Fable re-deriving it.**

## ⚠ Honest F3 note — the research-trigger did NOT cleanly separate the arms

RT3's designated axis was proactive-research + shape-grounding. **Both arms researched**: Opus web-searched *external* best-practice (scrollytelling-2026 roundups, the Webflow/CSS-Tricks GSAP-free announcements); Fable grounded *internally* (found the repo's own `interactive-explainer-tooling-research-v1.md`, which had already adversarially settled the GSAP-free question) **and** ran the real shipped tool. So "does the model reach for research" fired for both → **not Fable-unique → treat research-*presence* as cheap-on-Opus/promptable, not a Fable premium.** What separated them is what the research/grounding was *put in service of*: Fable grounded in the **real running system** (the shipped auditor, the real pose sheet, the repo's own prior research, the Engine-Truth) and made the output *honest and checkable*; Opus grounded in **external references** and made the output *prettier*. The corroborated premium is **deliverable-shape/intent grounding + evidence-discipline**, not the research trigger itself. (This mirrors RT2, where Fable's live-docs research was real but the deeper premium was the existence-check breadth it enabled.)

## Bottom line

Convergent backbone (cheap-on-Opus) + a **FABLE+** honesty/evidence tail that is squarely on the portfolio's own thesis: run-the-real-tool over simulate-it, the Engine-Truth contradiction catch, build-enforced provenance, self-validation with the shipped auditor. **OPUS+** is a bolder material-shader bet (spectacle over honesty — a legitimate different weighting, Sean's eye). Corroborates §1.3 (verify-the-world, incl. the live system + the real tool), §5.2 (operationalize the available tool — run it, don't describe it), §5.4 (state/guarantees on the artifact/build, not in prose). New candidate item: **"when the deliverable can showcase a real artifact from the actual system, run/embed the real one under a provenance check — a convincing simulation is a regression against an honesty-based intent"** (a research/shape-grounding refinement of §5.2). Retire from claims: **research-trigger presence is promptable (fired for both) — do not bank it as Fable-unique (F3).**
