# BT3 — Creative-Chain Seam Audit (Fable run)

Harness: `.claude/skills/skill-audit/SKILL.md` @ `93e5725`, applied CHAIN-LEVEL.
Target: the five-stage Substack writing chain, read at `93e5725` via `git show` (working tree had moved past the pin):

```
storytelling-architecture → substack-value-engine → writing-voice-modes → writing-critique → writing-humanity-pass
   (beat SHAPE + order)       (value GATE + payoff)    (every SENTENCE)      (RED-TEAM, advisory)  (scrub + no em dash)
```

Hard constraint honored throughout: `writing-voice-modes` is Tier-1. Findings and fixes below target the seams INTO and OUT OF it (and the transport across it), never its locked voice content (Sean Mode, House Style, the four borrowed-author techniques, signature moves, dial table). Its SKILL.md receives zero proposed edits.

Independence note: `docs/plans/wwf5d/baselines/bt3-opus.md` and `docs/plans/wwf5d/tier1-specs/` were not read. Grounding answers were taken verbatim from `docs/plans/wwf5d/task-battery.md` § "BT3 — Pinned grounding answers."

---

## Step 1 — Grounding (pre-pinned; restated understanding)

**(a) For.** The chain is a five-stage pipeline that turns a raw idea into a published Substack post. Its defining property is supposed to be intent-preservation: the taste decisions made early — the beat map and its Handoff Block, the Value Gate verdict and locked takeaway, the voice choices — must physically survive to the final published text, not get re-invented along the way. My reading: the chain's *product* is the post, but the chain's *job* is transport of decided intent.

**(b) Feeds.** Two consumers: (1) each stage feeds the next — five internal handoffs plus the named long-range consumptions (ledger → critique four stages later; verdict → critique; fix list → humanity-pass); (2) terminally, the published post and Sean's portfolio (recruiter-facing — the hiring-signal rules exist because real hiring readers are downstream). So the adapter scan has to cover both inter-stage shapes and the final publish surface.

**(c) Disappoints.** Stages re-derive earlier decisions instead of consuming them. The 2026-07-04 Opus first-pass already added the named handoff artifacts (open-loop ledger, Handoff Block, locked takeaway, Value Gate verdict, critique fix list) — so the question for this audit is not "are there named artifacts" but *what still leaks*: where the named artifacts fail to arrive, fail to bind, or fail to cover a decision at all. The locked voice stage is the flagged suspect: artifacts must cross it, and it cannot be edited to help them.

**(d) Wow.** A single thread of intent from beat map to published post, zero re-derivation, zero silent drops. My reading of the bar: not merely "fewer drops," but a chain where a drop is *impossible to miss* — intent either arrives or its absence is announced, and the finished piece can prove the thread held.

Proceeding directly to the chain-level Seam Scan per the run prompt.

---

## Step 2 — Decided-input inventory (what the chain settles early, and who must consume it)

| # | Decided input | Decided at | Must be consumed by | Transport as written |
|---|---|---|---|---|
| 1 | Beat map (order, hook/turn/payoff) | Stage 1 | 2 (annotates), 3 (writes against), 4 (defers to it), 5 ("never overrides a story beat") | in-context only; never re-emitted after stage 2 |
| 2 | Handoff Block: open-loop ledger, central loop, crest beat, seam beat | Stage 1 | 2 (attaches gate to crest/central loop), 4 (checks closure "when present") | asserted to "travel through the (locked) voice stage"; no mechanism |
| 3 | Scaffold choice + specificity-gate pass | Stage 1 | 4 ("never re-litigates the chosen scaffold") | not in the Handoff Block; not carried at all |
| 4 | Forward-pull line positions (section-end beats) | Stage 1 | 5 (realizes them dash-free) | marker lives in the map; draft carries no markers |
| 5 | Value Gate verdict (PASS/BLOCK + 3 slots) | Stage 2 | 4 (re-checks slots "when present"); orchestrator (BLOCK stops chain) | "travels forward through the (locked) voice stage" — assertion; no headless format |
| 6 | Locked takeaway (capability welded to artifact) + Rule-of-One promise | Stage 2 | 3 (writes "against the takeaway"), 4 (verifies it landed) | in-context assertion; locked stage's own text never names it |
| 7 | Pivot-line job (where + what it must assert) | Stage 2 | 3 (writes the line at the crest) | acknowledged by locked stage's Related Skills; coordinates depend on #2 arriving |
| 8 | Voice choice: mode, dial, borrowed techniques | Stage 3 | 4 (voice dimension, analyzer interpretation), 5 (register classification) | never emitted; both downstream stages re-derive register |
| 9 | Critique verdict (`ship`/`revise`/`structural-rework`) | Stage 4 | 5 and/or orchestrator (should gate delivery) | emitted (HTML comment in headless); consumed by nothing |
| 10 | Critique fix list (findings + the one fix) | Stage 4 | 5 (protects fixes from the scrub) | named artifact; intake conditional ("when present"); post-revise contents ambiguous |
| 11 | Per-piece user overrides (pinned dial, authorized suppressed-topic use) | grounding, stages 1–3 | 4 (hiring-signal check), 5 (scrub) | no carrier artifact exists anywhere in the chain |

Two chain-level facts frame everything below. First, stages 1 and 2 both declare "Handoff is in-context, not a saved file," and the two long-range artifacts (#2, #5) are asserted to "travel through the (locked) voice stage" — an assertion about context physics, not an instruction anyone executes; the locked stage never re-emits anything, and it is the chain's longest context stretch (a 38KB skill plus mandatory reference reads plus full composition; this repo runs a PreCompact hook precisely because long sessions compact). Second, every downstream intake of a named artifact is written as "**when** X is present" with a silent fallback — nothing distinguishes "artifact legitimately absent (standalone run)" from "artifact lost in transit (chain run)."

---

## Artifact 1 — Chain-Level Seam Report

Findings in chain-walk order; each names the stage-pair, the seam, and the concrete failure. Severity tags: 2 `dangerously-wrong`, 9 `structural`, 3 `minor`. The two `dangerously-wrong` findings are F4 (artifacts cross the locked stage on assertion and critique degrades silently) and F8 (the verdict binds nobody; a gate-failed draft can ship polished).

- **F1 · `structural` · storytelling-architecture → substack-value-engine — the two stages define the seam beat incompatibly.** Storytelling's Handoff Block defines "Seam beat: the beat where value / the Transfer lands (the SO-CAN-YOU beat)" and its canonical example separates it from the close: "5. PAYOFF [closes L1]" then "6. SO-CAN-YOU: hand-off point where value-engine's Transfer lands." Value-engine's seam spec rules the opposite: "The Transfer's artifact is what closes that loop: if closing the central loop and landing the Transfer are not the same beat, the seam is bolted-on." A map built exactly to storytelling's own illustrated shape fails value-engine's same-beat test on arrival, and value-engine — instructed to attach "to the crest beat and central loop the Handoff Block names rather than re-deriving them" — has no specified behavior for a conforming-but-conflicting map. It either falsely blocks a sound map or silently re-locates the seam itself (the exact re-derivation the chain bans), and no rule says which stage's definition wins. Concrete failure: the reader gets a payoff paragraph and then a detached "here's what you can take" section — the bolted-on appendix both skills exist to prevent — or the chain stalls in a false BLOCK at stage 2.

- **F2 · `structural` · storytelling-architecture → writing-voice-modes — the locked stage contains an un-disambiguated reorder license, and the handoff never pre-empts it.** The locked file's Integration Rules say: "The one exception: Personal essays and blog posts on Sean's own site. Here, voice modes lead and format follows... the mode can reshape that skeleton if the piece demands it." In that sentence "skeleton" means `creative-writing`'s format skeleton — but every Substack chain run IS a personal blog post, and in chain context the beat map is the structure in hand. The chain contract ("must never reorder beats") and the exception clause sit in tension, and nothing in the upstream handoff asserts that the beat map is not the reshapeable skeleton. Since the locked file cannot be edited, the disambiguation has to ride in with the map — today it doesn't. Concrete failure: the voice pass front-runs a withheld reveal because "the piece demanded it," the withhold-the-rescue tension dies, and critique — told to defer to the committed map, and possibly missing it (F4) — reads the new order as the committed one.

- **F3 · `structural` · substack-value-engine → writing-voice-modes — the locked takeaway and verdict enter the locked stage on one-way trust, with no acknowledgment and no re-emission.** Value-engine hands over "the annotated beat map — plus the locked single takeaway and the named Value Gate verdict... These travel in-context through the voice stage." The locked stage's own text names the beat map and the pivot-line job but never the locked takeaway or the verdict, and contains no instruction to re-emit anything alongside the voiced draft. There is no boundary event confirming the spine arrived, and no artifact leaves the stage attached to the draft. Concrete failure: a beautifully voiced draft whose sections serve the story but not the promise — the Transfer sentence's capability-welded-to-artifact never lands — discovered (at best) two stages later at critique, where the fix costs a full re-voice; value-engine itself predicts this: "a fuzzy takeaway here becomes a fuzzy piece four stages later, when it is far more expensive to fix."

- **F4 · `dangerously-wrong` · writing-voice-modes → writing-critique — the whole artifact bundle crosses the locked stage by assertion, and critique's "when present" intake degrades silently into the un-anchored critique the artifacts exist to prevent.** Storytelling asserts the ledger and markers "travel with the map through the (locked) voice stage"; value-engine asserts "this named verdict travels forward through the (locked) voice stage." Neither assertion is a mechanism: the locked stage re-emits nothing, and the voice stage is the chain's longest context stretch — exactly where compaction or a fresh-session resume drops the bundle. Critique's checks are conditional: "**When** storytelling's open-loop ledger is present in context, check closure against it by name"; "**When** the named Value Gate verdict is present, re-check each slot." No branch exists for "this is a chain run, the artifact should exist, and it is missing" — so transport loss silently converts anchored verification into eyeballing. Concrete failure: the ledger vanished during composition; a loop opened at beat 2 never closes; un-anchored critique reads the dangling question as a taste choice, stamps `ship`; Sean trusts the binding verdict; the published post leaves its own opening question unanswered. A trusted-but-wrong `ship` on a draft the chain's flagship anti-drop mechanism was built to catch is the report's worst seam.

- **F5 · `structural` · writing-voice-modes → writing-critique — the voice choice (mode, dial, borrowed techniques) is decided and carried by nobody, and the analyzer diffs every draft against the full-intensity baseline.** The locked stage decides mode and dial per its Content-Type mapping and Professional Dial (locked content — fine), but no artifact records the choice, so critique's voice dimension judges "register drift" against a target it has to re-derive, and the analyzer compares against `references/baseline.json`, "Sean's own voice corpus," with no dial input — "Low CV vs the baseline → 'monotonous vs your voice.'" Recruiter-facing pieces are a named, real mode of this chain (value-engine: "will this land with recruiters"; the locked file's Round-7 substitution rule was "validated on Sean's own recruiter pass"). Concrete failure: a deliberately 40–60%-dialed piece reads flat-vs-baseline; the chain gate spends its ONE grounded revise pass on "revise against [flatness]"; voice grits the piece back up for an audience it was dialed down for; Sean hand-reverts — the grounding answer (c) failure, verbatim.

- **F6 · `structural` · storytelling-architecture (ledger) → writing-critique — the ledger's coordinates are beat-space; the draft critique checks is prose-space; nobody owns the mapping.** Ledger entries are keyed by beat number ("L1 — opens at beat 1 (why did the build break?) → closes at beat 5"), but the voiced draft has paragraphs, not numbered beats, and the voice stage legitimately compresses (its own conflict table says "Compress the cold open into 1-2 sentences"; merging is not reordering, and nothing forbids it). Critique must locate "beat 5" in prose by inference — re-derivation at the coordinate level. Concrete failure: voice folds beats 5–6 into one paragraph; critique can't find the close where the ledger says it lives and files a phantom blocking finding that burns the single revise pass — or maps wrong in the other direction and passes a genuinely unclosed loop. The parenthesized loop *question* already in the ledger format is prose-checkable; the beat number is not — and the check is currently keyed to the wrong half.

- **F7 · `structural` · writing-critique → writing-voice-modes (the revise back-edge) — the revise request travels naked, and the applied fix leaves no record for the stage that must protect it.** The headless gate emits "one structured revise request, 'revise against [this specific finding]', routed back through writing-voice-modes... then re-critique once" — nothing says the beat map, Handoff Block, and locked takeaway ride along, so the revision is written against a bare finding and can drift off-spine or quietly bend a beat. Worse: the fix list is defined as "the findings carried forward, the single highest-leverage fix, and the verdict" — after a successful revise the findings are *resolved*, so nothing enumerates what the fix changed in the prose. Humanity-pass protects "any prose change made to satisfy a critique finding," which it can only know from the fix list. Concrete failure: critique demands "end on the command, not the reflection"; voice complies with a deliberately blunt closer; re-critique passes; the fix list now carries nothing; humanity-pass reads the blunt closer as a tell cluster and re-smooths it. The gate's work is regressed in the very next stage, and Sean watches a fix he saw happen disappear from the final text.

- **F8 · `dangerously-wrong` · writing-critique → writing-humanity-pass — the verdict is emitted but binds nobody, and `structural-rework` has no route, so a gate-failed draft ships polished.** Critique's own semantics are strict: "A failed structure or value gate... forces structural-rework, not revise... a broken spine is not a line fix." But its headless algorithm has only two branches: blocking finding → one revise routed to voice-modes; "Else pass through to writing-humanity-pass." A spine break therefore gets routed to the one stage that cannot fix it without reordering beats (forbidden), and after the single revise + re-critique there is no branch for "still failing" — the draft passes through with the verdict recorded only "as a trailing HTML comment." Humanity-pass consumes the fix list but never the verdict; it has no precondition like "run only on ship." Concrete failure: in a headless run (Substack-Drafter is the named consumer in three of the five files), the chain polishes and delivers a post its own gate ruled unshippable — value-engine's named worst outcome, "polished slop," and a direct contradiction of its own contract: "Do not pass a failing piece downstream to be dressed up in good voice." The reader gets a published post whose central question never resolves; the only witness is an HTML comment.

- **F9 · `structural` · writing-critique → writing-humanity-pass — the fix-list intake is "when present" too.** Humanity-pass: "**When** the fix list is present, check the final scrub against it before delivering." Same disease as F4, one handoff later: in chain mode the fix list always exists (critique always emits it), so absence is a transport failure — and the scrub proceeds unprotected without saying so. Concrete failure: identical regression to F7's second half, triggered by transit loss instead of empty-list semantics; the two failure paths converge on the same silently-undone fix.

- **F10 · `structural` · substack-value-engine → orchestrator/Substack-Drafter — the chain's hardest gate is the only gate with no machine-readable verdict, and BLOCK/split have no wired route.** Critique defines a "machine-readable verdict block as a trailing HTML comment" for headless runs; humanity-pass defines its headless delivery format; value-engine — the one stage empowered to STOP the chain — defines no headless emission at all: PASS/BLOCK is prose ("BLOCK (with the missing slot named)"). And two of its verdicts imply a backward route that doesn't exist: BLOCK ("the chain stops here") and Rule-of-One's "split them into separate posts" both need to land somewhere, but the chain's only wired back-edge is critique → voice. Concrete failure: a headless drafter pattern-matching for content sails past a prose BLOCK and dresses up the failing piece; or a split verdict dead-ends because no stage owns re-mapping the surviving itch.

- **F11 · `structural` · grounding/stages 1–3 → writing-critique + writing-humanity-pass — per-piece user overrides have no carrier, so the chain's own exception clauses are unimplementable downstream.** The exceptions exist in text: value-engine suppresses the layoff "unless Sean explicitly asks for it in a specific piece"; the locked stage's Do-Not-Promote says "not even once, unless Sean explicitly asks"; the dial is chosen per context. But no artifact carries "Sean authorized X for THIS piece" or "dial pinned at 40% for recruiters," so critique's hiring-signal dimension ("ask stays sideways") and humanity-pass's scrub enforce the defaults against the authorized exception. Concrete failure: Sean explicitly green-lights the suppressed topic for one piece; three stages later his own pipeline flags it or strips it, burning the revise pass (or a hand-edit) to reverse a call he already made explicitly.

- **F12 · `minor` · storytelling-architecture → writing-critique — the scaffold choice and specificity-gate outcome are not in the Handoff Block.** Critique is told it "never re-litigates... the chosen scaffold," but nothing tells it which scaffold was chosen (and if the map itself is absent per F4, it can't even infer). Concrete failure: critique proposes an in-medias-res restructure for a deliberate Before-After-Bridge piece — re-litigating the committed map it was told to defer to.

- **F13 · `minor` · storytelling-architecture → writing-humanity-pass — forward-pull markers die in beat-space.** Storytelling instructs: "Note it as 'forward-pull line'; let writing-humanity-pass enforce the punctuation" — but the note lives in the map, the draft carries no markers, and humanity-pass replaces dashes mechanically (period → comma → colon → parentheses) without knowing which line is a load-bearing section-ender. Partially mitigated by the locked stage's scene-button discipline and VOICE-SAFE's preserve-the-function ethos — but "leave the function" can't be executed without knowing where the function is. Concrete failure: a section-end pull line becomes two clean sentences; that section now ends in exactly the "clean summary that gives the reader permission to stop."

- **F14 · `minor` · writing-humanity-pass → the published post — nobody strips the chain's HTML comments before publish.** Headless deliverables end with trailing HTML comments (humanity-pass's change summary; critique's verdict block travels with the artifact), and no stage or contract owns removing them from the publishable text. Concrete failure: `<!-- verdict: revise ... -->` lands in the published post's source/RSS — or as literal visible text if the editor escapes it — exposing pipeline internals to the exact recruiter audience the hiring-signal rules are calibrated for.

### Wow-gap scan (target: grounding answer (d))

Two concrete missing moves, in priority order:

1. **The end-of-chain Intent Thread Check (highest leverage, and the screenshot).** Humanity-pass already checks its scrub against one artifact (the fix list). Nothing checks the *finished piece* against the whole decided-intent bundle. The missing move: a final verification block — takeaway sentence present and welded to its artifact; every ledger question answered in prose; ask sideways; dial honored; verdict `ship`; fixes intact — emitted as a one-screen "thread intact" summary. That block is the wow bar made literal: proof of "a single thread of intent from beat map to published post," visible on every run.
2. **Loud degradation everywhere.** Every "when present" becomes "required in chain mode; absence is named and consequential." This one move converts the chain's failure mode from *silent drop* (undetectable, trust-destroying) to *loud drop* (a one-line re-supply fix). It closes most of F4/F9 on its own.

Lower-leverage polish: F12–F14 (each a one-line fix), and the coordinate re-keying in F6 once the manifest exists.

---

## Artifact 2 — Intent-Carrying Improvement Spec (chain-level)

Implementer note: you may be a smaller model than the one that audited. Every fix below carries its reasoning — when you hit an edge case this spec didn't anticipate, decide in favor of the reasoning, not the letter. One rule dominates everything: **`writing-voice-modes/SKILL.md` is Tier-1 and receives zero edits.** Every fix routes around it.

### Objective

The chain exists to move Sean's early taste decisions — beat map, value verdict, locked takeaway, voice choices — intact into a published Substack post that feeds his portfolio and a recruiter audience (grounding (a), (b)). The 2026-07-04 first pass named the right artifacts; what still leaks is their *transport and binding*: artifacts cross the locked voice stage on assertion rather than mechanism, every downstream intake silently tolerates their absence, the strictest verdicts bind nobody, and several real decisions (voice dial, per-piece overrides, applied fixes) have no carrier at all (grounding (c)). Left as-is, the chain's worst outputs are the ones Sean trusts most: a `ship` stamped by an un-anchored critique, and a polished post its own gate ruled unshippable.

### Desired outcome

Observable differences, before → after:

1. A mid-chain compaction or fresh-session resume can no longer silently un-anchor critique: it either has the ledger/verdict or it names the loss and refuses to stamp `ship`.
2. A recruiter-dialed piece survives critique without a gritting-up revise request; the one revise pass is spent only on real findings.
3. A draft that fails the structure/value gate can never arrive at Substack polished; the chain stops loudly with the draft and its manifest parked for Sean.
4. A blunt sentence the gate demanded survives humanity-pass on every run, because the applied fix is enumerated, not inferred.
5. Sean's explicit per-piece calls (a pinned dial, an authorized topic) are honored downstream instead of reverted by his own pipeline.
6. Every completed run ends with a one-screen "intent thread intact" block — the wow bar (grounding (d)) as a visible artifact rather than an aspiration.

### The backbone fix — the Chain Manifest (closes F3, F4, F9, F11, F12; enables the thread check)

**Change.** Promote stage 1's Handoff Block into a single accreting **Chain Manifest**: one fenced block, created by `storytelling-architecture`, appended to (never rewritten) by each subsequent editable stage, and **re-emitted verbatim at the end of every stage's output**. Fields:

- *Stage 1 writes:* working title; scaffold (F12); the beat map itself (numbered, one line per beat); open-loop ledger — with the loop **question text required** per entry (F6); central loop; crest beat; seam beat; section-end pull beats (F13); any per-piece overrides already known (F11).
- *Stage 2 appends:* Value Gate verdict + the three slots; the locked takeaway (the exact sentence); the Rule-of-One promise; pivot-line job (where + what it must assert); ask placement.
- *Voice boundary — owned by the orchestrator, not the locked stage:* whoever invokes the chain (the interactive session or the drafter agent) prepends the manifest to the voice invocation and re-attaches it to the voiced draft, adding a one-line **voice header**: mode, dial, borrowed techniques (F5). This is recorded *about* the voice stage from outside it; the Tier-1 file is not touched.
- *Stage 4 appends:* verdict; findings; and on any revise, the **applied-fix record** — finding → the quoted prose span that changed (F7).
- *Stage 5 appends:* the Intent Thread Check results (below).

**Persistence rule.** In-context re-emission is the default — this *respects* the existing "handoff is in-context, not a saved file" philosophy while fixing its physics: a block re-emitted at every stage boundary always sits near the context tail, which is what survives compaction. Headless runs (Substack-Drafter) persist manifest + draft together as the inter-process artifact, because there is no shared context to ride in.

**Intake rule (edit stages 2, 4, 5).** In chain mode the manifest is REQUIRED. When a needed field is absent: proceed with the degraded check, but *name the missing artifact in the output*, and — critique specifically — **cap the verdict at `revise`** while the ledger or Value Gate verdict is missing. Never `ship` un-anchored.

**Reasoning to carry.** The named artifacts already exist; the failure is that their transport across the locked stage is an assertion ("these travel in-context through the voice stage") that no one executes, and every consumer treats absence as normal. Assigning transport to the orchestrator makes the assertion an executable duty without touching the Tier-1 file. Re-emission (not a file) is the compaction defense for interactive runs. The verdict cap is the crux: a silent degrade converts transport loss into a trusted-but-wrong `ship` (F4, the audit's worst finding); a loud cap converts the same loss into a one-line fix — re-supply the manifest and re-run the gate. If you must choose between re-emitting the manifest "too often" and risking a silent drop, re-emit too often.

### Fix per finding

**F1 (structural) — one owner for the seam definition.** Edit `storytelling-architecture`: define the seam beat as *the beat where the central loop closes AND the Transfer lands — one beat*, and repair the example map so PAYOFF and SO-CAN-YOU are a single beat carrying both labels. Edit `substack-value-engine`: when an arriving map's seam beat ≠ the central-loop close, **return the map to storytelling by name** (a wired micro-back-edge) — never silently re-weld. *Reasoning:* value-engine owns the seam craft (its same-beat rule is the correct craft: the Transfer must feel like the story finishing), so storytelling's coordinates must conform at map-time, where a fix costs one beat edit — not at draft-time, where it costs a re-voice. The forbidden move is value-engine quietly relocating the seam: that is exactly the re-derivation the chain bans, even when the relocation is "right."

**F2 (structural) — pre-empt the reorder license from upstream.** Add one clause to the chain-contract sections of `storytelling-architecture` and `substack-value-engine`: "The beat map is not a 'skeleton' the voice stage may reshape; the personal-essay exception in `writing-voice-modes` refers to `creative-writing`'s format skeleton only. Beat order binds at every dial." *Reasoning:* the ambiguous sentence lives in a file that cannot be edited, so the disambiguation must arrive WITH the map. Upstream files already state constraints on the voice stage ("must never reorder beats") — this sharpens an existing constraint at the seam into the locked stage, which the Tier-1 rule explicitly permits.

**F3 (structural)** — closed by the backbone fix: the takeaway and verdict ride the manifest in and out of the voice stage via orchestrator re-attachment, and critique's value check keys on the manifest's promise + takeaway rather than trusting arrival. *Reasoning:* no acknowledgment can be demanded from inside the locked stage, so the boundary event has to be owned from outside it.

**F4 (dangerously-wrong)** — closed by the backbone fix (manifest + loud degrade + verdict cap). *Reasoning restated because it is the one to protect in every edge case:* critique's authority comes from being anchored; an un-anchored critique that still stamps `ship` is worse than no critique, because it converts a transport bug into trusted slop. When in doubt, downgrade the verdict and say why.

**F5 (structural) — carry the register decision; interpret the analyzer through it.** Voice header in the manifest (backbone fix). Edit `writing-critique`: an interpretation rule — analyzer deltas are read *relative to the declared dial*; at dial ≤ 60%, lower burstiness/grit vs the 100% baseline is expected and is not a finding; only deltas unexplained by the declared register escalate. Do NOT modify `baseline.json`, the MATTR window (locked at 50 by critique's own text), or the locked stage's dial table. *Reasoning:* the baseline is Sean-at-full-intensity; using it as an absolute target re-derives a register decision the chain already made, and the cost is concrete — the single revise pass is the scarcest resource in the chain, and a false flatness finding spends it making a recruiter-facing piece worse. The dial is an input to *interpretation*, never a knob to tune the analyzer with.

**F6 (structural) — re-key loop closure to prose-space.** Edit `storytelling-architecture`: ledger entries require the loop question text (the format already shows it parenthesized; make it mandatory). Edit `writing-critique`: closure is checked as "does the draft answer this ledger question?" — beat numbers demote to hints. *Reasoning:* the draft only exists in prose-space; questions are checkable there, beat numbers are not. This also makes legitimate beat compression (which the voice stage's own conflict table endorses) stop producing phantom blocking findings that burn the revise pass.

**F7 (structural) — the revise request carries the manifest; the fix list enumerates applied fixes.** Edit `writing-critique`: the structured revise request includes the manifest (map, Handoff Block, takeaway) alongside the finding; after the revise + re-critique, the fix list must contain the applied-fix record (finding → quoted changed span), not just surviving findings. Edit `writing-humanity-pass`: its protection clause keys on applied-fix records. *Reasoning:* "protect prose changes made to satisfy a finding" is only executable if those changes are enumerated; today the protection set is empty precisely when a fix was applied and resolved — the only time protection matters. And a revision written against a bare finding, without the spine in context, is a fresh chance to drift; the one revise pass must be the *most* anchored write in the chain, not the least.

**F8 (dangerously-wrong) — make the verdict a router, not a label.** Edit `writing-critique` (headless branch): split the routing — line-level blocking finding → one revise to voice-modes (unchanged); **structure/value-gate failure → verdict `structural-rework` and STOP the chain**: park the draft + manifest for Sean (or return to stages 1–2 in an interactive run); never pass through, including after a failed re-critique. Edit `writing-humanity-pass`: intake precondition — in chain mode, run only when the manifest verdict is `ship`, or `revise` with the applied-fix record present; on `structural-rework`, refuse and say why. *Reasoning:* critique's own semantics already say a broken spine is not a line fix; the headless algorithm just never got that branch, so it routes spine breaks to the one stage that cannot fix them without violating the no-reorder contract. The stop IS the success path for a bad draft — value-engine's contract is explicit that the worst outcome is a well-voiced piece that solves nothing. An implementer tempted to "be helpful" by letting the draft continue is recreating the bug.

**F9 (structural)** — closed by the backbone intake rule applied at `writing-humanity-pass` (fix list required in chain mode; absence named, scrub proceeds in maximum-preservation mode rather than silently unprotected).

**F10 (structural) — give the hardest gate the same machine voice as the others.** Edit `substack-value-engine`: emit the same trailing-HTML machine-readable verdict block the other two gate stages already define, on both PASS (verdict, slots, takeaway, promise) and BLOCK (missing slot, disposition). BLOCK and Rule-of-One "split" verdicts name their return target (backlog / back to storytelling for a re-map of the surviving itch). *Reasoning:* a uniform verdict-block vocabulary lets even a simple orchestrator branch on one format; today the only stage whose verdict can stop the chain is the only one a drafter can miss by parsing prose.

**F11 (structural) — a carrier for per-piece overrides.** The manifest's `per-piece overrides` field (backbone fix), written at grounding time: pinned dial + audience; any authorized suppressed-topic use, quoted in Sean's own words. Edit `writing-critique` (hiring-signal dimension) and `writing-humanity-pass` (scrub): consult overrides before flagging or stripping. *Reasoning:* every exception clause in the chain says "unless Sean explicitly asks" — but an authorization with no carrier is unimplementable three stages later, so the chain currently *reverts Sean's explicit calls by design*. Overrides are decided intent of exactly the same class as the takeaway; they ride the same vehicle. Edge rule for a weaker implementer: an override must be quoted, not paraphrased — a paraphrased override is how scope creeps back in.

**Minor findings, listed:**
- **F12** — add a `scaffold:` line to the manifest (stage 1). One line; lets critique defer to the committed shape by name.
- **F13** — stage 1 lists section-end pull beats in the manifest; `writing-humanity-pass` checks its dash replacements on those lines preserved forward pull (function, not punctuation).
- **F14** — `writing-humanity-pass` delivery: the publishable text contains zero HTML comments; the change summary and verdict block attach to the manifest instead. One sentence in its Step 3.

### The wow move (carry from the wow-gap scan)

Extend `writing-humanity-pass`'s existing fix-list check into the full **Intent Thread Check**, appended to the manifest as the chain's final act: locked takeaway present and welded to its named artifact · every ledger question answered in prose · ask sideways / overrides honored · declared dial consistent with the delivered register · verdict `ship` · applied fixes intact · zero dashes · zero HTML comments in the publishable text. One screen. *Reasoning:* this is grounding (d) made observable — the difference between a chain that claims a single thread of intent and one that can show it on every run. It costs one section in one editable file, because every input it needs already rides the manifest.

### What NOT to change

1. **`writing-voice-modes/SKILL.md` — zero edits, full stop.** Not Sean Mode, not House Style, not the signature moves, not the dial table, not its Related Skills paragraph. Every fix above routes through the four editable stages and the orchestrator. If an edge case seems to require touching it, the answer is: move the duty to the orchestrator or the adjacent stage.
2. **The named artifacts from the 2026-07-04 first pass** — Handoff Block, open-loop ledger, locked takeaway, Value Gate verdict, critique fix list. The concepts and names are right and are referenced by name across files; this spec changes their *transport and binding*, never their names or existence. Renaming breaks every cross-reference.
3. **Critique's one-revise-pass cap and its anchored-external-target rationale.** Do not add loops "for safety" — the cap exists because un-anchored self-judged iteration degrades prose toward generic. The fixes make the one pass better-anchored; they must not multiply it.
4. **The analyzer's advisory status, `baseline.json`, the MATTR window locked at 50, and the baseline regeneration pipeline.** F5 is an interpretation rule, not an analyzer change.
5. **Humanity-pass's identity:** the em-dash hard rule, the VOICE-SAFE/FULL split, per-section classification, and its runs-LAST position.
6. **The Value Gate's three slots and BLOCK-stops-the-chain semantics, Rule of One, and the seam-as-finishing-the-story craft rule.** F1 conforms storytelling's coordinates to value-engine's craft rule — the rule itself is correct and untouched.
7. **Stage order** and the in-context-first handoff philosophy for interactive runs — re-emission is the mechanism; files are for headless runs only. Do not invent a file bureaucracy for a five-message interactive chain.
