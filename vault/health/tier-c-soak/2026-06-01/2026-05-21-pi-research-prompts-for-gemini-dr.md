This article outlines a strategic workflow for manually executing "Deep Research" prompts via the Gemini Advanced web interface to gather information about the `pi.dev` developer platform. The central thesis is that by routing these queries through a web subscription rather than the Interactions API, the author can bypass the limitations of the current Qwen3-14B agent—which lacks training data on this post-cutoff platform and has demonstrated hallucinations—while simultaneously preserving a limited API budget.

The document provides seven specific, structured prompt templates designed to extract comprehensive technical intelligence. Key supporting points include:
*   **Disambiguation and Verification:** Every prompt contains a "critical disambiguation" clause to prevent the AI from confusing `pi.dev` with Raspberry Pi or Pi Network, and a "fail-loud" instruction to explicitly report if the platform cannot be verified.
*   **Targeted Technical Scopes:** The prompts cover specific integration topics, such as using OpenRouter for cost-capped workflows (Topic 15), configuring local Ollama instances on M4-series Macs and RTX 4090 hardware (Topics 16 and 19), and establishing interoperability between `pi.dev` and the Gemini CLI (Topic 17).
*   **Persona-Driven Research:** The prompts instruct the AI to act as a senior analyst writing for a specific user profile: a solo developer managing a 14-agent fleet using an Obsidian-driven workflow on high-end Apple Silicon and NVIDIA hardware.
*   **Standardized Output Requirements:** Each prompt mandates specific deliverables, such as comparison tables (e.g., Pi vs. Claude Code), verbatim configuration snippets, and annotated source lists with access dates.

The article concludes with a post-research protocol for an automated agent (Claude) to rename, index, and integrate the resulting markdown reports into a permanent knowledge vault. While the manual process is presented as a successful cost-saving measure, the document leaves open the question of how to handle future "emergencies" if the remaining $5.80 API headroom is exhausted, and it relies on the manual effort of the user to poll the web UI for completed reports.