---
type: manual-tickets
description: Hand-curated Manual lane for the Agent Fleet Observability kanban board. One ticket per `-` bullet under the appropriate section. See agent-fleet-observability/docs/manual-tickets-schema.md for the parser rules.
---

# Manual tickets

Sean's hand-curated kanban entries. Parser is `lib.readers.read_manual_tickets`
in the agent-fleet-observability repo. Empty sections are fine; the chip will
show 0 until items land here.

## Todo
- **WS3 spec 4 — the four judgement beats + D6 system panel SHIPPED in `seanwinslow.com` on `codex/judgement-beats` (2026-08-18; final specimen/rulings commit `e9be568`)** — All **eight numbered tasks (Task 0 through Task 7)** are closed: baseline specimens; typed beat/system data and 30-word cap; watched-red byte/page/CDP gates; shared `stepMotionChain`; Code Brain's stacked five-panel document; entrance/travel/clamp/keyboard controller; three-engine + SELFTEST certification; and final measurements/specimens/DESIGN rulings. The live Code Brain band lands beat 1 with beat 2 cut off, travels through all four beats, and seats the D6 system panel at the far clamp with beat 4 peeking; D6 keeps Sean's reason that the full-picture system is what an AI PM must know and explain, and every future project's beats + system ship together. The other six work pages are byte-identical under the established identity gate: zero JS, unchanged seven-asset graphs, gzip deltas `0,+2,+1,+1,+2,+1` within the unchanged 4-byte tolerance; Driftgate's before/after pairs are also pixel-exact at 1280 and 390. **Final gates:** 139/139 unit tests; Astro 0 diagnostics; Chrome/Safari/Firefox each 23/23 page checks green (`f8ffp6` / `f933fo` / `foqvzf`); Chrome 13/13 CDP checks green; SELFTEST `f92qor` produced `actual=fail` for 21/23 executable checks, including IDs 23/24, with only documented exemptions 7/20. **Measured:** band p95 `9.2ms` at 1× and `9.2ms` at 4×; clamp release `771→985`; final panel basis **62%**; panel height **572.9px** against the reference's **299px**. Code Brain is `193052→198274`, **+5222 B gzip** inside +8192. The dated spec §5 correction records the actual production shape: **one external script tag whose graph contains the WorkBeats page loader plus shared reel chunk, two emitted JS files total**; the gate pins ≤1 tag / ≤2 files, while the other routes remain zero-JS. No media was generated and the G1–G4 retreats remain recorded. **REVIEWED AND MERGED 2026-08-21:** fast-forward to `main` at `164dd73`, gates independently re-verified post-merge (139/139 unit, floors 23/23 + 13/13, byte gate green); both SDD execution ledgers preserved at `docs/superpowers/sdd/` (commit `8afc0d0`); worktrees/branches for spec 3+4 removed. **Sean's end-of-chain verdicts (screen recording at `docs/choreography/spec-4-screen-recording-review.mp4`):** home scroll = awesome, feels infinite; home warp = invisible → root-caused and FIXED same day (`ef138a3`: `:hover` never re-evaluates under a translating strip so zero plates carried the filter; all-frames warp measured p95 9.3ms on the B7 shell vs the pre-B7 16.7ms edge that justified the hover gate); work page = "getting there" but composition rejected → spec 5 (next bullet). — assigned: Sean
- **WS3 spec 5 — the north star SHIPPED in `seanwinslow.com` on `codex/north-star` (2026-08-22; specimen/rulings commit `8c85036`) — AWAITING SEAN'S PRODUCTION JUDGMENT** — D1–D5 are built: one-viewport work composition with the plate retired; Code Brain's five-panel band and single-scene shells on the other six routes; bidirectional tile↔index association; Max-drama SVG warp on all fourteen home plates; and one 2.8s arrival grammar with old-root scrim, 64px warped page rise, 84px warped strip sweep, footer last, intro handoff field, morph retired, and fail-closed opt-outs/belts. **A1 seam result and dated spec correction:** transparent-root T failed because Chrome painted an opaque destination canvas; pseudo-root P passed with a simultaneous live filter, so Chrome/Safari keep live DOM seated and rise `::view-transition-new(root)`, Firefox alone uses the DOM `rise-only-floor`; scrim is opacity `0.45` (a 55% dim). **Final gates:** 156/156 unit tests, Astro 0 diagnostics, 11-page build; Chrome `fng17c`, Safari `fqtbjj`, Firefox `ftrk6e` each page 25/25, each command's Chrome CDP tail 14/14; SELFTEST `ffq1c7` flipped every falsifiable check including 15/25/26. Chrome-command p95 home/band/reveal (1×/4×): `10.3/10.0`, `10.0/10.2`, `10.0/10.1ms`; Safari-command Chrome tail: `9.3/9.2`, `9.3/9.3`, `9.3/9.2ms`; Firefox-command Chrome tail: `9.1/9.2`, `9.3/9.2`, `9.2/9.3ms`; all 1× gates are under 16.7ms and every home leg reached 84px. **Measured composition:** `336px = 42svh`, panel basis `62% = 730.22px`, zero panel overflow, title-center delta 0, document height 800. **Every route delta/grant:** home `+6675/+12288`; Agent Fleet `+4085/+4096`; Anima `+4084/+4096`; Code Brain `+804/+8192`; Driftgate `+4088/+4096`; Vault Evals `+4086/+4096`; Groundwork `+4088/+4096`; Intent MCP `+4084/+4096`; Info `+3940/+4096`; shell topology stays one external tag/two local JS files and allowed kinds only. Six matched after specimens plus real mid-rise, warped strip-sweep, 84px flick, and association captures are committed; no generated media, dependency, WebGL, grant widening, or push. **Sean's remaining review, deliberately taste rather than gate:** judge the warp drama, page-warp legibility, reveal oneness, and production composition; the 84px home and 64px page dials stand ready, and everything beyond those dials is v2. — assigned: Sean
- **WS3 spec 2 — the three layouts SHIPPED in `seanwinslow.com` on branch `codex/three-layouts` (2026-08-17):** home is a captionless `2.4rem` two-column film strip; work pages are full-width title → media slot → one supporting row with no sticky index and a `← Projects` route verified by following it to seven tiles; About is four quadrants with Tools / Record / Elsewhere / Available, no Projects index, and Available removed from `/info/`'s footer but retained everywhere else. Gate: Chrome `fdklak`, Safari `f7vh3d`, Firefox `ff0vxv`, each **19 page checks green**; Chrome **9 CDP checks green**; 78 unit tests, Astro 0 diagnostics, 11-page build. The caption removal superseded the recorded plate-naming reason; it now stands because a looping reel cannot repeat captions and because `view-transition-name`'s permanent stacking/backdrop side effects belong on the picture, not a three-composition layout figure. Execution found and fixed three plan defects: the contradictory About footer instruction, an expected-red `&& astro check` that could never reach Astro, and a total-title-length guard disproved by wrappable `Vault Evals` (corrected to the measured unbreakable-word constraint). Deliberately left for the feel gate/spec 3: the stacked tile gap at 768–1366, whether Contact becomes a fifth About meta with the signature, and whether `2.4rem` reads too tight before the looping reel exists. — assigned: Sean
- **Golden Loop Phase B — research DONE 2026-08-17; two human steps remain, then Phase C**: verdict BUILD at n=15 (`research/2026-08-17-phase-b-falsification-verdict.md` incl. DR demand-side addendum; decision log through D7; name CLEAR; DR $2.80, program spend $6.92 of $15-25). Remaining: (1) **Sean sends the mock one-pager** (`product/mock-one-pager.md`, drafted with forward-test script) to 2-3 recent AI-PM hiring managers — D7's demand falsifier, capture their words verbatim into D7's review; (2) milestone Substack post #1 (the teardown story, per D5) when Sean wants it drafted — chain: storytelling-architecture → substack-value-engine → writing-voice-modes → writing-critique → writing-humanity-pass; (3) then Phase C wayfinder session (map decision tickets into GitHub issues, `grilling` stress-test before ratifying). Phase A sidecar (local-only): `~/.creative-harness/partner-sessions/2026-08-16-golden-loop-kickoff.md`.
- **WS3 four design decisions surfaced by the segerman audit — take them before the text beats** (2026-08-17): All four are `drift` in the audit's sense: nobody chose them, and DESIGN.md is silent on every one. Full evidence in `seanwinslow.com/docs/choreography/segerman-audit-2026-08-17/diff.md` §1–§2 and DESIGN §5.1 (commit `27e0eed`). **Ordered by evidence, cheapest and sharpest first.** (1) **Inner-page display scale** — one `calc(var(--fs-hero) * 0.52)` in `work/[slug].astro:100` and `info.astro:98`. Our home matches the reference to 3%; our About title is **3.95× smaller** than his and our project title **2.16×**. His token *grows* away from home and steps with the breakpoint; ours does neither. **Highest ratio in the audit, on a page Sean explicitly flagged.** (2) **The type-scale inversion** — one media query; at 1120px our hero renders at phone size (69.35px vs 69.22px at 390px). We copied his root rule on purpose, but his architecture switches at 1100px and ours does not, so we took the numbers without the thing that justified them. (3) **Route-transition duration** — 520ms vs his ~2.0–2.8s; tokens already exist, so it is one edit; §5 decided the mechanism and never the duration. (4) **The one-screen fixed shell** — genuinely architectural, do NOT decide in a hurry, but record it as open rather than leaving it invisible. **Then** text arrival beats, with the audit's §4 in hand: the reference does **not** do them in CSS (SplitText 3.13.0 driving GSAP against WebGL-composited Troika text inside a 2.6s router transition), so the zero-JS route is a *different, cheaper effect* rather than the same one for free. The §B research measurements all still stand. **A write-on seen for 500ms is the smallest lever the audit found**, which is why it now sits behind these four. **BOARD UPDATE 2026-08-16 (evening) — two of the four are specced AND planned; none executed yet.** Split into four specs after the scope outgrew one document. **Spec 0 — the home page collides with itself, 768–1366px** ([spec](../../../seanwinslow.com/docs/plans/2026-08-17-home-band-collision-spec.md) · [plan](../../../seanwinslow.com/docs/plans/2026-08-17-home-band-collision-plan.md), commits `dda76b9` `dde7e05`): found while chasing decision 2 to its cause, and it is a **live visual defect**, not a refinement — the headline runs under the project tiles at every width 768–1099 fine / **768–1366 coarse, i.e. every tablet in landscape**. Home only; project and About are CSS grid and reflow. Root cause: the layout and the type scale disagree about where a band ends. Fix is one media query; the work is the floors check L26/1 never had. **Execute this first** — it is a live bug and its outcome is an input to spec 2. **Spec 1 — display scale + renames** ([spec](../../../seanwinslow.com/docs/plans/2026-08-17-display-scale-spec.md) · [plan](../../../seanwinslow.com/docs/plans/2026-08-17-display-scale-plan.md), commit `f206359`): `--fs-title-about` **1.15×** / `--fs-title-project` **0.85×**, named in tokens.css; three titles shortened; verified to fit today's columns so it can land before the layout work. **Spec 2 (the three layouts) and spec 3 (1200ms + the velocity warp) are deliberately NOT written** — spec 2's inputs change when spec 0 executes (once home stacks at 768–1366 the film-strip question is a different question), and its core forks are Sean's taste rather than recall; spec 3 is speculative by agreement since the DOM-transform warp may be abandoned for the WebGL fork. **Handoff prompt carrying the reasons, the corrections and the open questions: `seanwinslow.com/docs/prompts/2026-08-18-portfolio-build-continuation.md`.** **Corrections worth not re-making**: (a) 'bridge the desktop band up to the tablet band' was my recommendation, Sean accepted it, and measurement then proved it would have **propagated the collision to every desktop width** — `--col-hero` is rem, `--x-media` is %; (b) the audit's 'type-scale inversion' row was the **symptom**, the broken band was the cause, and the audit missed it because it measured type sizes and box widths and never asked whether two boxes overlap; (c) `experience.md` §2's claim that both sites split text into `span.line` is **false for our site**. **Execution plan**: run spec 0 then spec 1 in fresh sessions (both plans are written to be context-free — if either needs explaining, the plan is defective), then brainstorm spec 2 with Sean after he has seen the stacked band. **BOARD UPDATE 2026-08-17 — specs 0 and 1 are EXECUTED and green; the board is now four specs, not four decisions.** Decisions 1 and 2 are closed (see the two tickets below for what each found). Spec 2 is **designed, specced, planned and handed off but NOT executed**: [spec](../../../seanwinslow.com/docs/plans/2026-08-18-three-layouts-spec.md) · [plan](../../../seanwinslow.com/docs/plans/2026-08-18-three-layouts-plan.md) · [kickoff](../../../seanwinslow.com/docs/prompts/2026-08-19-execute-three-layouts-kickoff.md). **The four-spec split is now three specs after spec 2**, confirmed by Sean: **spec 3** = the one-screen looping shell **and** the velocity warp together (they are the same machinery — a loop needs a velocity signal and that signal *is* the warp's input), **spec 4** = the four judgement beats that fill the project page's media band. Decision 3 (route-transition duration, 520ms → 1200ms) folds into spec 3 rather than standing alone, because it is the same moment.

    **BOARD UPDATE 2026-08-20 — SPEC 3 BUILT AND EVIDENCED; F4 TRIPPED, SO THE ARCHITECTURAL DECISION IS NOT CLOSED.** Option-B home now has a Base-owned sticky shell with feel-number `--reel-screens: 3`, 14 physical plates / 7 named projects, modulo loop, ported `100/.6/.07/7.5/.3/.05` accumulator constants (deliberately no Lenis), signed p95 normalization and CSS-only 20px displacement authority; the route morph is 1200ms. A1 found transformed-main ownership displaced sticky `0→800px`, so Base owns the outside-main shell. A2 found the named morph survived the translated sticky subtree at both measured sizes. A3 measured raw hard-flick p95 `14.1398` and chose rung 2. All-frame SVG filtering hit strict F2 at `16.7ms`, so the pre-agreed hovered-tile fallback shipped at `8.9ms` p95 (1×); positive and negative saturated-edge probes each found `0/4664` foreign pixels in both gutters, and the active-filter morph remained green. B3 loop plus keyboard/programmatic zero-speed checks are green. B5 measured page settled `380ms`, plate landed `1200ms`, gap `820ms`; Chrome/Safari/Firefox page suites were 21/21 and Chrome CDP 11/11. The corrected byte gate requires zero work-route JS, the same seven asset kinds (`html:1/css:1/woff2:5`) and gzip within 4 bytes, while home retains the original +12,288 denominator (`374057→375567`, +1510).

    **Four B6 calls:** loop = endless; warp = bend, not italic; 1200ms = one cinematic event; **ending = STOP, not land, so F4 IS TRIPPED.** Option A remains the pre-agreed retreat, but direct live testing disproved the promised one-rule reversal: one override fixes the visual shell and wheel input only, while a shippable accessible retreat needs at least `Base.astro` + `reel.ts`, updates to both floor harnesses, and full recertification. That expansion was not authorized in the B6 evidence task and remains open; do not call the branch functionally complete. Also open for final triage: B4's deferred Minor (no focused unit tests for invalid/missing/negative displacement parsing or signed-p95 scaling) and the mobile stacked view visibly rendering the physical wrap copy. Review packet: `seanwinslow.com/docs/prompts/2026-08-20-spec3-review-brief.md`; exact BEFORE/AFTER pairs and supplemental evidence are under `docs/choreography/spec3-specimens/`.

    **BOARD UPDATE 2026-08-20 — B7 PAID THE REAL REVERSAL COST; F4 AND MOBILE DUPLICATION ARE CLOSED.** Fine-pointer, motion-allowed desktop now uses a Base-owned fixed `100svh` shell with no sticky pin or document release range; wheel input still drives the modulo accumulator across 14 physical / 7 named plates. Native focus moves each of the seven real plate links to the visible strip origin, remains escapable to primary navigation, and holds both raw and visual warp velocity at zero. Reduced motion, coarse pointer and sub-1100 widths are ordinary flowing documents: no fixed shell, reel transform or warp, the non-semantic wrap copy is `display:none`, and exactly seven real plates/links render. Final Chrome evidence (`npm run floors`, run `fd86g3`) was 21/21 page + 11/11 CDP, with `shellRect=0..800`, `docH=800`, `releaseRange=0`, seven distinct focus transforms, 390px `visible=7`, reduced/coarse `visible=7`, and unthrottled reel p95 `9.6ms`. B6's sticky-release and mobile PNGs remain preserved as historical failure evidence; no media was regenerated. The B4 focused-unit-test Minor remains deferred for whole-branch triage because B7 did not touch displacement parsing or signed-p95 scaling.

    **BOARD UPDATE 2026-08-21 — SPEC 3 INDEPENDENTLY REVIEWED AND MERGED TO MAIN (`9c9cb38`, `--no-ff`, Fable 5).** The review re-ran everything rather than trusting the report: all 5 Important final-review findings confirmed fixed **in source** (controller-owned `data-reel-active` activation with live media-query lifecycle; pointerdown freezes momentum without repositioning so a trusted travelled click activates; `deltaMode` normalization + the spec §3 platform factors verbatim; the F3 gate now measures the visual seam by screencast frame-difference against a local-movement baseline with an induced-fault path; `--rotate-work-baseline` throws without a valid home denominator) and all 4 Minors closed (5 focused reel unit tests incl. cold opt-out lifecycle; RAF stops at convergence; F2 asserts a non-`none` hover filter; SELFTEST named faults). Fresh runs: 114/114 unit, Astro 0/0/0, byte gate green (home +2,274/12,288; seven work routes delta 0, 0 JS, 4-byte tolerance), floors Chrome `fgjsbz` 21+11 (1× p95 10.2ms, seam ratio 1.18 vs 2.245 limit), Safari `fi8bx4` 21, Firefox `fq2nmq` 21 — check 15 first-try green in all three — SELFTEST `fz48s0` flips every falsifiable check. The B5 byte-identity ruling verified landed as issued: defective assertion + measured 4-byte replacement recorded in the plan, token/baseline/gate rotated in one commit (`e72dbb9`). Specimens verified by eye: mid-wrap seamless, work page visually unchanged, 13-frame 1200ms strip reads as one event. **Taken on report:** the iPhone p50/p95 baseline and B6/B7 historical run ids. **Parked for Sean's end-of-chain review:** the four taste calls (loop, ending-as-fixed-shell, warp rung 2, 1200ms) with specimens under `docs/choreography/spec3-specimens/`, plus spec 2's 663px media band and the removed Next-project landmark.
- **segerman.dev FULL AUDIT — the north star, measured; blocks the paused text-beats decision** (2026-08-16) — **§B (experience measurement) DONE 2026-08-17**, commit `69b481a` in `seanwinslow.com`: `docs/choreography/segerman-audit-2026-08-17/experience.md` + 98MB of frames, both sites measured with one CDP rig at 390/768/1280/1920 in both themes; calibration gate passed (all 8 of our recorded numbers recovered, 520/170/150/230ms read off a live transition); **his project-page reveals captured for the first time** (306 frames @11.8ms, ~2.6s vs our 525ms); type scale inherited verbatim including BOTH inversions (760→768 −59.6%, 1080→1120 −40%, identical on both sites — at 1120px our hero is phone-sized); his display token goes 10.4→12→22rem across home/project/about where ours goes 10.7→5.56→5.56 (his About title 163px vs our 41px — this is the home/about difference Sean flagged); his dark mode is invisible to CSS and lives in WebGL; at ≥1100px his pages are one viewport tall and the DOM never scrolls. **Still open: §A (architecture archaeology, Codex) and §C (the diff + verdicts).** Original scope below.  Sean's call, in his words: *"I don't want to keep going down a rabbit hole of loop holes and workarounds if we have an end result to learn from and dissect at our fingertips."* **The real driver is his own observation** that he is *"noticing differences on our home page and about pages"* and wants to know they will *"sync up down the building pipeline"* — so this is a **diff**, not admiration. **Why now**: the site has been built against the reference through a keyhole — ONE screen recording of the intro scroll-up, and one analysis of that single moment, which itself records that there is **no footage of the project pages**; that absence is exactly why the route-transitions session could not judge sufficiency and had to ship on Sean's call. Everything else about the reference was *inferred from its shipped dependency list*. Doing this now costs one session; doing it after two more features costs this session plus rebuilding them. **Kickoff written: `seanwinslow.com/docs/prompts/2026-08-17-segerman-audit-kickoff.md`** (commit `f6d8199`). Three halves, deliberately matched to three different tools. **(A) Architecture archaeology** — library inventory with versions and *config constants* (the loader's catch-up 0.9 / ease floor 0.08 / min 2400ms are the model: find numbers, not names), barba route + persistence config, the velocity warp's uniforms and how Lenis drives `uSpeed`, payload per route, full page inventory. **Codex 5.6 SOL High is the right tool for this half** — patient long-context reading of minified bundles, and it keeps Claude usage for the half that needs a driven browser; repo precedent is `vault_critic` shelling out to Codex CLI at $0 incremental. Require file/line citations back into the bundles. **(B) Experience measurement** — layout box geometry, type (computed family/size/line-height/tracking/axes per role), colour, **timing read off `getAnimations()` rather than guessed from footage**, scroll choreography (ScrollTrigger start/end, what pins, what is scroll-linked vs time-based), and **the project-page reveals captured as stepped frames for the first time** — at 390/768/1280/1920, light and dark. **Claude Code driving real Chrome over CDP**, NOT the in-app browser pane, which reports `visibilityState==='hidden'` and would make every timing measurement meaningless. **(C) The diff** — three-column table (reference / ours / delta) per page type, and **every row must carry one of exactly three verdicts: deliberate** (we chose differently and DESIGN records why — e.g. DOM+video instead of WebGL meshes), **drift** (nobody chose it, and this is the category Sean is worried about), or **not yet built**. A difference with no verdict is an unfinished audit. **Gate before trusting any of it**: the measurement rig must first reproduce OUR OWN known numbers (`--vt-morph: 520ms`, the 107px display size, the 16+8 floors) — a rig that cannot get our documented answers right cannot be trusted on his. **Only Sean can supply**: what he *likes*. He walks the reference (project pages especially) and marks the moments — **Claude in Chrome** is the right tool for that since it drives his real browser — and recordings land in `docs/choreography/` (read-only bible). Ask him to point at moments, not to review numbers. **The line this does not cross**: measure structure/proportion/timing/behaviour; do NOT copy his source, shaders, assets, copy or artwork. The output is a spec in our own words and numbers. **Deliverables**: `docs/choreography/segerman-audit-2026-08-17/{architecture,experience,diff}.md` + `frames/`, a recommendation section **allowed to contradict DESIGN §5**, and §5 updated only where the audit contradicts it. If the reference does its text beats in CSS, that settles the paused decision. **DONE 2026-08-17 — all three sessions, commit `27e0eed`.** A (Codex 5.6 SOL High) read the shipped bundles with file/locator citations; B (fresh Claude Code) measured **both sites** in one Chrome over CDP and shipped a `verify.mjs` re-asserting every headline number against raw JSON; C joined them with a verdict on every row. **Verified before use**: ran B's verifier (all pass), confirmed our root-scale provenance (`tokens.css:18-21`), confirmed the 0.52 inner-title multiplier, checked we had not inherited his dead `minEase` (we had not). **Three corrections to the inputs**, incl. one to my own earlier lead — the type-scale inversion is NOT our defect, his site has the same two inversions at the same widths to the same percentages. **THE FOUR DECISIONS NOBODY EVER MADE** (all `drift`): (1) **inner-page display scale** — his display token GROWS away from home (home 10.4 → project 12 → about **22rem**), ours shrinks to one shared `calc(--fs-hero*0.52)`; at 1280 our home matches his to **3%**, project is **2.16×** smaller, About **3.95×** smaller, and the multiplier is a component-file comment absent from DESIGN entirely — largest ratio in the audit, on a page Sean named; (2) **the type-scale inversion** — we copied his root rule deliberately and credited it, but at 1100px HIS whole architecture switches and the inversion sits on that seam; ours has no seam, so at 1120px our hero renders at 69.35px, indistinguishable from its 69.22px at 390px; (3) **transition duration** — 520ms vs his ~2.0–2.8s in five beats; §5 decided the mechanism and never the duration; (4) **the one-screen shell** — every reference page at ≥1100px is exactly one viewport with `html/body` fixed, 60/60 elements pinned and a media reel that **loops infinitely and has no bottom**; ours is a 945–3276px document. Largest structural difference between the sites and DESIGN had never mentioned it. **§5 corrected**: ScrollTrigger is **NOT shipped** (only GSAP core's generic hooks name it — the 2026-08-15 dependency-list reading inferred it from behaviour); the '0.08 ease floor' is **algebraically dead**; Lenis confirmed as the velocity signal but with a desktop/touch split; barba's persistent-canvas rationale promoted from inference to confirmed. Pinned versions: GSAP+SplitText 3.13.0, Lenis 1.3.17, barba 2.10.3, Three r181, Astro 5.16.3. **New and consequential**: his page typography is rendered into **WebGL via Troika SDF text**, which is why `getAnimations()` returns 0 across all 26 of his cells and why every timing number for him is pixel-derived. **Where we are plainly better, by choice**: home **9,762 B vs his 1,838,648 B (188×)**, project **7,680 B zero-JS vs 3.2–4.4 MB (417–568×)**, we answer `prefers-color-scheme` and we survive a resize across 1100px — which **permanently breaks his page**. **Unjoined/unmeasured**: accent colour on both sides (B's detector failed), his declared route-change timing (no WAAPI object — read ~2.6s as **2.0–2.8s**), his other four project pages, any engine but Chrome 151.
- Council/discovery spend ledger records pre-flight reservation amounts as rows ($11.19 council-reserved vs $0.79 actual; $7.30 discovery-reserved vs $0.53 actual, 2026-08-16) — verify budget.py settle logic actually replaces reservations before trusting daily/monthly caps, else caps trip early — assigned: Claude
- C2 "Golden Loop" falsification pass before week-4 build start: 5-tool teardown (Braintrust, Langfuse datasets, promptfoo, Statsig, Freeplay) + optionally show C2 mock to 2-3 recent AI-PM hiring managers (prj-ai-pm-system-thinking-strategy)
- **WS3 text arrival beats — the pencil twist, and closing Firefox** (2026-08-17): Next and last item in DESIGN §5's build order before the optional velocity layer. Continuation prompt at `seanwinslow.com/docs/prompts/2026-08-17-text-arrival-beats-continuation.md` (commit `e10f540`). **The task turns on a measurement that re-opens §5's own wording.** The GSAP bullet justifies SplitText partly on *"GSAP arrives with the intro anyway"* — true on home, where it is a gated dynamic import for fresh capable visitors, and **false on a work page, which has never downloaded it**. Measured: `gsap.min.js` **28.4KB gzipped** + `SplitText.min.js` **3.7KB**, against a whole work page that is ~7KB gzipped and ships **zero external JS**. So the naive reading of §5 would put roughly four pages' worth of JavaScript onto the exact route whose transition was just built at zero JS. Not a reason to refuse — a reason the trade must be re-earned, with the **build-time-split alternative priced rather than assumed impossible** (Astro can emit the headline already split into spans, with CSS doing the animation and no runtime at all; `HandUnderline.astro` is existing in-repo precedent for a drawn stroke). **Firefox is now specified rather than vague** — see the route-transitions ticket above for the recon result; the fix is to make check 15 **engine-aware** over two real contracts (the morph where the engine has it, the instant-navigation floor where it does not), have it report which branch it ran, add `--firefox` to `floors.mjs`, and re-run the induced-fault proof afterwards so the Chrome branch still fails when the site is broken. **Hazards the prompt carries**: the handoff already moves `.home__left` on its own curve, so a write-on must compose with it rather than fight it; a repeat visitor never has an "as the intro settles" moment; a work-page title now arrives three different ways, one of them *inside* a view transition; floors check 1 asserts contiguous headline strings in the served HTML, which a per-character split would break — and that is the check working; and **the motion budget is FULL**, so this is a new trade against §5's ledger, not headroom. **Skills the prompt names**: brainstorming (required before the choreography), writing-plans, TDD, verification-before-completion, systematic-debugging; craft via impeccable, animation-vocabulary, 2d-animation-principles, micro-interaction-patterns, visual-polish-checklist, ux-design-guidelines, modern-web-design — and `gsap-scrolltrigger` **only if the zero-JS route loses the trade**, deliberately not loaded first. **§A DONE 2026-08-16 — Firefox is in the gate, commit `0dd6e9b`.** Check 15 was made **engine-aware rather than lenient**: two real contracts, exactly one per engine — the morph and matched pair where the engine has cross-document view transitions, and §5's floor where it does not (instant navigation to a **complete** page: destination matches the tile's `href`, exactly one hero plate, and **nothing left behind**). That last clause is the one worth having, because the failure the floor branch hunts is a *half* transition — an engine that begins what it cannot finish — which is invisible to "did the page change"; it is now asserted as `leftoverPseudos=0 activeVT=false`. **The branch is chosen by engine capability, never by what the run observed, and the check names the branch it took.** `PageRevealEvent` is a property of the browser, not evidence a transition would have run, which is exactly what makes it the right discriminator: a Chrome with the opt-in deleted still reports `supported=true`, still takes the strict branch, still fails — **proved by inducing that fault**, `verdict=BAD fails=5`, not by argument. `floors.mjs` gained `--firefox` (opened like `--safari`; Gecko has neither an occlusion flag nor a drivable debugging surface). **Green in three engines: Chrome 151 `run=f1rc7k`, Safari 26.5.2 `run=fxp577`, Firefox 153.0 `run=f1hvai`, each `ALL checks=16 bad=0 verdict=GREEN`**, CDP 8/0, `--selftest` still flips check 15, 72 unit tests, `astro check` 0 errors. **The honest remainder, now printed by the runner itself**: the CDP suite is Chrome-only (it is the only way to set `prefers-reduced-motion`, throttle a link or drop a GL context), so the verdict reads `page=green (Firefox)  cdp=green (Chrome)` rather than a bare `cdp=green` — **reduced motion is verified in Chrome and in no other engine**, and that gap now covers Firefox as well as Safari. **Still open: §B research gate onward** (the GSAP-vs-zero-JS trade with the measured numbers in front of Sean, the brainstorming-gated pencil choreography, both arrival moments, and the new coverage). **§B RESEARCH GATE DONE, AND THE DECISION IS PAUSED BY SEAN 2026-08-16 — commit `26bfe19` / `f6d8199`.** Research doc: `docs/plans/2026-08-17-text-arrival-beats-research.md`, every figure measured in-repo, sourced. **Provenance check first**: the inherited "28.4KB/3.7KB" named paths that do not exist (the minified UMD builds live under `dist/`); both figures re-derived correct at `gzip -9`, and both irrelevant because this site bundles through Vite. **What this project actually emits** (built a throwaway page importing gsap + gsap/SplitText exactly as the site would, walked the transitive chunk graph, deleted the probe): gsap **27,796 B** + SplitText **3,588 B** = **31,384 B gzip**, against a whole work page of **7,680 B with zero external JS** — **4.08×the entire page**. Tree-shaking buys ~500 B; GSAP registers plugins at runtime and does not shake. **The asymmetry is the finding**: "GSAP arrives with the intro anyway" is TRUE for home (3,588 B marginal) and FALSE for a work page (31,384 B, all of it) — and the intro gate is *fresh visitor*, so it is only true on a FIRST visit even on home. **SplitText solves a problem this headline does not have**: it is free/unrestricted/bundleable (Webflow, Apr 2025), `registerPlugin` is required specifically to survive tree-shaking, and the font-load race genuinely is solved by `autoSplit`+`onSplit` — but its value is re-splitting text that REFLOWS, and `AI Product<br />Manager` is two lines, three words, break hard-coded. **B3 measured, not reasoned** (probe animation injected from outside the site via CDP, driven through a real tile click): an unguarded beat starts immediately and burns **525 ms of itself under the crossfade**, most of it before the page is even opaque; and `:active-view-transition` matches from before `load` until `finished`, so **one CSS rule** — `animation-play-state: paused` — holds the beat at `t=0` and releases it exactly at handover, **zero JS**, degrading correctly by construction on deep links, in Firefox and under reduced motion (§C's three entry paths get one mechanism, not three). **Two floors are not neutral**: reduced motion is a global `*` sledgehammer so it covers a CSS beat FREE and does not touch a GSAP one (which would need its own `matchMedia` guard); and floors check 1 asserts contiguous headline strings, so a per-LINE split leaves it alone while per-word/per-char turn it red — the check working. **The pencil vocabulary is already deployed**: `.mark::after` draws left-to-right on `--ease-draw` at `--dur-draw:150ms` in pure CSS, and `HandUnderline` is an existing aria-hidden SVG stroke; handwriting-by-`stroke-dashoffset` is disqualified on live TEXT (letters must become paths, at which point they stop being text for selection/search/screen readers) but legal on that mark, which is what §2.1's exhaustive grammar demands. **PAUSED because Sean wants the reference to settle it** rather than choosing between inferences — see the segerman.dev audit ticket. The measurements stand whatever the audit finds; §D choreography was deliberately NOT decided (it is gated on brainstorming, and the motion budget is full).
- **WS3 intro round 2 — the resting chart was sub-pixel on every phone, focus escaped the cover, and three "green" checks were testing nothing** (2026-08-15): Coverage went 7→13 page checks and 6→7 CDP checks, all behind a new `npm run floors` (build → serve → drive → verdict, one command, replacing six manual steps and a `grep`). **Three real site defects.** (1) *The resting chart rendered sub-pixel on every phone.* DESIGN §5 already recorded a rendering failure — 0.90px lines at the 518×292 tile — and that 0.90 turned out to be plain arithmetic (5 plate-px × the 0.180 fit), so I wrote the arithmetic as a test that reproduces the recorded number and therefore predicts any viewport. It predicted 0.68px on an iPhone 15 and 0.65px on an SE — *thinner than the failure already ruled unacceptable*. The renderer floored only the moving vocabulary because the resting stroke had to match `settled.png`; that file was deleted last session and the parity contract is now a state contract, so the reason had expired. Floors extracted to `src/intro/floors.ts`, inert at fit 1 so still-parity holds by construction. Measured 1.60px on four device metrics and on Sean's real iPhone. (2) *Focus escaped the cover.* The armed state hides header, `<main>` and footer but left all three tabbable — a keyboard visitor could tab the whole site while looking at a loading cover. `inert` now, applied by the **inline pre-paint script** (doing it in the deferred module left a window the harness caught by sampling inside it), which required moving the `overlay` slot to last in the body so the footer exists by then. (3) *The loader narrated itself 101 times.* §9 asked for a progressbar **and** a live region; the markup gave both to one element whose value walks 101 steps. Split: progressbar carries the value with an `aria-label`, a hidden `role="status"` announces exactly twice, and its first message is the only discoverable form of the skip contract — which any key has always honoured and nothing ever mentioned. **Two WebKit gaps closed** that §5 had called unreachable: no-WebGL2 and context loss needed a *seam*, not a driver — the harness server injects the identical `getContext` patch CDP uses, and the parent is same-origin so it can call `WEBGL_lose_context` on the real canvas. Both green in Safari now. **Frame times exist**: the whole intro costs 1–3 dropped frames inside the first 230ms at renderer init, and the handoff — the moment the site is judged on — drops **zero** frames at 1×, 4× *and* 6× CPU; Sean's iPhone ran p50 17ms / p95 17ms across 645 frames. **A real phone ran it**, via a probe the harness server injects on `?probe` (Xcode is not installed so the planned iOS Simulator route was dead; the LAN + a real device is better evidence anyway). Suspects closed: `100vh`/`100svh` diverge by a real 108px but `vh` ≡ `lvh` so the panel can never peek, costing 154ms of a 900ms sweep that the cover's own drift covers; chart floors confirmed on-device; no unrequested context loss. **The round's standing lesson, now in §5: a harness can be green while testing nothing.** The bfcache check passed while never exercising bfcache (the harness server's own `no-store` disqualified the page — found only by making the check report Chrome's `notRestoredReasons` instead of guessing); one check asserted the *opposite* of its contract by navigating "back" to `?intro`, which is a standing instruction to replay; and two a11y assertions passed against a CSS variable name and a comment about the attribute being removed. All three caught by watching each check fail before trusting it pass. Commits `523b9f5` `3ad35d4` `5c8e213` `85c82b8` `2f8bf10` `b86e353` `1e4a699` `b3e7188`. **Open for Sean, both needing him and neither blocking:** (a) *the touch-skip suspect is still open* — a phone has run the intro end to end but nobody has tapped one, so whether a tap skips too readily and whether the consumed tap declines to fall through to a link are unmeasured; the probe is built and it is one URL. (b) *Composition on portrait phones is a taste call, not a defect* — the chart contain-fits to a 393×219 band, 26–33% of screen height with 15px bots, floating in the plate's painted starfield; measured 1.26× of headroom before any cast sprite would touch an edge, so a fit-to-content option exists and is one line, but it crops plate margin and is his call. Also still unverified and stated as such in §5: reduced motion in WebKit (needs the real OS setting) and Firefox entirely. **Sean's two calls, taken this session and recorded in §5 so neither reads as an open question later:** portrait composition stays AS IS (the fit-to-content option was measured and declined — the covering starfield makes it read as a small constellation in a large sky, which is the point), and the section-G feel items (unlit lamp, idle variants, headline text-arrival beat) were each offered and each declined — next is CSS view transitions, as the build order already said.

- **WS3 intro floors harness built and run — it found the intro DEAD in every production build** (2026-08-15): Task 12 closed, and it earned its keep on the first run. **Rewrote the spec before coding it**: three of the plan's five checks asserted an architecture retired by the cover pivot (a settled `<img>` on `/`, a `static` state, a `loading→settled` walk with no `tracing`), so implementing them verbatim would have produced a harness that passed while asserting a contract the site no longer has; corrected list with every assertion traced to the line it comes from at `seanwinslow.com/docs/plans/2026-08-15-intro-floors-harness-checks.md`, and the plan's stale list kept unedited under a warning because an unrecorded drift is what caused the 518px-tile session. **Two production-only defects found, both of which had passed 37 unit tests, `astro check`, a clean build and Sean's feel gate.** (1) *The intro never ran in production at all.* The CSS minifier reserialises `--intro-max-wait: 15000ms` as `15s` and `--intro-floor: 3500ms` as `3.5s`; `index.ts` read them with `parseFloat`, which drops the unit — so the 15-**second** asset deadline was a 15-**millisecond** one and every fresh visitor to the built site retreated to the bare page ~50ms in, with the intentionality floor at 3.5ms. Dev never minifies, so every check anyone had run was looking at a different build from the one a visitor gets. Fixed with a unit-aware `cssTimeMs` plus a test that bans `parseFloat`-on-`getPropertyValue` anywhere in `src`. (2) *A stalled chunk hung the cover forever over an invisible page.* The deadline covered the asset phase only; a chunk request answered by nothing hangs `import()` before that race is built, and `renderer.init` can stall behind it — held open, the cover sat at 0% with `<main>` still parked 100vh down, which is precisely the segerman.dev failure DESIGN §5 stakes the contract on. One watchdog now spans the whole pre-canvas pipeline and stands down when the rAF loop starts. **Verified**: `scripts/intro-check.html` 7/7 green in real Safari 26.5.2 AND real Chrome 151 (no-JS floor, decoded pixels, repeat visitor, fresh walk with 101 monotonic % steps, stuck-asset retreat at 15.0s with `sw-intro-seen` deliberately unset, cover-equals-viewport, no residual transform); `scripts/intro-cdp-checks.mjs` 6/6 green in Chrome — reduced motion, no-WebGL2, `WEBGL_lose_context`, Fast-3G (at 100% all 16 assets really are in), offline mid-load, stalled chunk. The plan called Steps 3–4 manual; CDP is HTTP + a WebSocket on localhost, so they are permission-free and repeatable instead. **The harness is proven able to fail**: `?selftest=1` falsifies six of seven checks, and checks 6 and 7 had their real faults induced at the site one at a time (cover moved back inside `<main>` → 771×508@25,572 in an 820×540 viewport, check 6 red and only check 6; `intro-armed` left out of `disarm()` → check 7 red on all three assertions). 43 tests green, `astro check` clean, build clean. Commits `b9383f7` `7dc2eee` `80520cf` `c8cbadb`. **Two things for Sean.** (a) *Behaviour change he should know about*: a genuinely slow visitor (Fast 3G) now gets the complete page at 15s instead of the intro at ~31s and is not marked seen, so the intro still waits for them next visit — that is what a 15s max wait means, and `--intro-max-wait` is the knob if he wants slow visitors to still get it. (b) *Raised as a note, then **actioned on Sean's call the same session** (`54edacc`)*: `public/intro/settled.png` was 4MB shipping in `dist` and fetched by nothing — excluded from the byte manifest by name, referenced only by comments and the compositor that writes it. Deleted; `dist` drops from 41M to 37M. The parity contract it stood for was never a pixel comparison against the file (the timeline test asserts the final STATE), `npm run settled` regenerates it from `cast.ts` on demand, and `build-byte-manifest.mjs` keeps skipping the name on purpose so a regenerated file can never enter the loader's byte budget. Re-verified after the rebuild: `dist/intro` holds `plate.webp` and the sprites only, the 16-file manifest is unchanged, 43 tests and `astro check` clean, floors harness still 7/7 green in Chrome 151. DESIGN §5 amended again to record it. **Still not verified, stated plainly in DESIGN §5**: the six CDP checks are Chrome-only (WebKit has no equivalent no-permissions driver, so reduced motion, no-WebGL2 and context loss are confirmed in Blink and reasoned in WebKit); Firefox untested; no touch device. **Next per DESIGN §5 build order**: CSS view transitions (`view-transition-name` morphing tile → work-page hero), which is also what delivers the project-page reveals Sean flagged on segerman.dev; the velocity layer stays last.
- **WS3 intro v3 SHIPPED — full-page cover + Act-2 rewrite; Sean's feel gate open, Tasks 12/13 remain** (2026-08-15): Diagnosed Sean's Act-2 verdict with instrumentation, not guesswork (dev-only `window.__intro` probe on `/dev/scrubber/`, renderer state sampled every 0.05–0.1s). **Two root causes.** (1) *Placement drift*: the intro shipped as a 518×292px hero card inside `.home__media`, so lines rendered at 0.90px, signal dots at 2.9px and arrival overshoot at ~1px — the whole Act-2 vocabulary sat below the perceptual floor, which is exactly why the trace "died off" while the hub halo read fine. DESIGN §5 never specified placement; its skip-strike→*headline arrival* grammar and segerman.dev loader parity both describe a page-owning cover, and the tile came from the build plan misreading a comment that said the intro "lands **above** this". Sean confirmed the original intent (full-page loading cover). (2) *The end-dump*: ten of twenty edges drew simultaneously in 0.24s after all motion stopped — the literal "cut to all agents connected". **Ratified by Sean and built** (commit `7ada2af`): full-viewport fixed cover that fades out on settle (static-hero state RETIRED — repeat/reduced-motion/no-WebGL get the page immediately; no-JS floor is the cover's default `display:none` + inline pre-paint gate); plate now CONTAINS with the ground still covering (cover-fit cropped most of the fleet off a portrait window); envelope 8.0→**8.5s** (Act 1 4.0→3.5s, freeze 0.6s, Act 2 3.4→**4.4s**); **the wake now reveals the whole network dormant** as a radial wave ordered by true distance from the hub, the trace lights paths bright over it, and the settle *converges* — nothing is added at the end, and selectivity is finally legible because there is something to be selective against; halos capped at 3 + hub idle (was 6 at once); hub pulses instead of sitting pinned at 1.0 for 3.35s; failure now reads (dot stops dead via new signal `to` truncation, real gap snaps open, tool-use flinches, hub spikes-then-dips, monitor's beacon flash moved here where it means something) and its payoff is bound to its cause via a new `heal` beat (was housekeeping at TOTAL−0.14); arrivals rebuilt on 2D principles (peak deformation 7%→20–55%, squash/stretch along+across the travel vector, anticipation, arcs, decaying follow-through, irregular cadence; new drop/rise/sprout styles). Renderer gained `lit`/`bold`/`gap`/`gapAt` + screen-space floors so an active path is bold at any viewport while the resting chart keeps its exact plate stroke. **36 tests green** (2 new), astro check clean, build clean, final-frame parity verified live (0 non-settled edges, 0 halos at 8.50). Plan: `seanwinslow.com/docs/choreography/motion-revision-2026-08-15.md`. **Feel gate PASSED 2026-08-15** ("looks AMAZING") with one note: the ending was abrupt. Fixed in `cc80fec` — a 380ms hold (the brief's "hold the completed system for a beat") then the cover lifts off the top edge over 940ms while `<main>` rises 56px behind it and the blocks stagger, chrome last; cover travels 781px to the page's 51px, and that ~15:1 parallax is what reads as depth. Two bugs surfaced building it: the cover was mounted inside `<main>`, which the handoff transforms — a transformed ancestor becomes the containing block for `position:fixed` descendants, so the cover sat 51px low (Base gained an `overlay` slot outside `<main>`); and `--intro-floor` was still 4000ms after ACT1_END moved to 3.5s, so the ratified 8.5s piece was quietly running 9.0s (token fixed, and a test now pins `--intro-floor` to ACT1_END inside DESIGN §5's range). 37 tests green. **Handoff then inverted again (`67e3a4e`)** after Sean compared it to segerman.dev: "it doesn't feel like the loader slides up, it feels like the home page scrolls in." Frame-analysed his screen recording (`docs/choreography/segerman.dev-website-inspo/`, 8fps across the transition; write-up at `docs/choreography/segerman-handoff-analysis-2026-08-15.md`). **The reference's loader never leaves — it stays put and is covered**, while the page arrives over it as an under-scaled panel that grows to full-bleed; v1 had the agency on the wrong object. Rebuilt: cover only drifts -7vh, `<main>` sweeps `translateY(100vh) scale(0.94)`→home over 900ms as an opaque panel above it, blocks carry additional travel+stagger inside the panel (hero moves ~957px of its own, which is what reads as "scrolling"), plus a DOM-transform approximation of the velocity warp (skewY on text/media, flattening before rest). **Deferred by design**: the true velocity-coupled warp and the project-page reveals — both need Lenis as the velocity signal (§5 pins ≥1.3.26, lands *with* the effect) and a DOM-vs-WebGL-mesh decision our architecture hasn't made; §5's build order already sequences them after CSS view transitions and they should stay there. **DESIGN §5 amendment DONE (`bbab30a`, Task 13 Step 2)** — §5 had drifted from lagging the code into contradicting it, so this went past the planned one ledger line: contract text kept intact (it is what Task 12's harness checks against), amendments added in the file's own dated-marker convention — the intro is a full-page cover (with the drift that produced the 518px tile recorded explicitly, since an unrecorded drift caused it); **the static-hero state is retired** (that sentence was actively wrong); the `--intro-floor` ↔ `ACT1_END` invariant; the 8.5s two-act envelope + Act 2 never scrubs; contain-vs-cover; the handoff; `?intro`; L20 fleet pick superseded by the 15-role pivot; a ledger row for the handoff plus "the intro's slot is spent, not reserved"; and the Lenis/velocity bullet updated with the frame analysis. Explicitly NOT claimed: the plan's wording would have had §5 assert the floors were "verified by the Task 12 harness" — it has not run, and §5 now says so. **Task 12 CLOSED 2026-08-15** — the floors harness was built and run; it found the intro dead in every production build. See the next ticket. Note: Astro ignores the preview harness's PORT and picks its own fallback (4322) — read `preview_logs` for the real port.
- **WS3 constellation intro v2 BUILT — architecture pivot complete, Sean's feel gate open** (2026-08-15) *(superseded by the v3 entry above)*: Sean pivoted the constellation to his agent-fleet architecture (ChatGPT reference image + 2 motion clips, now in `seanwinslow.com/docs/choreography/` with `system-architecture-notes.md`). Shipped same-day: beat-map-v2 (15 roles, 20 directed edges, arrival + trace skeletons, commit `e0bb5d2`), 12 new role sprites (round 5, approved register) + 3 carried over, body-height normalization fix (`9394e08`), v2 data layer (15-role cast, EDGES table, curved/arrowed/dashed settled chart, `185cd4a`), v2 motion layer (8s two-act master: 4s governed arrivals -> 0.6s freeze -> 3.4s selective system trace per the notes script; curved edge engine w/ arc-length dashes, signal dots, halos, fail+recovery beat; 34 tests incl. final-frame-equals-settled parity, `4e04387`). Verified live: full run completes on `/?intro`; scrubber states match the map at 1.06s and 5.62s. Manifest 1.46MB. **Feel gate verdict (2026-08-15)**: sprites great, arrivals good, but Act 2 collapses after the hub wake — 'dies off and immediately cuts to all agents connected' (prime suspect: the settle-window edge dump + 53-beats-in-3.4s density). Handed to a fresh session via `seanwinslow.com/docs/prompts/2026-08-15-intro-v2-trace-polish-continuation.md` (observe-then-design-then-code method, Sean sign-off on the revised beat sheet before implementation, then Task 12 harness + Task 13 DESIGN §5 close-out). Known dev-only quirk: backwards scrubbing past an entrance can resurrect hidden bots (idle breaths are absolute-value; production is forward-only).
- **WS3 intro build inputs secured; three gaps remain before code** (2026-08-15): Pre-build readiness pass. **Closed**: L20 research reports persisted to `seanwinslow.com/docs/plans/2026-08-15-constellation-intro-research.md` (they existed only in session transcript); the two approved Seedance twins downloaded into the repo at `docs/choreography/twin-{mixed-fleet-bfe98494,claude-bot-750650a6}.mp4` (1280x720, 8.04s each — they existed ONLY as Higgsfield CDN URLs, a single point of failure for the choreography bible; found via `generate list --size 100`, the default 20 pages them out). Commit `0bc40f8`. **Remaining before code**: (1) **implementation plan doc** — superpowers:writing-plans, first thing in the build session; (2) **choreography bible decomposition** — beat-by-beat shot list with timings extracted from the twins (frame extraction, same technique as the seam work); (3) **sprite assets + background plate DO NOT EXIST** — the intro's actual art; generation needs Sean's explicit approval per standing rule, plus a decision on the cutout source (frames lifted from the Claude-bot twin vs fresh per-bot generations + remove_background), and the alpha-bleed export step must be part of whatever pipeline is chosen (pencil-edge halo risk, doubled by dual grounds). L20 fleet pick (Claude-bot vs mixed) stays callable on the real page by end of Week 2 per DESIGN §5; Claude-bot-first is the working default. Installed skill set is sufficient (pixijs-2d, gsap-scrolltrigger, sprite-asset-pipeline, 2d-animation-principles + Context7 for live Pixi v8/GSAP docs); no new tooling or purchases needed.
- **WS3 Week-2 trades DECIDED against the segerman.dev benchmark; kit §6.1 added** (2026-08-15): Sean ratified the researched recommendations conditional on matching segerman.dev's motion/micro-animation/delight with his own twists. Verified the condition against the reference's own shipped bundles before recording: **segerman.dev is an Astro site running Three.js custom shaders + GSAP/ScrollTrigger/SplitText + Lenis + barba — the four Week-2 candidates were literally its dependency list.** Its loader ships the exact displayed-vs-real contract in DESIGN §5 (catch-up cap 0.9, ease floor 0.08, min 2400ms) but has NO global bailout — observed hanging black at 27% indefinitely in the sandboxed browser on one stuck asset promise (per-video 30s timeouts exist; images have none), which proves Sean's max-wait bailout line is load-bearing, not polish. Its home media are WebGL meshes with a vertex shader whose `uSpeed` uniform bends media by scroll velocity — **Lenis is that shader's input signal, not scroll polish**, which is why adopt-alone buys nothing. Trades recorded in DESIGN §5 (commit `18fd21c`): intro GO (researched Pixi v8 + GSAP stack); route transitions = pure-CSS `@view-transition` with ClientRouter+transition:persist as the only upgrade path, never barba; **GSAP text choreography AMENDED skip → restrained adopt** (arrival moments only, pencil write-on twist — SplitText is part of the reference's feel and free since Apr 2025); **Lenis AMENDED skip → deferred-until-effect** (lands only together with a designed velocity-driven paper effect, DOM-transform first, pinned ≥1.3.26, wheel-only). Build order: intro → CSS view transitions → text arrival beats → optional velocity layer. Kit gained **§6.1** (same commit): diagnose seam character via step/seam share before re-rolling; uniform-step seams close deterministically with a smoothstep gain ramp over the final ~1.25s, geometry-neutral, proven by re-running measure-motion-fit.py. Nothing built yet; next session starts the intro with the day-one timeline scrubber.
- **WS3 Agent Fleet graded clip ADOPTED (option A) — blink closed at $0** (2026-08-15): Sean picked A off the review console. Raw clip archived to `src/assets/motion-archive/agent-fleet-observability-clip-superseded-2026-08-15-raw-blink.mp4`; graded candidate installed at `public/media/motion/agent-fleet-observability.mp4` (h264 High, yuv420p, 1040x586 — kit spec). Verified on the installed file: seam **5.06 → 2.86**, brightness step **−4.94 → −0.64** (the small deltas vs the pre-encode 2.94/−0.58 are the h264 round-trip). `scripts/measure-motion-fit.py agent-fleet-observability` re-run as proof: entry **identical** (fit 0.796, drift 0.00%, crop 0.15) — luminance gain is geometry-neutral, `motion-fit.ts` untouched. Committed `b965348` in seanwinslow.com. Candidate file left in gitignored `motion-review/` per never-delete rule. All seven tiles now carry brightness-neutral seams ≤4.75 except eval-suite's accepted 12.42 drift case. **Reusable technique for the kit if it recurs: when a seam is mostly a uniform luminance step (check step/seam share), a smoothstep gain ramp over the final ~1.25s closes it deterministically — no re-roll.** Week-2 trade decisions still pending Sean (intro GO / CSS @view-transition / Lenis skip / GSAP-text skip).
- **WS3 Week-2 brainstorm delivered + Agent Fleet seam finding: the 5.06 is a full-field blink, not shimmer — decision pending Sean** (2026-08-14): Two research passes ran under the L20 rider ($0, web + repo). **Agent Fleet eye-check finding**: the accept reasoning ("5.06 sits between Anima 4.68 and Intent 4.75") compared numbers whose characters differ. Measured first-vs-last across all seven shipping clips: six have brightness-neutral seams (global step ≤1.35 gray, ≤29% of seam) — line shimmer, reads as pencil boil. Agent Fleet's −4.94 full-field step IS 98% of its 5.06 seam: the bloom's next breath has already begun at frame 121 (brightness 223.7 → dips 203.7 mid-pulse → recovers ~220 → descending again at end), so every loop snaps the whole tile ~2% brighter in one frame — a flash, the artifact the eye is tuned for, on the brightest clip in the set. This is prompt-kit finding #3 (oscillator rest position unpinned) surfacing at the seam. **$0 deterministic fix exists and is rendered**: `motion-review/CANDIDATE-agentfleet-v3-graded.mp4` — same frames, smoothstep luminance ease (gain → 1.0226) over the final 30 frames; seam 5.06 → 2.94, step −4.94 → −0.58, mid-pack and brightness-neutral. Gain doesn't move gradients so the motion-fit entry should hold; re-run `scripts/measure-motion-fit.py agent-fleet-observability` on adoption as proof. Review console delivered in-session (hover sim with real fit transforms + isolated seam-moment loops + evidence table). **Options: A adopt graded / B keep raw / C re-roll with bloom rest pinned per kit §4. Sean's eye decides; nothing in `public/` touched.** **Week-2 trade recommendations brought (brainstorm only, nothing built)**: (1) Constellation intro GO — PixiJS v8 pinned WebGL (not WebGPU; fallback edge-failures, issue #10906) + GSAP master timeline (GSAP 100% free since Apr 2025, Webflow acquisition), gated dynamic import in a plain Astro script so repeat/reduced-motion visitors never download the ~200KB; honest research answer: Canvas 2D would carry 10-30 sprites fine, WebGL is justified by the paper-grade shader + per-sprite effects, and Canvas 2D remains the structural retreat if built behind a thin renderer interface; loader = paused timeline + slower-of governor over fetch-stream bytes against a build-time byte manifest (Content-Length untrustworthy); pencil-alpha halo hazard → alpha-bleed export step + both-grounds edge QA; 6.5–8.5 build-days, tail risk = choreography feel → day-one timeline scrubber harness; prior art One Last Beat (Awwwards) is exactly Pixi+GSAP. (2) Lenis SKIP — payoff ~zero on short pages, touch off by default, permanent rAF loop, reduced-motion respect only landed v1.3.26 (2026-08-05), documented conflict with Astro scroll restoration (astro#12725). (3) barba SKIP, use pure-CSS cross-document `@view-transition` instead — barba dormant (last release 2024-08); Astro ClientRouter unneeded for 10 static routes; CSS-only transitions: Chromium + Safari 18.2+ yes, Firefox degrades to today's instant nav, no-JS floor untouched by construction, gate behind `prefers-reduced-motion: no-preference`; this is the one candidate that changes navigation nearly free. (4) GSAP text choreography SKIP as standalone spend — GSAP enters as the intro's infrastructure; a second text-arrival beat competes with the skip-strike → headline moment and drifts toward template furniture. Ship gates unchanged: GitHub #4659070 · privacy pass · resume link · 404 line · 3 npm advisories.
- **WS3 Safari check PASSED, and it OVERTURNED the documented 10-bit failure mode** (2026-08-14): Ran the ship-gate Safari pass in a real Safari **26.5.2** (`Version/26.5.2 Safari/605.1.15`, `navigator.vendor = Apple Computer, Inc.`), verified not the Chromium preview pane. **Result: 7/7 clips decode and play**, no media errors, all 1040x586, playback advancing, and canvas pixel spread 32-65 — i.e. real decoded frames, not a blank surface reporting itself as playing. **The fit transforms also measurably land in WebKit**: gradient correlation raw -> corrected, code-brain 48->82, driftgate 37->78, groundwork 50->76, anima 34->69, intent 25->64, agent-fleet 26->62, eval-suite 42->56. **THE CORRECTION: "Safari cannot play H.264 High 10" is FALSE.** That claim was recorded in this file and in the prompt kit, and was always flagged as inferred from a VideoToolbox refusal rather than tested. Now tested by rebuilding the exact defect from the raw Seedance source: an H.264 **High 10 / yuv420p10le** file **plays in Safari** (1040x586, spread 31), and so does the raw **HEVC Main 10** at 1920x1080 (spread 31). Also worth knowing: **`canPlayType` is not evidence** — Safari answered `"probably"` for High 8-bit, High 10 AND HEVC Main 10 alike; it parses a codec string and never attempts a decode. **The `-pix_fmt yuv420p` rule still stands**, on the narrower and honest ground that High 10 drops off the hardware-decode path and a page with several live clips should not burn CPU/battery on software decode when 8-bit is free. Prompt kit §6 corrected and a new **§7** documents the harness. **The harness is the reusable part**: reading a browser title needs macOS Automation permission (it hung on the consent dialog) and screenshots need Screen Recording; neither is necessary. Serve over `python3 -m http.server`, have a self-testing page beacon its result via `new Image().src='/__result?'+summary`, and read the 404 line out of the access log — zero permissions. Check decoded PIXELS (canvas std-dev), never `readyState`. Page lives at `dist/safari-check.html` + `dist/probe/*.mp4` and is disposable — `dist/` is rebuilt, and the kit's §7 has enough to rebuild it. **Ship gate closed.** Remaining: GitHub Support #4659070 · independent privacy pass · resume link on Info · 404 line approval · 3 npm audit advisories.
- **WS3 Groundwork semantic audit CLOSED; agent-fleet replaced; DESIGN.md trued up; motion-fit tooling moved into the repo** (2026-08-14): **Groundwork audit — the suspected accumulator is FALSIFIED.** Ran a per-pixel temporal activity map and a first-vs-last seam map over all 121 frames. The archive slot (stacked spheres, bottom-right foundation niche) has **zero activity** — nothing ever arrives there, so it cannot accumulate; the concern in the handoff was hypothetical and the drawing simply never animates that region. **The whole foundation is loop-safe**: only the gear rosettes (rotation, a perfect cycle) and the central drafting-ink column (accent bloom, pulsing) move below the ground line, and **0% of the seam is below ground**. **The real seam is 100% above ground**, on the four horizontal crate lanes and the two ramps (left helix, right chute): the crates advance but do not complete a whole number of pitches in 120 frames, so the closing frame is offset from the opening one. That is the even-spacing-advance rule failing, NOT an accumulator. Two secondary notes: the third floor mixes travelling crates with static round-headed fixtures sitting in the lane (hazard #2, reads fine because they are clearly background machinery), and **the lanes do not run off either edge of the frame**, so §7.1's closed-circuit escape hatch is unavailable and the loop can only close by exact pitch. **Verdict: leave it.** The mismatch is spread across dozens of small crates rather than concentrated in one high-contrast element, which is why 3.51 reads as faint shimmer rather than a cut; the composition is strong and a re-roll risks more than it buys. If ever re-rolled, the prompt needs an explicit advance-by-exactly-one-crate-spacing clause plus a hold on the round-headed fixtures. **Agent Fleet replaced**: Sean's brighter Photoshop redraw + v3 clip. Raw download was HEVC **Main 10 / yuv420p10le** — re-encoded to the kit's spec (h264 High, yuv420p, crf 20, 1040 wide, no audio). Loop seam 5.06 (was 2.48), mid-pack against anima 4.68 / intent 4.75; bloom pulses as an oscillator and the composition holds, so accepted. Old still + clip archived to `src/assets/motion-archive/*-superseded-2026-08-14-v2.*`. **DESIGN.md trued up**: new **§3.2** records Sean's call that no night grade ships (with the mechanism preserved so it is not re-derived: lit windows / moon / lantern are bare cream paper, identical in value to the margin, so no global tone curve can separate light from paper and inversion must turn every light into a hole); **§5 motion budget restated** as a spend ledger, recording that the scrapped cursor trail's slot was spent on tile hover motion rather than banked, and flagging that Week 2's four candidates are unbudgeted — with the note that Lenis and route transitions change navigation rather than decorate it, so their trade is against the intro's job, not against the marks; **§9** stale "red underline" fixed. Same stale-red comments fixed in `src/styles/tokens.css` §8 and `src/styles/base.css`. **New repo tooling**: `scripts/measure-motion-fit.py` (self-contained uv script) replaces the scratchpad chain — run it after ANY clip re-roll. It flags `slack < 0` (transform would expose an edge) and `drift > 0.6%` (framing MOVES during playback, which no static transform can fix). **It found one: eval-suite drifts 1.80%** against a 0.00-0.45% floor everywhere else, which is why its fit tops out at 0.681 while the other six reach 0.78-0.94. That tile keeps a small residual shimmer on hover; accepted, since re-rolls stopped improving it. **Also caught a bug in my own tool**: the first `drift` metric compared only horizontal translation, so a pure push-in would have reported a clean 0.00% — exactly the defect it exists to catch. Now takes the worst spread across all four parameters. **Anima push-in: not present.** Measured drift 0.20%, about 1px at tile size, i.e. the measurement floor. Nothing to chase; Sean's read that the image and video look great as-is is correct.
- **WS3 hover jump diagnosed and fixed in two passes; Higgsfield CLI was logged out** (2026-08-14): Sean reported every tile's art shifting up-and-slightly-left and changing size on hover. **Cause: Seedance does not return the frame it was seeded with.** Pass 1 — every clip comes back framed 1-3% wider and 1.5-3.5% LOWER than its seed, vertical offset 2-3x the horizontal, which is exactly the "up and slightly left" Sean saw. `SceneFrame.astro` had asserted the 220ms crossfade hid this; it did not, and a crossfade across a misalignment is a morph, which reads worse than a cut. The offset is CONSTANT within a clip (5 of 7 measured zero movement first-to-last frame at a 0.25% grid), which is what makes a static per-scene fix legal. **The naive fix fails**: several scenes need 3.4% of downward shift but only 1.4% of upscale, so shoving the clip down walks it off its own top edge and opens a gap — neither field of view contains the other. Fix zooms BOTH still and clip onto the field they share. Pass 2 — a residual remained because pass 1 used a single uniform scale and **the clip is also SQUEEZED**; Sean caught it by eye as "slightly more compressed and squished together". Measured, it is systematic: every clip needs **1.45%-1.71% more vertical scale than horizontal** (~8px in a 520px tile). Source is the resolution chain — drawings are 1344x752 (1.7872) or 1672x941 (1.7768), Seedance renders 1920x1080 (1.7778), encode lands at 1040x586 (1.7747); nothing in it preserves the seed's proportions. **Only the CLIP takes the anisotropic correction**; the still is zoomed and cropped but NEVER stretched. Gradient correlation at 320px, raw / uniform / +aspect: code-brain .341/.774/.878, groundwork .360/.719/.826, driftgate .241/.748/.812, anima .215/.732/.777, agent-fleet .195/.635/.752, intent .180/.687/.689, eval-suite .298/.509/.538. Correcting aspect also SHRANK the crop (worst 2.80% -> 2.16%; four scenes now under 0.2%) because a better-fitting clip overlaps the drawing more. Files: `src/data/motion-fit.ts` (new, carries numbers + reasoning) and `src/components/SceneFrame.astro`. The still is only cropped where a clip can actually be reached, so reduced-motion / touch / no-hover show the drawing whole. **Follow-ups: (1)** any re-rolled clip invalidates its `motion-fit.ts` entry and must be re-measured — eval-suite's is knowingly stale the moment Sean's web-app regen lands. Scripts in order: `align/register_cover.py` -> `align/register_aniso.py` -> `align/fit_aniso.py` in the session scratchpad. **(2)** `higgsfield` CLI was logged out (eval v6 died 19:10 with `Not authenticated`); Sean has re-authenticated. **(3)** a long-running `npm run dev` serves STALE cached image transforms — the :4321 server predating commit baddd0f was still showing the three RETIRED concepts (gate, checkpoint, open film strip) while disk held the new ones. Restart the dev server after replacing any scene asset, or review against `npm run build`.
- **WS3 night treatment: Sean's call is that the scenes need NO night grade — DESIGN §3.1 must record it** (2026-08-14): After screening all seven on both grounds in a live console (5 candidate treatments applied to the real stills and clips, $0, zero generation), Sean's verdict: *"these look great on light and dark mode as is... I don't think we really have to worry about anything clashing."* Agreed on the evidence — **the scenes are ALREADY night scenes** (deep drafting-ink skies, dark forests, heavy graphite mass), so on charcoal they read as lit plates on a dark wall rather than as cream rectangles. The L21 screening's "cream rectangles on charcoal" finding was made against an earlier, lighter scene set and aged out when the scenes were redrawn. **DESIGN.md §3.1 still calls the night treatment an OPEN design problem and dark mode an "acknowledged placeholder" — now stale, needs the decision written in.** Worth preserving in that edit: the reason the invert failed is structural, not a tuning miss — **in these drawings the lit windows, the moon and the lantern glow are BARE CREAM PAPER, identical in value to the margin**, so nothing in the luminance signal separates light from paper and any global tone curve must treat them the same; inversion therefore turns every light source into a hole. Console preserved in the session scratchpad; treatment 4 (edge feather only, art untouched) remains available as a ~10-line change if the tile edge ever wants softening.
- **WS3 stale references: three surviving "red" mentions + the motion budget line** (2026-08-14): Found while reading for the night-treatment work. The accent changed to drafting ink on 2026-08-13 (DESIGN §2.1) but three places were never updated: **DESIGN.md §9** still specifies *"Keyboard focus = instant **red** underline"*; **`src/styles/tokens.css` §8** is headed *"Red usage — EXHAUSTIVE"* and asserts *"red is NEVER a text color on the dark ground"*; **`src/styles/base.css` ~L81** repeats *"On the dark ground red is a mark only"*. All three should read drafting ink / lifted ink. Separately **DESIGN §5's motion-budget line is stale in both directions** — it reads *"full (intro + trail + marks + grain)"* but the cursor trail was SCRAPPED and tile hover motion was ADDED without the trade being recorded. That line gates Week 2 (Lenis, barba-pattern router transitions, GSAP text choreography, WebGL constellation intro under the L20 research rider), so it needs restating honestly BEFORE any of that lands. Zero-risk doc work; no code or media depends on it.
- **WS3 scene set rebuilt loop-first; DESIGN §7.1 records two concept changes** (2026-08-14): All seven tiles animate. Three scenes were redrawn because they were *structurally* unloopable — not badly prompted, but drawn so no prompt could rescue them. **Driftgate: the gate is gone** (film-strip road + barrier across the lane + terminal correction mark) **and is now the flip test** — an animator's light table, two drawings on a peg bar, inspector flipping one against the other, crew still behind the door per DESIGN §1. **Intent MCP: the border checkpoint is gone, now the kid in a tent** (Sean's own "in-tent" pun from the previous portfolio) with the agent half-materialising out of the drawing, unfinished parts dotted. **Anima: same staging, film strip closed into an unbroken ring** (pages arriving at a reel was an accumulator). **Eval Suite** kept its concept, redrawn within it (identical standing figures at regular pitch, all instruments moved overhead; the seated-robot-with-a-cube had no coherent next position and popped). Recorded in DESIGN.md §7.1 along with the generalized hazard list, and §7's stale "red accent" line fixed (superseded by §2.1 on 2026-08-13). Seams: code-brain 2.22, driftgate 2.41, agent-fleet 2.34→2.48 pending, groundwork 3.51, anima 4.68, intent 4.75, eval-suite 12.35 (re-roll running). Prompt kit at `seanwinslow.com/docs/prompts/2026-08-14-scene-motion-prompt-kit.md`.
- **WS3 media ops: three failure modes that cost hours, all now documented** (2026-08-14): (1) **`higgsfield generate list` only returns `completed` jobs** — an in-progress job is absent from it. Reading that absence as "the job was dropped" sent a whole diagnostic down the wrong path; use `generate get <id>`, and `generate create` *without* `--wait` to get the id immediately. (2) **Render times vary 4x by scene** — most clips finish in 6–7 min, Code Brain takes ~26, Eval Suite has run 39+. A short client timeout is indistinguishable from a failure. Use `--wait-timeout 55m`. (3) **Passing a local path to `--start-image` re-uploads the file every submission**, and Higgsfield runs an IP check on new media; referencing it before the check clears refuses the job — the web app says *"IP check not finished for input media"*, the CLI surfaces a bare **HTTP 502** with no explanation. **Fix: `higgsfield upload create` once, wait ~2 min, reuse the UUID.** Sean found this one from the web app UI after the CLI gave nothing useful. Also: **encode must pass `-pix_fmt yuv420p`** — Seedance returns 10-bit and the earlier encodes carried it to H.264 High 10, which VideoToolbox refuses and Safari cannot play (all six re-encoded from raws; unverified in a real Safari, worth one manual check before ship).
- **WS3 open before ship, unchanged** (2026-08-14): GitHub Support **#4659070** still open · independent privacy pass not run · resume link on Info unresolved · 404 line *"This one got erased."* still awaiting approval or replacement · 3 `npm audit` advisories from Astro's own sharp/esbuild pins (build-time only) · **scene night treatment still an open design problem** (DESIGN §3.1) and now more visible with seven refreshed scenes, since dark mode still shows cream rectangles on charcoal · **DESIGN §5's motion-budget line is stale** — it reads "intro + trail + marks + grain" but the trail is scrapped and tile hover motion was added, so the budget needs restating before Week 2 adds Lenis / barba transitions / GSAP text / the WebGL constellation intro.
- **WS3 code-brain motion clip: three attempts, all hang in `in_progress` — cause still unknown, both hypotheses dead** (2026-08-14): **Correcting an earlier claim in this file:** the resubmits did NOT "leave no job record". `higgsfield generate list` only ever returns `completed` jobs (verified: 20/20 items completed, a known-live in_progress job is absent from it), so the jobs were there the whole time and I read their absence as deletion. The real signature is simpler and stranger: **code-brain video jobs are created normally, enter `in_progress`, and stay there far past the ~6-7 minutes all six sibling clips took.** The original `fe59c3fd` eventually resolved to `failed`; job `bc4d1646` sat `in_progress` for 11+ minutes under direct polling. A two-arm diagnostic was run to discriminate: **Arm A** same seed with the glow-breathing and star-twinkling clauses removed (testing whether many small luminance oscillations over a dark field were the cause), **Arm B** original prompt verbatim with the seed downscaled 3.1MB PNG -> 270KB JPEG (testing whether the largest-of-seven seed was the cause). **Both arms behaved identically to the untouched attempts, so both hypotheses are falsified** — it is neither the prompt's luminance clauses nor the seed payload. What is left is something about this scene's content specifically; the only structural thing that still distinguishes it from the six that worked first try is that it is the sole night scene, a large dark low-texture field carrying many small high-contrast light sources. That is a description, not a diagnosis. **Do not spend more on this without a new idea** — three renders are already sunk. Site is unaffected: six of seven tiles animate and `SceneFrame` treats a still-only tile as a complete designed state.
- **WS3 text-to-video test CLOSED: image-to-video wins, hypothesis did not survive** (2026-08-14): Ran the pre-approved test on Eval Suite (chosen because it was both the worst loop on record and the ideal loop candidate, so the i2v regen was a live control rather than a historical one). Seams — 2.5 i2v **6.64**, 2.5 t2v **6.29**, 2.0 t2v **7.80**. The premise was that a model authoring its own frame zero would loop far better; it bought **0.35 of a seam point**, and the 2.0 arm was actually worse than the seeded control. Cost was severe and landed on locked rules: Seedance 2.0 drew a **ruled construction grid** across the paper (DESIGN §3 bans ruled lines outright) in a generic explainer-video register that §1's anti-template rule exists to keep off the site; Seedance 2.5 held the pencil far better but cropped to a macro shot that threw away the composition, dropped the caricature register to plain smiley robots, and turned the **entire drawing** drafting-ink blue, past even the relaxed §2.1 reading. **The real finding: loop quality is a PROMPTING property, not a seeding property.** The seeded regen moved 13.29 → 6.64 on prompt changes alone, a bigger swing than any model or mode choice produced. Do not re-open t2v for the tile set. Review console preserved at `ws3/review/index.html` in the session scratchpad; three-way clips are side by side and looping there.
- **WS3 shipped clips were 10-bit H.264 — fixed, but check any future encode** (2026-08-14): All six tile clips shipped as H.264 **High 10** / `yuv420p10le`. Seedance returns 10-bit and the earlier encode command had no `-pix_fmt`, so ffmpeg carried it through. VideoToolbox refused to hardware-decode them and fell back to the software decoder — VideoToolbox is the path Safari depends on for H.264, and Safari has no High 10 support, so these were plausibly dead tiles in Safari. All six re-encoded 8-bit from the original raws at the same crf 20 / 1040 wide / no audio (no double resample). **Any future tile encode must pass `-pix_fmt yuv420p -profile:v high`.** Unverified tail: this was not confirmed in a real Safari, only inferred from the VideoToolbox refusal plus documented High 10 support — worth one manual Safari hover before ship.
- **WS3 eval-suite clip is 1425KB, ~1.7x the band the others sit in** (2026-08-14): The loop-first regen carries a continuous fall plus five cycling stations, which is simply more motion to encode; at the locked crf 20 that lands at 1425KB against the 400–840KB the other five occupy. Not a bug and not a blocker — clips are `preload="none"` and load one at a time on first hover — but it is a real size-versus-motion call Sean has not made. Options if it matters: accept, or cut the station count in a future regen. Do not silently raise crf; 20 is locked after the crf 31 disaster.
- **WS3 SESSION HANDOFF 2026-08-14 — continuation prompt written, two tasks queued** (2026-08-14): Repo `~/Code-Brain/seanwinslow.com` at `ecc353b`, PRIVATE, no remote. Week-1 exit bar essentially met: 10 routes clean, all copy placed and approved, portrait with hover reveal, seven drafting-ink tiles placed, **six of seven with hover motion wired**. Code Brain's clip was still rendering at handoff (job `fe59c3fd-2c5f-4f97-9395-9b72e3701aa6`) — collect it, encode crf 20 / width 1040 / no audio, drop at `public/media/motion/code-brain.mp4`. **Continuation prompt:** `continuation-prompts/2026-08-14-ws3-week1-continuation.md`. **Two pre-approved tasks:** (1) regenerate `eval-suite` + `intent-engineering-mcp` motion loop-first (even spacing / closed circuits / oscillators; keep the reject-bin beat but stage it as recurring); (2) **test text-to-video against image-to-video** — one tile, Seedance 2.5 `t2v` vs Seedance 2.0 `t2v`, no start image, measure loop seams, Sean decides. Hypothesis: a model authoring its own first frame may loop far better since it is not fighting to preserve someone else's drawing; if it wins the whole set may be regenerated that way. **CURSOR IS SCRAPPED** — the onion-skin never read as onion skin and the light-table fix recoloured the entire dailies-desk ground; Sean will explore alone, do not propose cursor treatments unprompted. Nothing cursor-related ever entered the site, only demo artifacts, so nothing to revert. **Technical findings that must not be rediscovered:** video models here have NO negative-prompt support (early negation-stuffed prompts likely CAUSED the camera drift blamed on the model); always open with Sean's anchor line *"Flat 2D hand-drawn cel animation, animated on twos"*; encode crf 20 at 1040 wide (crf 31 cut a 3.47 Mbps source to 174 kbps and mushed the linework — paper grain is the worst case for h.264); **do NOT go 4K**, a tile renders ~520 CSS px so 1040 is already 2x; stills stay the art-directed drawing since SceneFrame crossfades 220ms and hides the 2.8–6.3% drift; **loop quality is semantic not pixel-level** (Driftgate measures 3.27/255 "seamless" and still looks wrong because the actions were terminal) — cyclical loops, terminal never will; batch submissions can be silently intercepted by a preset recommendation, check `submitted_count` and resubmit with `declined_preset_id`.
- **WS3 ACCENT DECIDED: drafting ink `#2F5D7C` / `#6BA3C9`. Motion route decided: Seedance 2.5 with a cel-animation anchor line** (2026-08-13): Sean chose drafting ink after seeing all four side by side, knowingly accepting that it is more than a scarce accent (verbatim: *"I know it's more than just an accent, but it looks beautiful and I don't mind more of that beautiful blue color being involved on the page"*). Applied to `tokens.css` + `DESIGN.md` §2.1, which records the relaxation AND its scope: blue may carry atmosphere inside the artwork, but the UI mark grammar is unchanged and exhaustive, and there is a new **mark-over-tile legibility check** because a blue mark on a blue wash stops reading as a mark. **Motion:** Sean's prompt fix beat the model swap. Opening with *"Flat 2D hand-drawn cel animation, animated on twos"* pins Seedance 2.5 to a 2D register and it holds paper, linework and wash perfectly; the earlier failure was a missing style anchor, not the model. Kling 3.0 also holds (no reference role to fall into) but its motion is stiffer. **Route: Seedance 2.5 + anchor line.** Web weights: 58KB (Seedance) / 77KB (Kling) at 760 wide, so seven tiles is well under 1MB on first hover. **Rider that applies to both:** the camera drifts despite a locked-camera prompt and Seedance redraws the composition slightly, so **the static tile must be the video's own first frame**, which also demotes the generated stills from deliverables to seeds. **Headshot resolved:** `sw-color-headshot-2.jpg` measured — background moved sage `#9BBEAA` -> teal `#87B9B6`, blue-to-green gap closed from -15.6 to -2.1. With the tiles going blue this retires the earlier clash objection entirely; the photo now joins the cool family and the warm skin is the single warm note. **Still open:** the motion budget (DESIGN §5 says adding requires removing; recommendation remains the onion-skin trail, and Sean is now weighing it against a live tunable demo at `WS3-trail-demo.html` + artifact), and regenerating the remaining six tiles against the new accent.
- **WS3 BLUE VARIANTS + HOVER-MOTION TESTED — two findings that reverse the swatch verdict** (2026-08-13): Generated 3 blue variants of the Code Brain tile (the hard case: it is the only one of seven where the accent lands on *light* rather than an object) plus a working hover-motion test. Console `WS3-blue-variants.html` + artifact. **Finding 1, colour:** red and blue do not behave the same way in a graphite night drawing. Red has nowhere to belong in a moonlit pencil scene so it stays put and reads as an intervention. Blue belongs everywhere (night, moonlight, water, cold) so it *leaks*. **Drafting ink `#2F5D7C` (Sean's swatch favourite) broke the one-accent discipline outright** — it took the sky, the moon halo, the lake and the hills, and became a blue picture rather than a drawing with an accent. **Blue pencil `#3A4FA0` was the only blue that held the line** (bloom behind the house, cream sky, graphite landscape, structurally identical to the red control). Screen-blue `#4A7FB5` sits second and is the most thematically pointed (windows read as monitors, on-thesis for an overnight fleet). **Finding 2, motion:** Seedance 2.5 is the WRONG model — it silently demotes `start_image` to a style reference (response echoes `reference_images`) and re-imagines the scene, throwing away the cream paper and returning a painted night illustration. **Kling 3.0 is correct** (only exposes `start_image`/`end_image`, no reference role to fall into): paper, linework and red bloom survive every frame. Two riders: (a) Kling still drifts the camera despite a locked-camera prompt, so the static tile must be **the video's own first frame**, not the drawing, or the tile jumps on hover; (b) weight is a non-issue, 5.9MB raw compresses to **77KB at 760 wide**, so seven tiles is ~0.5MB loaded on first hover. **Accidental lead on the night-mode problem:** the failed Seedance render darkened everything while *keeping the lit windows lit*, which is exactly what the banned inversion could not do. **Motion budget still needs settling** — DESIGN §5 says the home budget is full, so adding hover motion requires removing something; recommendation is the onion-skin cursor trail (L7), which fires on the same mouse movement over the same region, but that is Sean's lock to break. **Also: Sean says he re-edited the colour headshot to add blue, but no newer export exists anywhere on disk** — `sw-color-headshot-1.jpg` is byte-identical to the committed copy and still samples sage green. He needs to export it.
- **WS3 accent colour + tile treatment OPEN — lab built, awaiting Sean's eye** (2026-08-13): Sean proposed swapping the locked col-erase red (L2) for a blue, reasoning that his headshots are blue and the signature character is blue-derived. **Sampled the actual pixels: the headshot ground is sage green `#9EC0AC`, not blue** (skin `#E79849`, hair `#653C19`, shirt `#19211F`). That makes red a complement to the photo's ground and analogous to skin and hair, and a blue accent a near-neighbour to the sage, which is the harder relationship. Also the portrait is now B&W at rest with colour only on hover, so at rest there is no hue to clash with at all. **Not a veto** — blue pencil `#3A4FA0`/`#7A8DE0` keeps the pencil story intact (the blue col-erase is the red one's twin, and the P&P house style originally used blue construction lines), but the change should not be made to fix a clash the pixels say is not happening. Lab at `WS3-accent-tile-lab.html` + artifact: four accents x four tile treatments, both grounds side by side, live-switchable. **Key research finding:** segerman's home media are duotone monochrome rendered in the site's own two colours, with full colour only on the work pages — so Sean's Lightroom black-and-white instinct IS the reference's own strategy, arrived at independently. What does not transfer is that his sources are photographs with full tonal range while these are drawings that are mostly bright paper, which is exactly why darkening the paper kills the lit windows. Sean also wants the tiles regenerated with whatever accent wins.
- **WS3 scene night treatment is an OPEN design problem — the invert is dead** (2026-08-12, L21 screening ran): Sean judged all seven scenes under the warm cream-invert and rejected it outright, verbatim *"I don't like the inversions at all. They look demonic and scary."* Recorded in the new repo's `DESIGN.md` §3.1. **L21's immediate-generation rider is deliberately NOT triggered** — the failure is in the treatment, not the seven scenes, so generating seven night versions against an unsolved treatment would waste seven assets. Two findings that constrain the eventual answer: (1) the paper grade alone is not a night treatment, it reads as slightly dimmer and the scenes stay cream rectangles on charcoal, and it is what dark mode ships today as an acknowledged placeholder; (2) inverting a drawing whose subject is illumination turns every light source into a hole — the 2AM windows, the lamps, the moon all become black discs, which is structurally most of why it read wrong. **Next: brainstorm alternatives with Sean** (candidates worth putting up: redraw-for-night per scene, a lamplight patch that keeps the light sources lit while darkening the paper, a duotone regrade that never crosses zero, or accepting cream plates on charcoal as a deliberate pinned-drawing look). Screening console: `WS3-night-screening.html` + artifact.
- **WS3 Week-1 build STARTED — repo scaffolded, document renders, blocked on copy + assets** (2026-08-12): New PRIVATE repo at `~/Code-Brain/seanwinslow.com`, **no remote, nothing pushed** (commit `3ef1217`). Astro 5.18.2 static, 10 routes building clean: home, info, 404, 7 work pages, plus sitemap and llms.txt. `src/styles/tokens.css` is the single source of truth per L26/2 (rem-vw root at vw/172.8; Anybody 900 wdth 95 at 10.7rem, scaled down from the console's 11.4rem per Sean's L16 rider; micro tier collapsed to two steps). DESIGN-v2.md moved in as `DESIGN.md`. **Honesty mechanisms are build-enforced, not documented:** `src/data/figures.ts` fails the build on any disputed figure (verified end-to-end against `synth.partial_nights`), and Driftgate renders no GitHub link because the repo is private. **Blocking Week-1 exit:** (1) Info page three paragraphs — the one substantial piece of new prose, needs Sean's go before it is written; (2) Info portrait photo — decided as Sean's photo 2026-08-12, no file exists, none in the old repo; (3) the seven locked scene renders are **not on disk anywhere** — they live only in Higgsfield job history, so the L21 night screening pass cannot run until they are pulled down; (4) Substack URL unverified and X handle not found anywhere in the old portfolio source — both are omitted rather than guessed; (5) `metaDescription` and all seven project one-liners are still the pre-L24 scope §5 drafts, unreviewed against the final statement. **Also flagged:** three `npm audit` advisories from Astro's own sharp/esbuild pins (build-time only, macOS, static output) — resolve or accept before ship; and one line of copy the build session wrote unprompted, the 404's "This one got erased.", which needs approval or replacement.
- **WS3 design: ALL PHASES COMPLETE — DESIGN-v2.md APPROVED (L27), Week-1 build gate OPEN** (2026-08-12): Sidecar closed at 27 locks, every one with Sean's verbatim reason (`~/.creative-harness/partner-sessions/2026-08-11-ws3-portfolio-identity.md`). Phase B closed: type = Anybody 900 + Schibsted Grotesk (L16, slight hero scale-down rider) · "Groundwork"/"Driftgate" capitalized (L17) · Claude-bot choreography twin approved = Seedance 2.5 8s job `750650a6` (L18; left-to-right line-reaches-bot-then-pop). Phase C closed via grilling + UI Reviewer + Visual Polish Auditor + 3-persona + eye-roll (nothing died): intro = WebGL sprite build, twins are choreography bible only, byte-driven loader with ~3.5–4.5s floor (L19/L22) · Claude-bot-first sprites, on-page fleet pick by end of Wk2, MUST-level animation-research rider (L20) · Wk-1 night screening of all 7 scenes, warm cream-invert = only placeholder night grammar (L21) · character via anchor-conditioned regen, Anima-slot fallback; work pages carry project-authentic content (L23) · de-clone package: "beneath the surface" draft DEAD, statement = "Building AI products that hold up beyond the demo." (L25), "Projects" label, pencil toggle icon, Available/Now kept (L24/L25) · 10-item build-spec batch adopted (L26: token sheet, focus grammar, reduced-motion list, failure states, red-never-text-on-dark). **Artifacts:** `DESIGN-v2.md` (approved build gate) · `WS3-type-console.html` + `-r2.html` · sprite/plate generation queue per L19. **Next: Week-1 build kickoff** (scaffold seanwinslow.com, static-complete pages; projects + About content assembled in build sessions off DESIGN-v2.md + scope §3/§5 — no new partner session needed). Ship still gated on the privacy remedy + independent privacy pass.
- **WS2 Owner's Cards DRAFTED — six cards, schema decided, awaiting four approvals + the privacy remedy** (2026-08-11): `WS2-owners-cards.md` in the gitignored REVAMP folder. Schema decision **ADAPT driftgate** (scored 0/4 clean against WS2 §6's requirements — it is a forward-looking authority contract, the board needs a backward-looking evidence record; inherit the grammar, add a `clauses[]` evidence spine + `live_status`, with six build-time validations that fail the render on an overclaim). Six cards drafted with every clause's evidence state; incident lane written around *agents that exit 0 while doing nothing*. **Sean must approve before build:** (1) the narrowed death-condition spine sentence, (2) Knowledge Lint's dated retirement clock (recommendation reversed to keep-with-deadline — it is the only auditable commitment on the board, and the cost is building the triage loop or retiring the agent by 2027-02-11), (3) the incident-lane split framing, (4) the privacy remedy below. Also blocking: Card 2 currently publishes the two fields `agent-fleet-observability/lib/anonymize.py` deliberately zeroes, and the proposed `cards/` reader bypasses `public_pass()` entirely — both must be resolved in code before card copy goes near the board.
- ⚠️ **PRIVACY LEAK — remediated 2026-08-11; the last step is FILED and waiting on GitHub.** *(Updated 2026-08-12: GitHub Support ticket **#4659070** submitted from Sean's account, status open. Routing note for any refile: do NOT use Repositories → Deletes, that is a repository-deletion flow; this went in under Repositories → Repository features → Branches. Also verified: `code-brain` has **0 forks / network 0**, so GitHub's fork caveat does not apply and the remedy can fully close the hole. Gate closes when a fresh `git clone --mirror` comes back clean, not when Support replies.)* The `git filter-repo` scrub ran and was force-pushed. **A normal `git clone` is now completely clean** (0 manifest objects, 0 `ats:` slugs, 0 bucket names, verified against a fresh clone from GitHub, 781 commits intact). **But `git clone --mirror` still yields the data**, because GitHub retains `refs/pull/*` forever and 140 PR refs carry the pre-rewrite commits in their ancestry — 104 manifest objects still reachable by object SHA (e.g. via `refs/pull/141/head`). No PR *tip* contains them, so nothing shows in a PR diff view, but anyone who runs `git clone --mirror` + `git rev-list --objects --all` gets the 69 company names. **The only remedy is GitHub Support:** open a ticket from Sean's account asking them to garbage-collect stale refs and purge cached views for `seanwinslow28/code-brain` — this is GitHub's own documented procedure after a sensitive-data force-push, and it cannot be done from the client side (PR refs are not deletable via push, and closing/deleting PRs does not remove them). Nuclear alternative: delete and recreate the repo, which is immediate and total but destroys 140 PRs, the issue tracker (`docs/agents/issue-tracker.md` names GitHub Issues as the project tracker), stars and forks. **Backup:** full pre-rewrite history preserved and verified at `~/Code-Brain/code-brain-git-backup-2026-08-11.bundle` (1.1 GB, `git bundle verify` passed) — keep it until Support confirms, then delete it, since it contains the unredacted data.
- **Mac Mini: `git push` needs credentials — 1 commit waiting** (2026-08-11): the re-sync landed and committed as `da4856f` on `vault/process-inbox-2026-07-14`, but the push failed with `could not read Username for 'https://github.com': Device not configured` (Keychain error `-25308` — a non-interactive SSH session can't unlock it). The Mini is **1 ahead, 0 behind, working tree clean**, so it is a plain fast-forward whenever Sean is at the machine or a credential helper / SSH remote is configured. Consider switching the Mini's remote from HTTPS to SSH so headless pushes stop depending on an unlocked Keychain.
- **OPERATIONAL LESSON — `reset --hard` after a history scrub deletes the scrubbed files from disk** (learned the hard way 2026-08-11, near-miss): resetting the Mini to the rewritten branch removed all **92 job-feed manifests from the working tree**, because filter-repo had removed them from history and the reset faithfully reproduced that tree. Those manifests are the entire evidence base for Owner's Card 2 (158 refusals / 235 runs / 92 days / 488,461 fetches) and **20 of them existed only on the Mini** — the MBP mirror stops at 2026-07-22. Recovered in full from the pre-reset commit `ae803a2`, which survived in the object store (`git show ae803a2:<path> > <path>` per file); verified by re-deriving 235 runs / 158 refusals exactly, and they now sit untracked-and-ignored, which is the correct end state. **The generalizable rule: before resetting any clone onto scrubbed history, copy the scrubbed paths out of the working tree first** — the pre-sync backup taken from `git status --porcelain` did *not* include them, because they were tracked and unmodified and so never appeared as dirty. A backup keyed on "what git says changed" misses exactly the files a history rewrite is about to delete.
- ✅ DONE 2026-08-11 — **Mac Mini re-synced after the history rewrite** (retained for the record; superseded by the two items above) (2026-08-11, consequence of the history rewrite): every commit SHA changed, so the Mini's clone of `vault/process-inbox-2026-07-14` has diverged completely and its auto-commit/push will fail. **Nothing is lost — it had 0 unpushed commits** — but it has **14 uncommitted modified files that are today's production repairs** (`flush.py`, `meta_agent.py`, `vault_critic.py`, `job_db.py`, the two flush hooks, and their tests) deployed by copy rather than by pull. A plain `git reset --hard` would revert the critic and flush fixes and re-break both agents. Safe sequence, to be run deliberately on the Mini: `git stash -u` → `git fetch origin` → `git reset --hard origin/vault/process-inbox-2026-07-14` → `git stash pop`, then re-run `pytest tests/test_vault_critic.py` to confirm the repairs survived. Note the job-feed manifests are now gitignored, so the two untracked critic manifests and `vault/daily/` will behave differently after the pop. **Not done in this session — the Mini is production and changing it needs Sean's go-ahead.**
- ✅ DONE 2026-08-11 — **original leak ticket, retained for the record: 69 job-hunt target companies were public on GitHub** (found 2026-08-11 by the independent privacy pass, verified directly; **CLAUDE.md rule 9 violation, highest priority in the campaign**): `vault/health/` is **not** gitignored, and **72 `job-feed-manifest-*.json` files are tracked and pushed to `origin/main`** (confirmed present at `origin/main` @ `60c312d`). Their `runs[].failed_pollers` entries are literal company slugs. **69 distinct companies**, i.e. effectively Sean's entire private target-employer watchlist (76 entries, correctly gitignored at `vault/20_projects/prj-job-hunt-2026/job-feed/watchlist.yaml`) is readable by anyone — including anyone at those companies. *(Company names deliberately not reproduced in this ticket; `tickets.md` is itself tracked and public. Enumerate them from the manifests if needed.)* This leak is independent of whether the Owner's Cards ever ship. **Decide with Sean:** (a) `git rm --cached` + gitignore `vault/health/job-feed-manifest-*.json` — stops future exposure, leaves history; (b) full `git filter-repo` history scrub, the 2026-06-10 precedent (see `docs/2026-06-10-code-brain-recruiter-readiness-audit-plan.md`); (c) strip only the `failed_pollers` field going forward and scrub history for it. Also audit the other tracked manifest families in `vault/health/` (77 synth, 61 critic, council/gemini spend) for the same class of exposure. **Do not push anything to code-brain until this is decided.**
- **CORRECT THE FLEET RUN COUNT — "2,210 runs / 1,975 at $0.00" is wrong; it is a ratified, public-bound figure** (surfaced 2026-08-11, WS2 card drafting, verified read-only on the Mac Mini): measured directly from both `agent-run-history.csv` ledgers — **Mac Mini: 1,719 rows, 2026-04-23 → 2026-08-11 (111 days), $83.60, 1,609 runs at $0.00**; **MacBook Pro: 416 rows, 2026-06-11 → 2026-08-11, $5.60, 414 at $0.00**; **both summed: 2,135 runs, $89.20, 2,023 at $0.00**. The **$83.60 is correct for the Mini alone** (daily-driver $44.40 + gemini-dr $39.20 reproduces exactly), but **2,210 matches neither machine nor their sum**, and 1,975-at-$0.00 is wrong on both readings. WS2-archaeology's own per-agent table sums to 1,714 — within 5 of the truth — so the 2,210 headline above it looks like an arithmetic error that was then carried into the verdict. Fix in **WS2-archaeology.md §1 and §3, WS1-credibility-verdict.md §0/§7 item 15, and MASTER-PLAN.md §1** (the ratified spine amendment). Pick one framing and never mix: *"$83.60 across 1,719 scheduled runs over 111 days, 1,609 of them at $0.00"* (Mini) or *"$89.20 across 2,135 runs, 2,023 at $0.00"* (both machines). Note this is exactly the failure the archaeology warned about in its own §1 two-ledger caveat and then committed one paragraph later.
- **Vault Critic repair is unproven on the scheduled path — do not ship its card as healthy before 2026-08-12 03:30** (surfaced 2026-08-11, verified on the Mini): today's `critic-manifest-2026-08-11-manual-112037.json` is `status: ok`, 1 expansion, 1 codex call, 0 failures — but that was a **manual** run. The **nightly** `critic-manifest-2026-08-11.json` fired at 03:30 before the repair and is still `status: error, codex_calls: 8, codex_failures: 8`. The launchd path has not executed post-fix. Check tomorrow's nightly manifest before the Owner's Card claims recovery; until then the card's `live_status` reads *recovered — unproven on the scheduled path*.
- **Knowledge Lint's weekly cadence has three missing weeks** (surfaced 2026-08-11, verified on the Mini): 15 reports exist, 2026-04-18 → 2026-08-09, and the run ledger says 14/14 success — but there are two gaps against a 7-day interval, 2026-04-26 → 2026-05-10 (14 days) and 2026-06-07 → 2026-06-28 (**21 days**), i.e. three scheduled runs never fired at all. "14/14 success" describes the runs that ran, not the cadence. Find out whether launchd missed them (Mini asleep? plist unloaded?) and either fix the schedule or state the real cadence on the public card. Related, resolved: the lint's honest-deferral refusal rule (`knowledge_lint.py:998-1019`, BT5 C3) has **never fired** — all 15 reports show the Tier-2 host reachable, so that clause ships as `defined-never-fired`, not `fired`.
- **Eval suite README contradicts itself — headline says 10 cases, body says 14** (surfaced 2026-08-11, WS2 card drafting): `evals/vault-synthesizer/README.md:3` headlines *"A 10-case binary pass/fail eval suite"*, but its own Phase-1 addendum 20 lines down says *"4 cases added (vs-022 to vs-025)… target 11/14"*. Actual state verified by a live run 2026-08-11: **14 active cases** (`vs-012`…`vs-025`) in `cases.yaml` at **10 passed / 1 failed (vs-017) / 3 skipped**, plus **11 deferred** in `deferred-cases.yaml` — 25 written. This is a public repo and the WS1 verdict's required Owner's Card disclosure ("the suite is N cases against Anthropic's 20-50 floor") depends on the number. Fix the headline, refresh `last-run.md`, and reconcile `EXPLANATION.md`. Blocks Card 1 copy freeze.
- **Vault synthesizer eval has no recorded run since 2026-05-28, across a 192-line synthesizer change** (surfaced 2026-08-11, WS2 card drafting): commit `0f0213b` (2026-07-05, BT5 Phase C) rewrote 192 lines of `agents-sdk/agents/vault_synthesizer.py` and edited `evals/vault-synthesizer/cases.yaml` in the same commit, but `last-run.md` still records 2026-05-28. The suite passes 10/14 today so nothing regressed — but the claim *"no synthesizer change ships without it"* is not supported by the record. Either wire the suite into a pre-ship check that writes `last-run.md`, or drop the claim. Card 1 currently carries this as a required public caveat.
- **Reconcile "31 partial nights (32%)" for the vault synthesizer** (surfaced 2026-08-11, WS2 card drafting; **blocks the flagship Owner's Card**): WS2-archaeology §2.1/§5 cite 31 partial nights, but the `agent-run-history.csv` breakdown for the same agent has no `partial` bucket — 109 runs = 89 success + 11 error + 7 deferred + 2 partial-empty. The 31 appears to come from the 97 `synth-manifest-*.json` files and the 109 from the CSV, i.e. two different denominators. Read-only check on the Mac Mini (`ssh 192.168.68.200`). 32% partial is too large to publish unsourced or to silently omit.
- **Verify Knowledge Lint's report count and weekly cadence on the Mac Mini** (surfaced 2026-08-11, WS2 card drafting; blocks Card 6): WS2-archaeology cites **15 weekly reports through 2026-08-09**; the local MBP mirror holds **12**, ending 2026-07-19, with gaps at 2026-05-03, 2026-06-14, and 2026-06-21. The mirror froze when Mini→MBP vault sync stopped 2026-07-22, so this is probably incomplete mirroring — but it could also be missed runs, and the card claims a weekly cadence. Confirm on the Mini and name the machine on the card. While there, extract a count + date window for the lint's honest-deferral refusal rule, or the clause ships as `none` (the card schema forbids a `fired` state without a count).
- **Two agents declared in config have never existed as code** (surfaced 2026-08-11, WS2 corrections): `pyproject.toml:47-48` declares `health-audit = "agents.health_audit:main"` and `md-to-anki = "agents.md_to_anki:main"`, and `config.toml:155,159` carries `[agents.health_audit]` / `[agents.md_to_anki]` stanzas — but `git log --diff-filter=AD` returns nothing for either source file. They were never written. This is what put two phantom names into CLAUDE.md's retirement list (now corrected to the audit's real eight). Delete the dead entrypoints and stanzas, or implement them.
- **Four pre-existing test failures in the agents-sdk suite** (surfaced 2026-08-11 during WS2 correction verification): `tests/test_doc_to_audio_cli.py::test_main_json_mode_emits_parseable_json` and three `tests/test_job_db.py::test_update_status_cli_*` fail at HEAD, confirmed unrelated to this session's `meta_agent.py` edit by stashing and re-running. Suite is otherwise 961 passing. The job_db ones likely date to the 2026-08-11 description-cap change (`c8ce9bc`).
- **Board `agent_grid.html` hardcodes "8 agents" in its aria-label; ACTIVE_AGENTS is now 9** (surfaced 2026-08-11): `agent-fleet-observability/templates/partials/agent_grid.html:10` and the rendered `index.html:100,314` say *"Fleet state, 8 agents"* / *"24-hour activity · 8 agents"*. `meta_agent.ACTIVE_AGENTS` gained `vault_critic` and `job_feed` on 2026-08-11 (9 total). Public-facing count on the board — derive it from data rather than hardcoding, same discipline as the regression-night count.
- **Second privacy pass owed on the WS2 Owner's Cards before any board push** (filed 2026-08-11): `WS2-owners-cards.md` (gitignored REVAMP folder) carries six drafted cards + incident-lane copy destined for the public `agent-fleet-observability` repo. Build-pass privacy check done by the drafting session; the **independent verification pass has not been run** and must be, by a separate reader, per MASTER-PLAN §0. Checks: no company names / role titles / application state from the job_feed corpus, no income/medical/contact/employer data, no absolute paths revealing private vault structure beyond what the board already exposes.
- **Research: model-role assignment as a spend/productivity method** (filed 2026-08-11, Sean's ask): how do you decide which models — open-weight and closed — get assigned which roles, so spend and productivity optimize together rather than trading off? Sean already does this by routing table and instinct (`agents-sdk/lib/hybrid_router.py` `task_map`, Tier-A/B/C machines, `fallback_disabled`, `fallback="none"`); the question is whether a defensible method exists behind it or it's all local heuristics. **The receipts are already unusually strong** — $83.60 across 2,210 runs / 111 days with 1,975 runs at $0.00, plus 158 logged refusals to reach the paid API. Single-shape question, so the research queue or a `gemini-deep-research` falsification pass both fit; do NOT route to local LDR if it compounds into multi-target. Output feeds the resume skills line, the projects section, and a likely WS5 artifact ("here is my model-role assignment map and what it actually saved"). A light capture rides along with the WS1 blog sweep since those sources discuss routing anyway.
- **Fleet doc corrections — "six retired agents" is eight, "8-day regression" is nine** (surfaced 2026-08-11 during WS2 archaeology): `agents-sdk/AUDIT-2026-04-09-agent-downsizing.md` states *"Of 10 enabled agents, only 2 were producing value. The other 8 were disabled"* and names process-inbox, daily-driver evening, daily-driver weekly, pr-digest, sprint-health, meeting-defender, preserve-session, spending-analysis — but CLAUDE.md's "6 agents disabled 2026-04-09" names `health-audit` and `md-to-anki` instead, which the audit never mentions. Separately, the featured silent regression is **nine** days (May 1–10) per README.md, `evals/vault-synthesizer/EXPLANATION.md`, and three vault connections — "8-day" appears only in later shorthand. Both numbers are load-bearing for the public fleet board, so fix before anything ships. Also reconcile `meta_agent.py` `DISABLED_AGENT_COUNT = 5`.
- **W&B + Chainalysis: no pollable ATS — add to manual tracking** (surfaced 2026-08-11): of the 12 dead job_feed pollers, 7 were recovered as slug fixes but lindy, crewai, wayfair, weights-and-biases, and chainalysis have no board on Greenhouse/Lever/Ashby. Weights & Biases matters most — Weave evals sit directly on Sean's edge and it's a natural WS1 corpus source for eval-language claims. Add these to `target-companies.md` manual tracking (the 2026-06-18 precedent for unsupported ATSes).
- **Mac Mini clone is on branch `vault/process-inbox-2026-07-14`, not main** (surfaced 2026-08-11): the scheduled fleet runs from this side branch and auto-commits vault changes to it. `agents-sdk/` code at HEAD was verified identical to main for the files touched this session, so nothing was broken — but a long-lived divergent branch running production is a silent-drift risk of exactly the kind this session spent the day fixing. Decide whether to merge/rebase onto main or document the branch as intentional.
- **No running agent has a named death condition** (surfaced 2026-08-11, WS2 §4): the campaign spine claims every system ships with an eval gate, spend cap, refusal rule, and death condition. Fleet-wide the first three have real fired evidence, but zero of the nine running agents has a defined, observable kill condition — the April retirement was a decision, not a pre-declared condition that tripped. Either write per-agent death conditions (marked "defined 2026-08, not yet fired") or narrow the public claim. Blocks honest Owner's Cards.
- ✅ DONE 2026-08-11 (Claude Code, Sean-approved): **Job-hunt revamp — master plan doc written and ratified.** `MASTER-PLAN.md` lives in the private REVAMP folder (gitignored) — 12 locks encoded, 3-phase 12-week season, seven workstreams each with goal/first-move/owner-tools/dependencies/death-condition, delegation map, dependency spine. Sean ratified 2026-08-11: 40 hrs/week campaign budget, season thresholds, WS1 verdict deadline 2026-08-25. Broken into the six campaign tickets below, each pointing back into the doc.
- ✅ DONE 2026-08-11 (14 days early): **Revamp WS1 — credibility audit.** `WS1-credibility-verdict.md` delivered in the private folder. **All 9 claims verdicted, all 7 completion criteria met, no gaps logged** — the provisional-verdict death condition never triggered. Evidence base: 241 live postings / 40 companies with full text, 9 tiered engineering-blog primaries, per-community falsification. Headline outcomes: spine reframed to governed reliability (token optimization falsified as a market claim, demoted to supporting signal); 2 of 3 adjacent rings dead as titles, Forward Deployed **Product Manager** in as the live adjacent ring; per-agent authority exercised — **6 public cards, not 10** (Deep Researcher, Vault Indexer, Daily Driver demoted; no Flush card); no community enters distribution on evidence. **The [L5] publishing gate is RELEASED**, conditional on §7 "findings → shipped work" being visibly consumed by WS2 and WS3.
- **Revamp WS1 follow-through — §7 binding changes must land in shipped work** (filed 2026-08-11): the verdict's lock is that findings visibly land, not just get written. WS2 owes: 6 cards not 10, synthesizer leads in capability/regression eval language with the 10-case size disclosed, "refuses to spend" not "guardrails" for job_feed, six→eight retirement correction, 8-day→9-day regression correction, an incident lane, a card schema with a live-status field. WS3 owes: bullseye-first AI PM headline with governance as the specialization, no chat-with-my-resume bot, llms.txt/MCP shipped-not-claimed, live eval panel as the forward element. Full list in the verdict §7. — assigned: Sean
- **Revamp WS2 — Mac Mini fleet archaeology + Owner's Card drafts** (filed 2026-08-11, runs parallel with WS1; public shipping gated on the WS1 verdict): per MASTER-PLAN.md §WS2. SSH archaeology for honest fired-clause evidence (cap saves, deferrals, the six April retirements as deaths-by-design), then per-agent four-clause Owner's Card drafts; decide whether driftgate's role-card schema becomes the public card schema. Honesty over coverage — no card ships a clause without real evidence or an honest "not yet fired" marker. **Status 2026-08-11:** archaeology pass DONE — evidence catalogued per agent per clause across 2,600+ logged runs and ~300 manifests; three strong card candidates and three weak ones identified (provisional pending the WS1 verdict); several live reliability findings surfaced that need Sean's approval before any fix. Card drafting is the next session. Evidence in the private folder. — assigned: Sean
- **Revamp WS3 — portfolio v1 scoping session** (filed 2026-08-11): per MASTER-PLAN.md §WS3. Settle DESIGN.md keep-or-drop, stack choice, and the MCP-resume usefulness research pass before build sessions start; segerman-structure gallery + agent-readable layer in v1, live-data + MCP grafts as v2 build-in-public episodes. Copy/project selection gated on WS1 verdict. — assigned: Sean
- **Revamp WS4 — frontier-scan ritual: topic list + week-1 run** (filed 2026-08-11, starts immediately — private research, not publishing): per MASTER-PLAN.md §WS4. Draft the 6-week rotating topic list, create the experiment-candidates note in the private folder, run week 1's last30days scan. Graduation to a scheduled agent only after ~6 weeks prove the loop changes what gets built (ratified bar: 2 of 6 picks become shipped artifacts or builds). — assigned: Sean
- **Revamp WS6 — named-fifty seed + no-ask exemplars** (filed 2026-08-11, private prep; sends begin with the first post-verdict artifact): per MASTER-PLAN.md §WS6. Seed the first 15 names into the private list, draft 2-3 no-ask note exemplars so the register is designed before the first send, mine Executive Circle for outreach tactics. TalentBoard profile start rides Phase 1. — assigned: Sean
- **Revamp WS7 — campaign Owner's Card draft** (filed 2026-08-11, unblocked — budget named): per MASTER-PLAN.md §WS7. Four-clause contract on the campaign itself: weekly 15-min artifact review (eval gate), 40 hrs/week (spend cap), no-filler/no-ask/verdict-gate rules (refusal), 12-week review on gameproof indicators (death/pivot). Draft in the private folder; publishes with the site per WS3 scoping. — assigned: Sean
- ✅ DONE 2026-08-11 (verified, no action needed): **Privacy: gitignore `vault/20_projects/prj-job-hunt-2026-REVAMP/`** — already covered at `.gitignore:133` inside the PRIVATE LAYER block; confirmed via `git check-ignore`. Ticket was stale.
- **Pencil & Prompt S3 remainder — publish pages, nav, wordmark generation + S2 asset application** (surfaced 2026-08-08; updated same day): ✅ Sean approved both pages 2026-08-08 ("ready to go", two hand-edits folded into voice-samples); ✅ his matched-angle About image pair wired in (`_assets/launch-2026-08/new-about-images/`, hero-about-1 top / hero-about-2 bottom, placement provisional). REMAINING: (1) publish [pages/start-here.md](../20_projects/substack-studio/pages/start-here.md) + [pages/about.md](../20_projects/substack-studio/pages/about.md) as Substack pages with their images ([Rung 0] links stay placeholder until S4); (2) build launch-lean nav (Home · Start Here · Building the Ladder · About · GitHub · Portfolio — needs pages/sections live); (3) ✅ WORDMARK DONE 2026-08-08: 5 GPT Image 2 candidates generated via Higgsfield (`_assets/launch-2026-08/wordmark-candidates/`); Sean picked dual-v2 (script "Pencil" + slab-mono "Prompt", amber/teal blooms); strip cropped and UPLOADED to the site Wordmark slot + saved — the site header now reads Pencil & Prompt (v3 rough-vs-code variant reserved for large surfaces: banner/social/title cards). Still open: apply remaining S2 assets Sean opts into: masthead-family-portrait.png fits the profile header/banner slot (2688×1520, within 3:2–21:4), avatar-parent-and-kid.png fits Logo (2048² 1:1), hero-start-here.png ready, hero-origin-confession-v2.png reserved for S4, series cards await sections. Browser state done 2026-08-08: 3 posts unpublished, 9 restacks removed, bio replaced, theme Custom, Introduction set. Platform note: profile-publication type has NO pub-name/subtitle fields; profile Name+Bio double as site name+subtitle, so the wordmark IS how "Pencil & Prompt" gets on the site. **Campaign coordination (2026-08-11, per MASTER-PLAN.md §WS5 in the private REVAMP folder): this remainder completes BEFORE or WITH the first job-hunt-campaign post so the content cascade lands on a finished front door.** — assigned: Sean
- **verify_supplement: consume JSON archive endpoints** (surfaced 2026-08-07, agent-company backfill): `_simple_fetch` in `tools/llm-council/council/discovery/gather/web.py` rejects `application/json`, so Web Supplement quotes cited to Arctic Shift (`arctic-shift.photon-reddit.com/api/...`) or HN Algolia JSON endpoints can never machine-verify — the 2026-08-07 solo-money-paperwork ledger needed manual orchestrator verification (8/8 verbatim) as a workaround. Teach the backstop (or its fetcher) to accept JSON bodies for quote substring checks; also consider decoding HTML entities (`&#x27;`, `&rsquo;`, `&mdash;`) before matching — three ledgers needed entity-free span trims for Krebs/HN/Tom's Guide quotes that ARE verbatim on the rendered page.
- **Agent-company founding — research campaign (sitting 3+)** (surfaced 2026-08-08, partner session L1-L12 complete, territory+shape locked): run the L10 research front-load as follow-on sessions, FEASIBILITY SPIKE FIRST per the L12 research gate — vision-model drift-detection eval on the anima corpus (go/no-go evidence before any build), then software-factory literature review (Stripe/OpenAI/Ramp/Anthropic + NotebookLM corpus, Codex synthesis), groundwork v1 audit, architecture ratification via LLM council, Arize eval-stack design, wayfinder ticket map; kickoff prompt at [docs/prompts/2026-08-08-agent-company-research-campaign-kickoff.md](../../docs/prompts/2026-08-08-agent-company-research-campaign-kickoff.md); decision record: ~/.creative-harness/partner-sessions/2026-08-07-agent-company-founding.md (12 locks). **✅ STEP 1 FEASIBILITY SPIKE COMPLETE 2026-08-08 — VERDICT: GO.** 32-case blind eval on anima's Sean-ratified corpus (11 clean / 14 drifted / 7 haircut incl. adversarial control), pre-registered bars frozen before any judge ran; BOTH judges cleared all three bars — Claude Fable 5 blind subagents: recall 0.86, false-alarm 0.09, haircut 3/3+3/3+control; gemini-3.5-flash (anima production pin, N=3): recall 1.00, false-alarm 0.18, haircut 2/3+3/3+control. All residual errors are one case shape (mascot leg-count, undecidable without a stated canon fact) — addressable by the series-bible layer already in the product shape, per anima's own layer-ownership precedent. The haircut mechanism (declared canon-update notes) worked end-to-end, including the control (a note does NOT excuse undeclared changes). Evidence memo: [vault/20_projects/research/2026-08-08-vision-drift-feasibility-spike-go-no-go.md](../20_projects/research/2026-08-08-vision-drift-feasibility-spike-go-no-go.md); harness + per-case receipts alongside at `2026-08-08-vision-drift-spike/`. Spike cost <$1. [L12] stands. Untested risks carried forward for steps 4-5: video drift, long-context text canon (FlawedFictions), integrated multimodal report, per-creator calibration at scale. **✅ STEP 2 LITERATURE REVIEW COMPLETE 2026-08-08 (same sitting, $0):** three tracks — NotebookLM corpus (22 sources, 4 citation-backed cross-queries via notebooklm CLI), first-party blog sweeps (Anthropic/OpenAI/Stripe/Ramp read as primary sources with ADOPT/CAUTION/CONTEXT tags; Stripe's Minions + OpenAI's Harness Engineering are the two deepest published factory accounts), and Codex synthesis per the kickoff (6 convergent findings, 9 scaled-down adopts, 10 be-warys incl. named source contradictions, 10 gaps, and a ranked TOP-TEN decision agenda). Headline: the literature's #1 convergent finding — verification, not generation, is the scarce capability — is the same seam the discovery campaign found from the demand side and the same seam the product occupies; factory literature, pain evidence, and product shape now triangulate. Review at [vault/20_projects/research/2026-08-08-software-factory-literature-review.md](../20_projects/research/2026-08-08-software-factory-literature-review.md) with all evidence mirrored alongside; Codex's TOP TEN is the recommended step-4 ratification agenda (GAPS = pre-reading questions; groundwork audit owns #2/#7/#8, eval-stack design owns #3/#6/#9). **✅ STEP 3 GROUNDWORK V1 AUDIT COMPLETE 2026-08-08 (same sitting, $0, read-only):** repo is ahead of this ticket's snapshot — all 20 wayfinder decision tickets closed, v1 real (3,694-line stdlib validator, 709 passing tests, honest known-limitations). Verdict: groundwork is the constitution and org chart — needs 2 (accountability: owners/forbidden-actions/death-conditions) and 9 (blast-radius change consent) fully SATISFIED, roles/verification/memory-governance/HITL PARTIAL — and by explicit design ("files, not an engine"; roadmap Never list) none of the nervous system: orchestration, evals, tracing, model routing, cost metering, product hosting all MISSING on purpose. Recommended posture INVERTS the L1 assumption: do NOT grow groundwork toward the fleet — adopt it as the governance layer, extend it only with files-shaped CONTENT (fleet-role ontology, judge charter, model-assignment field, cost-cap + trace policy files), and build the runtime nervous system separately (agents-sdk already owns most primitives). Three ratification cautions: enforcement parity (hooks are Claude-Code-only — mixed-model fleet gets instruction-strength governance unless the runtime mediates all side-effectful actions), the company would be groundwork's FIRST real adopter (interview run twice ever, simulated only), and the review-inbox is the 25-hr/wk founder bottleneck to build early. Audit at [vault/20_projects/research/2026-08-08-groundwork-v1-audit.md](../20_projects/research/2026-08-08-groundwork-v1-audit.md) (both agent reports mirrored alongside). Resolves the groundwork half of TOP-TEN #2/#7/#8. **✅ STEP 4 COUNCIL PRE-MORTEM COMPLETE 2026-08-08 ($0.54, premium profile, Grok member failed → clean N-1 continuation) — RATIFICATION PACKAGE AWAITING SEAN.** Architecture proposal v1 (ten concrete decisions on Codex's TOP-TEN agenda, grounded in spike+lit-review+audit) went through the council: UNANIMOUS weakest = D5 model routing as written (disagreement→Needs-Review would make Needs Review the modal verdict — "the founder becomes the hidden third judge"; plus local-fleet SPOF) → REWRITTEN with axis-specific disagreement policy, pre-registered ≤15% Needs-Review-rate alarm, 200-case downgrade floor, degraded-mode fallback. UNANIMOUS locks: D1 release-gate contract + D6 verification stack. Convergent v1-debt: D4 canon model → amended to event-sourced canon ledger (stable IDs, typed events, materialized snapshot, confirmation-decay, export/delete primitives, verdict-replay guarantee). THREE non-overlapping missing decisions ratified in as D11 creator-data/IP boundary (blocks first payer), D12 dispute→eval ingestion contract (blocks WAU ramp), D13 artifact promotion/reproducibility (blocks the 30-day maintenance bar). Council's shared dangerous assumption: spike proves detection-when-canon-is-stated, NOT canon-extractability from real creators' material — PRE-LOCK CONDITION: canon-extraction test on 3 real creators' back-catalogs before D2/D4/D5 lock. Package (final proposed text per decision + sequencing): [vault/20_projects/research/2026-08-08-architecture-ratification-package.md](../20_projects/research/2026-08-08-architecture-ratification-package.md); proposal + full council transcript alongside. **✅ SEAN RATIFIED 2026-08-08 ("Signed.")** with the Fable 5 second opinion's six amendments + six additions incorporated (fresh-research review at [vault/20_projects/research/2026-08-08-architecture-second-opinion.md](../20_projects/research/2026-08-08-architecture-second-opinion.md): wedge re-verified open same-day; judge panel = disagreement-detection not voting; durability gap closed — watchdog/auto-resume/idempotency keys, agent-to-agent loops banned; Reasoning-Trap orchestrator seating; detect-crop-anchor recipe rehabilitates DINOv2 as deterministic pre-filter; Phoenix OSS self-hosted $0 for the Arize layer). **✅ CANON-EXTRACTION TEST COMPLETE same day — PASS on all four pre-registered bars; D2/D4/D5 stand, no reopening.** Three blind corpora (Pepper&Carrot CC-BY sequential 3-era chain; Pencil Test Act 1; Grandmaster design set): fact precision 0.90-0.95 (hex-level palette matches vs ratified canon), coverage 0.85-0.95, evolution-vs-drift discrimination exemplary (kid transformation + grandma two-age unification derived from pixels; Saffron redesign correctly scoped; stage-3 self-audited a stale evolution event), onboarding cost ~$1-12 per 50-installment catalog (under the $20 tripwire). Residual failure mode = fine attributes under stylization (glasses shape, lighting-shifted colors) — exactly what D4's creator-confirm catches; consistent with the drift spike's boundary: models own structure/judgment, stated canon facts own fine attributes. Bonus D4 policy refinement adopted (era-scoped defaults, two-era corroboration before overwriting, occasion-wear states). Memo: [vault/20_projects/research/2026-08-08-canon-extraction-test.md](../20_projects/research/2026-08-08-canon-extraction-test.md). **✅ STEP 5 EVAL-STACK IMPLEMENTATION DESIGN COMPLETE 2026-08-08 ($0):** design doc at [vault/20_projects/research/2026-08-08-eval-stack-implementation-design.md](../20_projects/research/2026-08-08-eval-stack-implementation-design.md) — Phoenix OSS self-hosted spec (pinned images, Postgres, explicit working-dir + 90d retention, local auth; Phoenix = observability, NOT system of record → weekly pg_dump suffices), three-project span routing (product 100% / fleet filtered / evals; personal $0 fleet stays OFF company Phoenix), one `sk.*` trace schema for both layers carrying the D13 version tuple + cost-per-verdict + A2 durability fields, D11 minimization client-side (TraceConfig masking + allowlisting redaction processor + CI canary test with sentinel fixtures), eval corpus store (spike 32 cases → detection/judge seed + judge gold set; extraction-test corpora → extraction seed; corpus-vN git tags; bars inherited from the spikes' pre-registered thresholds, pass^3 on judge lane), A1 judge calibration ops (2 diversified judges — Claude w/ canon facts vs Gemini w/o; temp-0 pinned, pairwise forced-choice, κ/α only, OCR escape, monthly gold-set re-runs, judge swap = recalibration event), and 5 pre-registered cron+Pushover alarms + weekly failure review. AX Free deferred until strangers arrive. No new decisions (addition 6 honored). **✅ STEP 6 WAYFINDER MAP CHARTED 2026-08-08 — THE CAMPAIGN IS CLOSED; BUILD BEGINS AT THE FRONTIER.** Company repo created: `seanwinslow28/driftgate` (PRIVATE, name explicitly PROVISIONAL — naming ticket #2 checks availability before locking, repo renames after). Map issue [#1](https://github.com/seanwinslow28/driftgate/issues/1) + 17 child tickets (#2-#18) as native sub-issues with 27 native blocking edges, groundwork's wayfinder conventions: naming (#2), system-of-record grilling (#3), scaffold + groundwork instance via dogfooded interview (#4), Owner's Cards ×6 w/ kill switches + A3 mid-tier orchestrator seat (#5), D4 canon ledger w/ extraction-test refinements (#6), onboarding extraction dogfood + cost-ceiling datapoint (#7), D2 state machine w/ A2 durability (#8), A4 visual pre-filter w/ unverifiable-panel (#9), text-canon lane scoped by FlawedFictions (#10), Phoenix deploy per step-5 doc (#11), eval-seed migration (#12), judge lane (#13), D13 tuple wiring + replay (#14), review inbox + D12 dispute intake (#15), 5-alarm cron (#16), build-in-public capture layer (#17, counts against ≤20% meta-tooling), SLICE FINISH LINE: advisory drift report w/ receipts end-to-end on a real installment (#18, blocked by 6 tickets). Frontier now: #2 naming, #3 grilling, #4 scaffold. — assigned: Sean

- **Portfolio repositioning — PR #46 open, iterating; DO NOT MERGE yet** (Sean, 2026-08-08: "I still want to make some changes, so I'm not merging until everything is perfect"): branch `reposition/about-2026-08` in sw-ai-pm-portfolio, all 15 sidecar locks executed + two preview-review polish rounds (statement hero rebalanced + promoted; About v3 opening spread = paragraph beside character, lead title retired to meta; plate contained 1280px + hairline frame; RecruiterCallout removed superseding L14b; Proof points heading centered; resume v3 corrected export live). UNCOMMITTED in the working tree: CharacterLane.astro tune (Sean's bottom:-20 + 928×522 resize, ~9% smaller) — commit when Sean blesses. Still open: (1) more Sean changes coming; (2) judgement-scene on-page treatment feels awkward — Sean wants a brainstorm before deciding (cutaway itself works; it's the page fit); (3) entry-overture verdict at /labs/overture/ (integrate or delete before merge); (4) machinery line lowercase vs sentence case. Dev server: `npm run dev` in sw-ai-pm-portfolio → localhost:4321 (HMR; character knobs in src/components/hero/CharacterLane.astro `.character` block, keep width/height 16:9). — assigned: Sean
- **Sourdough weekly feeding** (dark start 2026-08-05 per sidecar L7; F-001 done in-session): 15-min ritual every week, checklist at `~/.creative-harness/sourdough/rituals/weekly-feeding-checklist.md`; log every feeding AND every miss in `feedings/FEEDING-LOG.md`; after ~6 feedings run the first blind comparison per `protocol/blind-comparison-protocol.md`. All content local-only — never in this repo. — assigned: Sean
- **creative-partner sidecar contract: two conventions surfaced by live session 1, let them season before codifying** (surfaced 2026-08-05, P&P refocus session wrap report): (1) evidence arriving from OUTSIDE the room between sittings (the session consumed three research syntheses; the loop absorbed them via round observations — worked, but undocumented); (2) how a sidecar hands off to EXECUTION artifacts (the session put its map in the vault pointing back at the sidecar — felt right, but is convention, not contract). Neither misfired; codify in [.claude/skills/creative-partner/references/sidecar-contract.md](../../.claude/skills/creative-partner/references/sidecar-contract.md) after 2-3 more live sessions show the stable shape, per the review's no-scope-expansion rule. Live session 2 (2026-08-06, P&P visual identity, first visual axis) added two more: (3) render-as-evidence — Sean ordered candidate images generated MID-session to rule with eyes on; ran outside the loop at his explicit order, landed as round observations, worked well (renders flipped the orchestrator's pre-render lean); (4) header hash stamps should be shell-computed straight into the file, never retyped — a mistyped hash slipped into session 2's header at creation and was only caught by re-verification; the L5 checkpoint also caught locks appended into the wrong section (repaired byte-identical, logged in the trace). Live session 3 (2026-08-06/07, portfolio About repositioning, 15 locks + 1 divergence run) re-fired three conventions cleanly: outside evidence absorbed via round observations (rt3 specs, an external ChatGPT site critique, anima briefs), render-as-evidence escalated to an interactive HTML prototype (built at Sean's order, ruled on, retired with the answer captured in rounds 4/L7), and execution handoff via a tickets.md entry pointing back at the sidecar. New candidate convention (5): mid-session skill-calibration edits at Sean's explicit order (cheese-bank entries from live redlines) executed outside the loop and logged as orchestrator notes on the lock. — assigned: Sean
- **Pencil & Prompt refocus — research round before build** (surfaced 2026-08-04 partner session; sidecar at `~/.creative-harness/partner-sessions/2026-08-04-pencil-and-prompt-refocus.md`, 6 locks + 1 divergence run): the publication's new working set is LOOSE-LOCKED (divergence-thesis masthead; Building the Ladder expedition spine; Raising Agents story series; six provisional territories with the tested-mechanism-library-as-product model). Next session(s): run (1) `fusion-discovery-council --lens substack` on the six territories' pain hypotheses, (2) per-mechanism competitive checks (Executive Circle MCP + web: does a free equivalent exist), (3) `gemini-deep-research` on prior art for the first mechanism + the tested-library model — **✅ DONE 2026-08-05 (Kickoff D complete): both DR topics ran on the DR tier ($2.80 each, $5.60 total, 538s + 505s, well inside the $20/day $50/month caps), reports filed as pointer notes in [research/deep-dives/](../20_projects/substack-studio/research/deep-dives/) and synthesised at [research/2026-08-05-prior-art-synthesis.md](../20_projects/substack-studio/research/2026-08-05-prior-art-synthesis.md) — 8 findings + an implied build order. Headlines for the reconvene: the library bet survives but the white space is the SEAM (enterprise eval rigor is real but private; public artifacts have no evidence standard), heterogeneous multi-agent debate is the best-evidenced mechanism and Sean already runs the infrastructure (llm-council / fusion-discovery-council) so it is the rung-#1 candidate, morphological analysis is the sleeper rung-#2 (+18.5% diversity, d≈1.03), temperature-tuning and "think outside the box" are FOLKLORE (the round's most publishable contrarian finding), and the masthead should re-cut from "sameness" to "stickiness" (homogenization persists after the session, d=0.414, and is negligible d=0.12 on free-form divergent tasks — the loose thesis is weakest exactly where readers assume it is strongest). Build-order inversion flagged: the measurement protocol must publish BEFORE rung #1, since a per-entry verdict is only as credible as its metric. ⚠️ VERIFICATION DEBT: all citations in both reports are opaque Google grounding-redirect URLs and Topic 1 contains a known-false claim ("Claude 3.7's 2M tokens, Gemini 2.0's 10M tokens"), so every figure must have its primary source resolved before appearing in a published post — listed in the synthesis. Nothing decided; the sidecar reconvene makes the calls. **POST-ROUND VERIFICATION (2026-08-05, $0): a [citation tier audit](../20_projects/substack-studio/research/deep-dives/2026-08-05-citation-tier-audit.md) resolved all 108 grounding-redirect URLs (they DO resolve — DR output is cheaply auditable, now a standing pre-citation step, script at `agents-sdk/scripts/audit_dr_citations.py`) and split the two reports hard: Topic 2 is 90% defensible (NeurIPS/ACL/EACL/AAAI/PNAS/TACL/INFORMS/Cambridge AI EDAM/~15 arXiv) but Topic 1 is only 23%, with NINE of its sixty citations from futureagi.com's own SEO blog (Future AGI is also row 1 of its landscape matrix) and BOTH load-bearing examples self-sourced to their own marketing pages. [Move A verification](../20_projects/substack-studio/research/deep-dives/2026-08-05-move-a-verification-two-named-examples.md) then checked those two directly: **Techpresso FALSIFIED** (50 copy-paste prompts, zero eval results, zero documented failures) and **Aksoy Capital's 1-hour retraction policy is real and accurately reported but unexercised** (`/data/retracted/`, `/retractions/`, `/corrections/` all 404, no searchable instance) **and out-of-domain** (algorithmic financial research, not a technique library). Net: the white space is REAL, but the claim must be stated precisely — "no prompt or technique library publishes per-entry tested verdicts with honest failures", NOT the broad "nobody does this", which Aksoy would falsify. Methodological lesson worth its own post: query SHAPE drove source tier (research-shaped → 88% academic, market-shaped → 65% marketing, same model/tier/day), and the bad sources were fresh 2026 marketing, not stale research, so recency filters cannot catch them. **✅ KICKOFF E COMPLETE 2026-08-05 ($0, both moves).** [Move B](../20_projects/substack-studio/research/deep-dives/2026-08-05-move-b-candidate-falsification.md) checked twelve named candidates by direct fetch (URL behind every yes AND every no) plus an Executive Circle MCP pass. **CORRECTION 2026-08-05: that pass does NOT discharge refocus item 2 in full — an earlier version of this line said it did, and that was wrong.** Move B queried Executive Circle on the *library-model* question only, which is [Kickoff C](../20_projects/substack-studio/continuation-prompts/KICKOFF-2026-08-05-C-competitive-checks.md) **step 3** of five. Kickoff C's actual body — per-territory Nate-Jones + web competitive checks across all SIX territories, each ending in a CLEAR / CROWDED-BUT-DIFFERENTIATED / COMMODITY verdict, output at `research/2026-08-05-competitive-check-six-territories.md` — is UNRUN; Move B's note contains zero mentions of any of the six territories and that output file does not exist. Eleven clean NOs — Anthropic prompt library, DAIR.AI, promptingguide.ai, LangSmith Hub ("user-generated and unverified", their words), PromptHub, PromptLayer, Braintrust, DSPy, OpenAI Cookbook, awesome-chatgpt-prompts, HF finephrase. **The twelfth changes the positioning: Nate Jones already publishes tested verdicts with honest failures** (2026-07-15 harness-audit post — 742-word brief vs 5,197-word method, 3 runs each, 2 blind scorers, his richer artifact scored 19.67/20 on content and FAILED the delivery contract 2 runs of 3, published as a loss with explicit scope limits). So "we test and admit failures" is dead as a differentiator; the surviving white space is **CUMULATION** — per-entry, versioned, retractable, against a published measurement protocol. Nobody does that. This upgrades Finding 8 from prudent to load-bearing: the measurement protocol IS the differentiation, not just its credibility gate. [Move C](../20_projects/substack-studio/research/deep-dives/2026-08-05-divergence-mechanisms-evidence-map.md) resolved all six publishable figures to primaries — all real and quoted accurately, with three corrections that must travel: (1) **d=0.414 does NOT support the masthead re-cut** — it rests on 2 studies / 6 effect sizes, the during-vs-after moderator test was NULL (p=.141, authors: "offers no evidence for a difference"), and they hedge with "might persist"; ship it with the n attached, demote it to open question, or make measuring persistence a rung; (2) the 91%/82% debate figure is Hegazy 2024 (arXiv:2410.12853) on **2024-era models**, and neither DR-cited source contains it; (3) NoveltyBench D=16.50 vs 13.60 belongs to continuous context sampling vs the G2 baseline (ACL 2026 SRW), **not to xRAG** — that paper calls xRAG's goal "orthogonal to ours". iDesignGPT's +18.5% / +11.4% / d≈1.03 / 48 participants confirmed verbatim in Nature Communications; all eight named frameworks confirmed real. ⚠️ **Doc re-anchor ticket is STILL BLOCKED** (an earlier version of this line declared it unblocked — corrected 2026-08-05). Item 3 of the research round is complete and item 2 is partially complete, but **two of the four original kickoffs have never run**: [Kickoff B — discovery pain-mining](../20_projects/substack-studio/continuation-prompts/KICKOFF-2026-08-05-B-discovery-pain-mining.md) (`fusion-discovery-council --lens substack` on the six territories' pain hypotheses; deep tier $4.00 recommended, needs Sean's spend confirm; latest discovery session on disk is 2026-07-30 and all ledgers predate the 2026-08-04 refocus, so **no territory pain hypothesis has any evidence behind it**) and [Kickoff C — competitive checks](../20_projects/substack-studio/continuation-prompts/KICKOFF-2026-08-05-C-competitive-checks.md) steps 1-2 and 4-5 ($0) — **Kickoff C is now ✅ COMPLETE (2026-08-05, see below); Kickoff B is the only kickoff still unrun.** This matters for the value gate: the DR round proved the white space is real and the mechanisms are sound, but neither question touches **whether readers actually have the pain** — the Itch slot that project CLAUDE.md §3 hard-blocks on. When those land, reconvene with the sidecar BEFORE the doc re-anchor, because Findings 1-2 and 7 all changed and the reconvene should decide the new positioning (cumulation, not testing) and what carries the masthead now. Nothing final until real posts prove the value. **✅ KICKOFF C COMPLETE 2026-08-05 ($0, all five steps)** — [competitive check on the six territories](../20_projects/substack-studio/research/2026-08-05-competitive-check-six-territories.md): per territory, the closest Nate Jones coverage (posts read in full, not previews) plus the 2-3 closest free equivalents, each fetched directly. Verdicts: **d1 Cartography COMMODITY as a beat / CLEAR as an instrument** (Wikipedia's WikiProject AI Cleanup already ships a free 50+-pattern attractor field guide built from thousands of real diffs — un-out-catalogueable by one person; this independently ratifies the critic's demotion of d1 from beat to instrument), **d2 Stolen Methods CROWDED-BUT-DIFFERENTIATED** (Zoe Scaman already feeds AI "frameworks from jazz improvisation, ecosystem design, evolutionary biology, network theory, game theory" with zero measurement and nothing shipped; six of Nate's eight adversarial prompts are themselves offline-protocol ports, stated and never run), **d3 Import Duty CROWDED-BUT-DIFFERENTIATED but weakest** ("constraint-based prompting" is saturated SEO advice; only the *social/physical* imports and the customs-violation report are open — if a draft's takeaway is "add constraints," kill it), **d4 Input Axis COMMODITY on the corpus half / CLEAR on the data-hole probe** (the most saturated shelf checked, and the closest surviving relative of the premise the GPT Image 2 test killed), **d5 Manufactured Opposition CROWDED-BUT-DIFFERENTIATED with the sharpest delta** (four free tools exist — `agent-review-panel`, `devils-advocate-agent`, `adversarial-workflow`, OpenClaw's suite — and not one installs a *stake* or tests whether the disagreement changed the output; all ship the *homogeneous* single-model variant the DR round's Finding 4 says underperforms, while Sean already runs the heterogeneous one), **d6 Sourdough CLEAR** (the memory field measures recall accuracy, not whether an aged creative collaborator beats a fresh one — but CLEAR partly *because* it is expensive; the absence may be a rational market response). **The finding that outranks all six:** free equivalents cover the MECHANISM in five of six territories and publish an EVALUATION of it in zero — `agent-review-panel`'s own README says it plainly: *"this project has not independently benchmarked review quality."* The commodity trap is one layer up from where the sidecar assumed: a post whose deliverable is a mechanism competes with GitHub; a post whose deliverable is a verdict on a mechanism competes with nobody. **Step 3 extended Move B into the category Move B never checked** — agent-skill marketplaces, the shelf the both-tiers rule would actually ship onto: claudemarketplaces.com indexes 23,600+ Claude Code skills carrying name/author/description/**install count only**, no test results, no verdicts, no failure cases (popularity standing in for evidence, the same trust substitution Finding 2 found in prompt marketplaces, one layer up). So the absence is now verified in four categories: prompt libraries, enterprise eval tooling, practitioner newsletters, agent-skill marketplaces. **Two findings outside the six:** (1) the folklore rung has partial encroachment — a free Medium post (2026-03) already published 20 runs across four temperature settings concluding "19 of 20 outputs recommended identical core strategies regardless of temperature," plus a free live tool, so the finding is no longer novel and the rung must be sold on protocol and scale, not surprise; (2) Nate's tactical artifacts are **paywalled** (both posts checked are truncated at the paywall), so he is an encroachment risk on *angle*, not a free equivalent on *artifact* — the genuinely free equivalents are GitHub repos, Wikipedia, and SEO content. Third independent pass now lands on the same build-order inversion: **the measurement protocol is the only item on the list a competitor is not already giving away.** Evidence only; the reconvene makes the keep/kill calls. **✅ KICKOFF B COMPLETE 2026-08-05 ($23.04 actual / $25.87 reserved against the $30/day policy-v3 discovery cap — note CLAUDE.md and the skill doc both still say $10/day $50/month, which is STALE; `council/cap_policy.json` v3 reads daily 30.00 / monthly 100.00 and is the enforced policy). Sean chose six narrow `standard` runs (one per territory) over the kickoff's suggested single `deep` run, because per-territory verdicts need per-territory framing. Synthesis at [research/discovery/2026-08-05-territory-pain-validation.md](../20_projects/substack-studio/research/discovery/2026-08-05-territory-pain-validation.md); six ledgers + six briefs alongside it; $0 agent backfill appended to every ledger and 13/13 quotes confirmed verbatim by `verify_supplement` (exit 0 on all six). **Verdicts: T1 Stolen Methods KEEP with the claim inverted** (forum pain says prompt *tips* fail; arXiv 2602.20408 says structured protocols — CoT + ordinary personas — produce "the highest idea diversity, outperforming humans", so the tips-vs-protocols gap IS the thesis and is now citable); **T2 Import Duty KILL as a demand claim, keep as story engine** (perfect 1.00 citation precision and still zero first-person evidence — the panel's own gap says the evidence "relies on conceptual articles"; the one real user quote is about *reliability* delaying a project, not constraints); **T3 Input Axis RESHAPE + RE-RUN, do not cite this ledger** (0.57 precision / 0.00 recall, the only Sonar-fed run of the six, five of six "independent domains" are consultant SEO squatting the "why AI writing sounds generic" keyword, and one citation credits a CAPTCHA-walled North Devon council complaints page with prose it cannot contain); **T4 Manufactured Opposition KEEP reshaped to theater-detection** (pain real — live r/claude thread — but three competitors already ship devil's-advocate prompts and NONE tests whether the output changed; even arXiv 2503.14263 measured satisfaction and psychological safety, not decision quality); **T5 Cartography RESHAPE, premise falsified** (the "nobody has mapped the attractors" half is plainly false — seanjkernan, michellekassorla, stationx and Reddit all ship tell-lists — but nobody tests accuracy, and arXiv 2502.15666 (APT-Eval, 14.7K samples, 12 detectors) finds detectors "frequently flag even minimally polished text as AI-generated", causing "false plagiarism accusations"; also note the evidence is writing-only, nothing on visual tells); **T6 Sourdough PROMOTE, strongest evidence in the round** (1.00 precision AND 1.00 recall, plus arXiv 2606.10949 — Writer, ICLR 2026 workshop, benchmarking Mem0/MemOS/Zep — "memory amplifies sycophantic behavior across all conditions, with up to 25x higher sycophancy rates than in-context baselines" and "every model triples its sycophancy rate under at least one memory system"; the sidecar's guess was right and its confidence was too low — though "nobody publishes longitudinal results" is now HALF FALSE, benchmarks exist, and the surviving white space is narrower: no PERSONAL longitudinal collaborator log published against a fresh instance). **The cross-cutting finding: three of six territories (T1, T4, T5) independently converge on "the diagnosis is commodity, the tested verdict is vacant"** — the same seam the DR round reached by a completely different method, and now a third independent pass at the same build-order inversion (measurement protocol before rung #1). **Five unpredicted pains logged:** fragility of centralized memory state (server dies, months of context die), dependence/autonomy degradation ("going back to a fresh session feels broken"), being FALSELY accused of AI as its own distinct pain, reliability-not-constraint-blindness as what actually breaks deadlines, and the privacy cost of feeding a personal corpus to a hosted model (absent from all six panels; the first objection a professional reader raises). **Method finding worth acting on:** `citation_precision` is recorded only in the session JSON, never rendered in the ledger, and it ranged 0.57–1.00 across one batch while every run reported "dropped: 0" — read it before trusting any ledger; and the `Corroboration: N domains` badge is domain-blind, so vendor SEO wears a "well-corroborated" label. Reconvene with the sidecar now — item 1 of the research round is closed and all four kickoffs have run. — assigned: Sean
- ✅ DONE 2026-08-05 (Claude Code, session-map S1): **substack-studio doc re-anchor** — all six docs rewritten onto the divergence thesis from the sidecar locks L1-L7 + the three 2026-08-05 research syntheses: SOUL.md (masthead: push the agent past the median; honest-arc DNA kept), POSITIONING-AND-EDITORIAL-SPEC.md (median-bitten reader, cumulation white space, six-territory roster with L7 fates), SERIES-COMMAND-CENTER.md (Building the Ladder rungs + Raising Agents episodes + Unlock Hunts + Graveyard policy; Take Two/Back to Basics retired with lineage noted; new frontmatter with `verdict:` field), SKILL-PACKAGING-PLAN.md (catalog swapped to divergence mechanisms, entry #0 = the measurement protocol, verdict axis added to the taxonomy; §6.1 answered by library-as-product, the library HOME decided at S5), CLAUDE.md §0/§2 (guardrails now carry the research caveats: precise library claim, no stickiness-number masthead, banned $2.51B / 10-30x figures, Hegazy vintage rule, d3 "add constraints" kill rule, T3-ledger-not-citable), README.md (orientation). Research caveats carried INTO the docs per the S1 kickoff. Subtitle locked via in-session naming pass (Sean's pick): **"Push your AI past the average, one tested experiment at a time."** — reason recorded in spec §10. Old docs + back catalog moved to `vault/20_projects/substack-studio/_archive/` (superseded-docs/ + back-catalog/, with README) so the old setup can't be confused with the new; quarry status preserved. Hygiene in the same pass: stale discovery caps ($10/day/$50/month → policy v3's $30/day/$100/month) fixed in code-brain CLAUDE.md + fusion-discovery-council SKILL.md + its run-template. Image house-style review deliberately NOT done here — the session map moved it to S2, which owns it. Next: S2 (image pass) / S3 (pages + profile).
- **4 pre-existing agents-sdk test failures — CLI tests shell out to bare `python3`** (surfaced 2026-08-05 during the gemini_dr migration, confirmed pre-existing by re-running on a stashed tree, NOT caused by that work): `tests/test_job_db.py::test_update_status_cli_{end_to_end,rejects_invalid_status,rejects_unknown_row_id}` invoke `subprocess.run(["python3", ...])`, which resolves to system Python rather than `agents-sdk/.venv` and dies on `ModuleNotFoundError: No module named 'dotenv'` — the tests then assert on stderr text and fail for the wrong reason. `tests/test_doc_to_audio_cli.py::test_main_json_mode_emits_parseable_json` fails separately because the Kokoro model weights (`agents-sdk/models/kokoro/*.onnx`/`.bin`, gitignored) are absent on this machine. Fix: point the subprocess calls at `sys.executable` instead of `"python3"`; either skip the Kokoro test when weights are missing or run `scripts/install_tts_models.sh`. Small hygiene pass — full suite is otherwise 955 green. — assigned: Sean
- **Rewrite "The Night My Vault Said Nothing" under current writing-voice-modes** (surfaced 2026-08-04 partner session): the draft at `vault/20_projects/substack-studio/_archive/back-catalog/bonus-the-night-my-vault-said-nothing/post.md` (moved to _archive in the 2026-08-05 doc re-anchor; still the live rewrite source) predates the voice-modes G1-G5 incorporation pass and "sounds nothing like me" (Sean). Re-run through the full chain + Sean hand-rewrite before it can serve as a Raising Agents episode candidate. — assigned: Sean
- DONE 2026-08-04 (Claude Code, Sean-reviewed): **writing-voice-modes incorporation pass** — G1-G5 hard sentence mechanics + content-model-first / domain-words / tools-as-cast findings folded into the public [SKILL.md](../../.claude/skills/writing-voice-modes/SKILL.md) (new section, 2 anti-pattern rows: Interior-Translation Tell + Nature-Documentary Voice, Step-4 gate now opens with a G1-G5 mechanics sweep, Beat Flow's colon advice fixed); all verbatim VoicePrint evidence (5 round diffs + museum authority sample, ~20K chars) appended only to the gitignored `references/voice-samples.md` per the privacy layer. Screenplay-lane finding (filmable action lines) committed in screenwriting-modes. Commits `c8264cb` + `c9df74c`. Target 30 stays open; round-5 draft remains in the VoicePrint lane's drawer.
- DONE 2026-08-05 (Claude Code): **gemini_dr.py migration to google-genai >= 2.0.0** — the Gemini DR path is live again; Kickoff D is unblocked. Four changes: (1) `agents-sdk/pyproject.toml` pin moved `>=1.74.0,<2.0.0` → `>=2.0.0,<3.0.0`, venv upgraded 1.75.0 → 2.16.0; (2) report extraction rewritten for the 2.x model — the flat `interaction.outputs` list is gone, so the new `_extract_report_text()` reads `interaction.steps` and concatenates **every** `model_output` step in order, plus every text-bearing part within each (thought / user_input / tool-call steps excluded by type, so internal reasoning never reaches the vault); `budget_exceeded` added to `_TERMINAL_STATUSES` (new in 2.x — without it a dead interaction polls to the 65-min wall) and status coerced via `str()` for `UnrecognizedStr`; (3) `create()` trimmed to the validated shape `input=/agent=/background=True` (dropped `store=True` + `agent_config`, which only requested thinking summaries the extractor skips anyway); (4) credential resolution is now `resolve_api_key()` — Keychain slot `com.sean.agents.gemini_api_key` first, then `GEMINI_API_KEY` env var, then `agents-sdk/.env` → repo-root `.env` (fresh-machine fallback, reference-runner pattern); the key value is never logged or printed, only the source label. **The Keychain slot was populated this session from the repo-root `.env`, closing the companion "key missing from Keychain" ticket** — `resolve_api_key()` now returns `source=keychain`. Tier logic, spend ledger, the $7/task · $20/day · $50/month caps, and vault output conventions were not touched. Evidence: `pytest tests/test_gemini_dr.py` = **54 passed in 0.44s** (was 38; +16 covering the 2.x steps shape, a `test_polling_reads_steps_not_outputs` guard that fails if anything falls back to the dead 1.x attribute, an end-to-end test pinning the exact `create()` kwargs, and 4+9 credential-resolution cases incl. the `.env` fallback); full `pytest tests/` = **956 passed, 4 failed**, and those 4 (`test_job_db.py` ×3, `test_doc_to_audio_cli.py` ×1) were confirmed pre-existing by re-running them on a stashed tree — they shell out to bare system `python3`, which lacks `dotenv`, and to absent Kokoro model weights. Suite stays hermetic: no test reaches the network (0.45s wall, `genai` patched + `Client.assert_not_called()` retained). Live proof: the 2026-07-30 log shows `BadRequestError: 400 ... legacy Interactions API schema is no longer supported`; today's `vault/90_system/agent-logs/gemini-dr-2026-08-05-dr.log` shows `API key resolved from: keychain` → `Interaction created: id=v1_ChdvQ...` → `status=in_progress`, i.e. the schema rejection is gone. Live DR smoke test was Kickoff D Topic 1, fired on Sean's explicit cost confirmation ($2.80 predicted, DR tier): `completed` in 538s, report at [vault/20_projects/research/2026-08-05-what-exists-today-weight-post-2025-sources-heavily-across-th.md](../20_projects/research/2026-08-05-what-exists-today-weight-post-2025-sources-heavily-across-th.md), $2.80 logged to `vault/health/gemini-spend-2026-08.json`. **The smoke test earned its keep — it caught a truncation bug the unit tests could not.** First pass took only the LAST `model_output` step; the real API chunks a DR report across several (measured on this interaction: 3 steps of 12160 + 6273 + 25569 chars), so the note landed with 42% missing — Deliverable A and Parts 1-3 gone — while still reading as a long, well-formed, entirely plausible report. Only spotted because the body opened mid-Part-3. `interaction.output_text` is **not** a whole-report aggregate either (it equalled the final chunk alone), so it is now a degraded last resort, never a peer source. Fixed to concatenate all model_output steps; re-fetched the stored interaction (free — retrieval, not a re-run) and repaired the note in place, 27737 → 46167 chars, all of Deliverable A + Parts 1-5 + Conclusion now present, 60 citations. Two regression tests added from the measured shape (`test_extract_concatenates_every_model_output_step`, `test_extract_does_not_prefer_output_text_over_steps`). Lesson worth keeping: for this API, silent truncation is the default failure mode and it looks like success — assert on report structure, not just on non-emptiness. Commits `c04b057` + follow-up.
- **Company OS portfolio project — "groundwork"** (seeded 2026-07-15; Session 1 ✅ 2026-07-16; Session 2 ✅ 2026-07-16): design brief at `docs/plans/2026-07-15-company-os-design-brief.md` (PR #133); public repo created (https://github.com/seanwinslow28/groundwork) and wayfinder charting done — map at https://github.com/seanwinslow28/groundwork/issues/1 with 18 decision tickets (#2–#19): name/repo task + both research tickets resolved (findings merged to `research/` on groundwork main), 4 blocking edges wired, 1 charting-surfaced task added (#19 directory-convention verification). Next: Session 3 = work the map one ticket per session, starting with the first frontier ticket **License: MIT or Apache-2.0? (#3)** (quick; first meaty one after it is Third deep function #4). Build sessions (post-map) use Fable 5 + Codex plugin review gate — noted on the map. Scaffold/build under `/Users/seanwinslow/Code-Brain/groundwork/` only once the map says the way is clear. — assigned: Sean
- **groundwork design-brief evidence corrections** (surfaced 2026-07-16 by Session 2's wayfinder research, tickets [#12](https://github.com/seanwinslow28/groundwork/issues/12)/[#13](https://github.com/seanwinslow28/groundwork/issues/13)): amend `docs/plans/2026-07-15-company-os-design-brief.md` — (1) §6's "copying a SKILL.md alone fails across harnesses" is now false for the portable core (Agent Skills open standard; Codex/Cursor/Gemini load SKILL.md natively) and true only for the Claude-extension surface — work packages' job shifts to making silent degradations explicit; (2) §2's 66K boot-tax case was an individual's self-accumulated ~36 plugins measured via `/context`, not an org-wide default-on deployment — recite as "documented individual case + documented org default-on mechanism." Small doc pass; the map resolutions carry the citations. — assigned: Sean
- **code-brain memory-audit pass** (surfaced 2026-07-16 from the groundwork memory research, brief Appendix D): the documented auto-memory failure modes (claude-code #34776 — contradictory corrections accumulating without expiry, stale load-bearing assumptions, no health review; closed "not planned" by Anthropic) apply directly to `.remember/` buffers and `~/.claude/projects/-Users-seanwinslow-Code-Brain-code-brain/memory/`. One-session job: add provenance label + `review_by` date conventions to entries, prune contradictions/stale facts, and consider a periodic `/memory-audit`-style check (knowledge-lint extension candidate). Also note code-brain runs 4 inject layers (index hook + tickets hook + remember + auto-memory) — check combined boot-token cost against the 66K-token boot-tax finding. **Expanded 2026-07-23** with the three useful deltas from the Hermes-inspired plan clip ([vault/00_inbox/Claude-Hermes-Inspired-Memory-Plan.md](Claude-Hermes-Inspired-Memory-Plan.md) — the other five-sixths of that plan code-brain already runs via .remember/flush/remember-skill/vault-indexer/query.py): (1) one-time bootstrap import — index the 2026-07-23 local Claude Code transcript archive (off-repo, gitignored) into the vault index so old sessions become semantically searchable with source citations, sentinel-guarded so it runs once; (2) hard character cap + consolidation rule on the `.remember/now.md` working buffer (a memory file allowed to grow forever becomes a junk drawer); (3) hybrid recall in `query.py` — vector + keyword in parallel, merged, reranked by recency/source weight. Still NOT a new memory system — hygiene + three bounded upgrades to the one that exists. This is also the designated home for the "re-teaching Claude/Codex operational context each session" pain (examined 2026-07-23; deliberately kept out of creative-project scope). — assigned: Sean
- **the-oracle: bi-temporal/provenance schema fold-in** (documented 2026-07-16 in the-oracle at `docs/2026-07-16-bitemporal-memory-schema-note.md`, committed on `f8a4/measurement`, CHANGELOG'd + CLAUDE.md-linked): propose pricing-table `source`/`as_of`/`review_by` provenance inside the F8a.5 plan (the F8a.4 10×-stale-pricing finding is a memory-provenance failure; F7 sourced-cutoffs is the precedent), then hindsight records (Phase 4) + reference-data supersession (Phase 4+) via their phase plans. Proposals only, rule-2 gated. — assigned: Sean
- **Paid-content exposure policy for `vault/40_knowledge/references/`** (surfaced 2026-07-16 during PR #133): the public repo carries full-text clips of paid/subscriber articles (many `ref-nate-*`/`ref-aakash-*` files, plus `ref-ai-native-company-rules-15-commandments.md` triaged there by PR #131 — also still in git history via the 2026-07-14 auto-commit). New inbox paid clips are now gitignored (PR #133), but the references dir needs a deliberate decision: keep as-is, convert to excerpt+link stubs, gitignore the dir, or history-scrub via `git filter-repo` (like the 2026-06-10 privacy audit). — assigned: Sean
- fusion-discovery-council **D3 dashboard hardening bundle** (surfaced 2026-07-09, all triaged LEAVE at the D3 final review — none merge-blocking): add `<!doctype html>`/`<meta charset="utf-8">` + explicit `encoding="utf-8"` on the CLI's `output.write_text` (multibyte ⚠/·/— could mojibake under a non-UTF-8 launchd locale); renderer crashes if a classified-success session carries string-typed numerics (`verified`, `citation_precision`, `why_now_coverage` — unreachable from pipeline-written files today); day-granular discrepancy matching hides intra-day leaks (2026-06-30: 3 ledger runs vs 1 session, no flag); same-topic re-runs all suggest one output filename (overwrites); empty-gather sessions get "n/a (pre-E3 run)" though the cause is short-circuit not vintage (kind-aware label); `DISCOVERY_SESSIONS_DIR=""` cannot disable persistence (falls through to canonical — only `sessions_dir=None` disables). Also noted for budget.py (separate, pre-existing): `_spend_dir()` mkdirs as a side effect of resolving. — assigned: Sean
- fusion-discovery-council **E4 follow-up: expose `velocity_weight` via config/env** (surfaced 2026-07-09 in the E4 final Opus review). The velocity channel ships inert — `VELOCITY_WEIGHT=0.0` in `tools/llm-council/council/discovery/scoring.py`; turning it up currently requires editing that constant. Thread it through `run_discovery`/tier config or a `DISCOVERY_VELOCITY_WEIGHT` env var (alongside the existing `DISCOVERY_VELOCITY=pytrends` gate) so activation is a config change, not a code edit. Do this when validating pytrends signal quality on real runs (the natural moment to tune the weight). Also the natural pairing point for the deferred **demand-intent (autocomplete/PAA)** provider. — assigned: Sean
- intent-engineering MCP **spec-format parser is too strict** (surfaced 2026-07-07 building the-oracle spec): `audit_intent_spec` scores a valid spec 0/25 unless its YAML sits in `---`-delimited **frontmatter** — bare YAML in a fenced ```yaml block (exactly what `generate_intent_spec_scaffold` returns as `template_yaml`) and numbered markdown headings both parse as "section not located." Root cause in `sw-mcp-intent-engineering/src/intent/parser.ts`: `extractFrontmatter` only reads YAML from frontmatter, and `MARKDOWN_HEADING_MAP` doesn't match numbered headings like `## 1. Objective`. So the scaffold the MCP hands you can't pass the MCP's own audit without reformatting into frontmatter. Fix options: (a) parse fenced ```yaml blocks as spec YAML, and/or (b) strip a leading `N.` from markdown headings before matching. the-oracle spec reached 25/25 only after wrapping the YAML in frontmatter. — assigned: Sean
- intent-engineering elevation (Fable Phase B, 2026-07-05) **paired MCP change owed**: the mirrored checklist item `stop.zero-interaction-mandate` in sw-mcp-intent-engineering `src/intent/checklist.ts` still reads "…Mandate is present"; SKILL.md now reads "…present with the halt-and-error-note fallback" — update the tool description to match and cut an MCP release (marker: `<!-- PAIRED-MCP-CHANGE -->` in the skill's Stop Rule Quality group). — assigned: Sean
- intent-engineering elevation (Fable Phase B, 2026-07-05) **deferred reference-file halves of bt1 F7/F8**: extend `.claude/skills/intent-engineering/references/intent-spec-template.md` to match the elevated SKILL.md — add the Preservation Constraints (What NOT to Change) subsection inside Section 6 and the downstream-shape wording in `strategic_context.downstream`; mirror the same in the MCP scaffold templates when making the paired release. — assigned: Sean
- Fable 5 campaign **Phase B ✅ EXECUTED 2026-07-05** on `feat/fable5-phase-b` (Fable-driven; blind runs as fresh Fable subagents): all raw captures committed (introspection + BT1/BT2/BT3 blind runs + 4 diffs incl. the reclaimed BT5 pair), WWF5D `§1–6` co-authored from corroborated deltas (F1/F2 held; §7 open for validation), 5 Tier-1 skills elevated (validate green), `anima-register-seam-spec.md` + `creative-chain-spec.md` saved for Phase C. **Sean: review + merge the PR.** Then (Opus, zero Fable budget): run `validation-harness.md` → write wwf5d §7; Phase C implements the two specs + the tier1 deferred tickets; two BT5 owner forks to adjudicate (see BT5 ticket). ORIGINAL: Phase B ready to drive per `phase-b-fable-runbook.md`, budget WWF5D 40% → Tier-1 30% → anima 30%. — assigned: Sean
- Fable 5 campaign **BT5 fix implementation (Phase C)** — MBP Tier-2 reachability diagnosis shipped 2026-07-05 (paired specs: `docs/plans/wwf5d/fable-runs/bt5-fable.md` C1–C5, `baselines/bt5-opus.md` A–D, adjudication notes in `fable-runs/bt5-diff.md`). **🔓 FORKS DECIDED 2026-07-05 (Sean, on the Mac Mini per `docs/plans/wwf5d/bt5-mac-mini-fix-prompt.md`): Fork 1 = WIRE IT (make `soul-tier-a-conflict` real via C3, own PR); Fork 2 = STAY ON MBP + C5 catch-up (preserve MBP-class synth quality; off-LAN misses become cheap typed deferrals + a gated same-day catch-up; C5 decided after ≥1 wk observation). Both are Fable's dispositions.** **✅ C1–C4 IMPLEMENTED 2026-07-05** (local, unpushed): `feat/bt5-phase-c-c1-c2` (C1+C2 route-once/breaker/typed-deferral/notify-gate + C4 host_probe/consumer-sweep/enum + CLAUDE.md) and stacked `feat/bt5-phase-c-c3` (lint Tier-2 wired: `lint_tier2` route, corpus injection 178→3 batches, honest deferral, killed `except:pass`). 4 commits, +1078/-81, full agents-sdk suite green (939), validate green. **REMAINING (Sean): (1) review + push/merge the two stacked branches; (2) the real acceptance is done-criterion 6 — after ≥1 real week of nightly runs, histogram new synth manifests: expect zero `status=error`+`model_used=none`, any miss as `wol-deferred` w/ `duration_seconds<180`, and check lint reports carry a `Tier-2 LLM scan:` line; (3) THEN decide C5 (gated same-day catch-up fire ~09:30 + Monday lint catch-up) with a reduced budget.** Also fixed CLAUDE.md agent-table drift (Tier-2 model `qwen3.6_35b-a3b-32k`; "skip-and-continue" falsified) + `_normalize_model_name` stale enum. — assigned: Sean
- systematic-debugging elevation follow-up (Fable Phase B, 2026-07-05): true **enforcement** of the new Evidence Block phase-exit gate would be a PreToolUse hook ("hooks enforce; subagents judge") — a SKILL.md can only make skips transcript-visible. Decide if it's worth a hook that blocks Edit/Write on files named in an active debugging session until an Evidence Block exists in the transcript dir. — assigned: Sean
- claude-mastery reference drift (Fable Phase B, 2026-07-05): `claude-mastery/reference/shortcuts.md` (and settings reference) don't document `alwaysThinkingEnabled` (default-ON semantics — absent/true = enabled, false = opt-out), `effortLevel` (low|medium|high|xhigh), or that legacy `megathink`/31999-token keywords are gone in v2.1.199; plan-and-think SKILL.md now carries the verified story with a provenance footer — port it to the canon files so the two don't drift. — assigned: Sean
- ✅ DONE (PR #112, 2026-07-01) — **option (c) shipped**: unfetched/phantom cited URLs are now a hard precision miss (denominator only, never a hit); kills the multi-quote edge case that inflated precision to 1.0 (now 0.5). Phantom-URL test + τ=0.5 boundary test added. ORIGINAL: fusion-discovery-council **E1 follow-up: settle ALCE citation-precision semantics for unfetched/phantom cited URLs** (surfaced 2026-07-01 in the E1 final Opus review, shipped-as-is per Sean's call). `verify.py::citation_metrics` uses `v.point.urls` (all model-cited urls, incl. ones never fetched into the bundle) as the precision denominator. Two competent reviews split: Task 3 review said `point.urls` is correct per the spec prose "each (point, cited_url)"; the final review flagged phantom urls can skew the REPORTED precision (its "inflates to 1.0" claim only holds in a narrow multi-quote edge case — controller traced the common case as precision-LOWERING). Metric-only, gate unaffected. DECIDE with a fixture: (a) keep phantoms in denominator, (b) exclude non-bundle urls `[u for u in v.point.urls if bundle.has_url(u)]`, or (c) count unfetched citations as hard precision misses (most aligned with "verified, not hallucinated"). Add a phantom-URL test asserting the chosen value + a τ=0.5 boundary fixture (currently only 0.92/0.10 tested → `>=` vs `>` unpinned). — assigned: Sean
- ✅ DONE (PR #112, 2026-07-01) — plan-doc corrected to `v.point.urls` + option-(c) prose; all four hygiene items shipped (abbreviation/decimal-aware claim split with 3 tests, `|| true` dropped, test_nli imports cleaned, empty-bundle session JSON now carries verify_mode + citation_* keys). ORIGINAL: fusion-discovery-council **E1 plan-doc bug to correct + code-hygiene**: the E1 plan/spec Step 3 example code (`docs/superpowers/plans/2026-06-30-discovery-e1-entailment-gate.md`) writes `urls = v.supporting_urls` for the precision loop, which yields precision 1.0 (contradicts its own asserted 0.5 test); implementation correctly uses `v.point.urls`. Fix the plan doc so a future re-run doesn't reintroduce it. Non-blocking code-hygiene when next touched: `_CLAIM_SENT` splits on every `.` ("Mr. Smith"/"v3.0" → spurious fragments); `install_nli_model.sh` download `|| true` swallows the hf error message; `test_nli.py` unused `import importlib` + duplicated mid-file imports; empty-bundle session JSON omits `verify_mode`/`citation_*` keys (schema-uniformity). — assigned: Sean
- ✅ DONE 2026-07-22 — **PM3 t1 verdict = KILL the trend premise (RESCOPE contingent on Sean's re-run behavior); Phase 2 closes without building the spec.** Ran t1 (session `20260722-122152-4b67cc`, 6 verified/0 dropped, $6.81 est) and compared vs frozen t0 via the 4a two-stage matcher: **2 SAME · 3 RELATED · 3 ABSENT · 2 emerged**, 0/6 titles carried over. Movement is **≤ noise** (red-team #5 lands): the mean-score jump 38→65 is a **recency artifact** (t0 6/8 aging → t1 6/6 fresh, mechanical recency multiplier), count/granularity churn is the 4a same-day-noise signature across time, and vanished/emerged pains sit inside the sampling envelope; the *matcher* worked (3-for-3 rejecting cheap false candidates) so cross-run *identity/dedup* is sound but *trend* is not. Full note: `vault/20_projects/research/2026-07-22-pm3-t1-trend-noise-verdict.md`. NEXT = Phase 3 (E5 · D5 · PM1+PM5 · Step F buyer conversations) in a fresh session. ORIGINAL: PM3 t1 re-run due ~2026-07-21 (t0 seeded 2026-06-30, standard, "AI coding assistants") — compare vs `pm3-t0-ai-coding-assistants-2026-06-30.json`. — assigned: Sean
- ✅ DONE (PR #139, merged 2026-07-22) — fusion-discovery-council **OpenRouter `max_price` fusion 500**: attaching a `max_price` provider filter to the `openrouter:fusion` tool returns HTTP 500 (confirmed by A/B on an otherwise-identical body; t0 ran fine WITH it on 6/30 → OpenRouter-side change). Blocked ALL paid discovery until fixed. Scoped fix in `council/discovery/fusion.py::_build_body` (drop `max_price` for the fusion call only — NOT `provider_price_policy`, which the regular council still uses; cost safety preserved by model-pinning + `max_tool_calls` + enforced F8b ledger caps). Two `test_discovery_bounds.py` tests updated; full suite 745 pass / 1 skip. — assigned: Sean
- fusion-discovery-council **discovery cost overshoot: $6.81 vs t0's $1.85 (3.7×) for identical standard config** (surfaced 2026-07-22 during PM3 t1). Likely dominated by fusion server-side web-tool spend (never bounded by `max_price`; capped only by `max_tool_calls=5` × 3 panelists). Under the $30/day cap so not a breach, but a real efficiency regression. Investigate: lower `max_tool_calls` for standard tier and/or add a fusion-compatible per-call cost bound. The `max_price` removal (ticket above) may have contributed via provider routing — check both together. — assigned: Sean
- F8b **ledger reservation reconcile question** (surfaced 2026-07-22): `vault/health/council-spend-2026-07-22.json` `total` reads **$17.25** = 4 × $4.31 worst-case *reservations*, while `actuals` settled only ~$6.84 — failed-run reservations don't appear released. If the daily-cap check reads `total`, stale reservations over-consume headroom. Verify reservation release/reconcile on run failure + completion; confirm the enforced daily-cap check reads settled actuals (or reconciled net), not gross reservations. — assigned: Sean
- Pencil & Prompt (substack-studio): re-evaluate the locked format set (POSITIONING-AND-EDITORIAL-SPEC.md §7 — teaching spine / tool drop / **Fix My Mess**) against the prompts→systems pivot + the new A/B/C frame. Surfaced 2026-06-29 in the B-and-C lock session: Sean flagged "Fix My Mess" may not survive as-is — likely **absorbed into Direction C ("The Gate")** as reader-submitted Gate test cases, or reconceived. The three §7 formats predate both the pivot and A/B/C. Do a dedicated format-set re-evaluation pass before sequencing post #1. Context: vault/20_projects/substack-studio/2026-06-29-directions-B-and-C-locked.md (Open decisions #2). — assigned: Sean
- Pencil & Prompt (substack-studio): REFOCUSED 2026-06-29 to the original soul. Masthead is now SOUL.md ("have the agent think like me; partner, then system, then art"); the system / A-B-C framework is re-cast as the DESTINATION, not the opener. Done this session: SOUL.md (new masthead), surgical reworks of the spec + CLAUDE.md + README + SERIES-COMMAND-CENTER (both-tiers reader added: a copy-paste kit AND the symlinkable .claude skill, no longer non-coder-only), SKILL-PACKAGING-PLAN.md (Anima creative skills mapped to harden/universalize in the Pocock user-invoked vs model-invoked taxonomy), and a new Back to Basics tool-onboarding format. Partially resolves the format-set re-eval ticket above: Back to Basics added, Fix My Mess flagged to fold into Direction C. NEXT ACTION: build the Post 1 "Make the Agent's Taste Yours" build sheet collaboratively via the paste-ready prompt in CONTINUATION-2026-06-29-post-1-build-sheet.md (lock the demo subject first, then the capture plan + the two shipped artifacts, value gate before any draft). Open decisions in SKILL-PACKAGING-PLAN.md section 6 (chiefly: where the skill tier lives). — assigned: Sean
- fusion-discovery-council **Step B ✅ COMPLETE + Step C panel-vs-single gate ✅ DONE (2026-06-30)** — supersedes the "REMAINING" notes in the bullets below. Step B all shipped to main: PM4+D1 #103, D4 #104, E3 #105, **D2 receipts UI #106**. Step C panel-vs-single-model gate (red-team #4) ran on branch `feat/discovery-stepc-panel-vs-single-gate` (**unmerged, no PR yet**): **verdict = the 4-model Fusion panel beats single-model on blind variance-council rating at ~8% cost premium → E2 = GO.** Spend $2.77 (cap fine). Decision record `vault/20_projects/research/2026-06-30-panel-vs-single-model-gate.md`; field report `docs/field-reports/2026-06-30-fusion-discovery-council-stepc-panel-vs-single-gate-field-report.md`. **NEXT:** open/merge the Step C PR, then build **E2** (fix panel self-preference: judge ≠ panelist family · blind to authorship · swap evidence order). The OTHER Step C gate — **PM3 longitudinal** — is still pending (needs a 2–4 wk time gap; the new `EvidenceBundle.to_dict/from_dict` serializer is its groundwork). Then Step D = E1 cost-model decision (lean local NLI). — assigned: Sean
- ✅ DONE (PR #110, 2026-06-30): fusion-discovery-council Sonar cost-integrity leak (follow-up, surfaced 2026-06-30 in Step C): `gather/sonar.py` bills ~$0.02/run that isn't recorded to the spend ledger, contradicting the "every collector is FREE" docstring — a small but real cost-integrity gap. Fix: thread the Sonar call's billed cost into recorded spend, or document it as a known fixed line item. — assigned: Sean
- fusion-discovery-council production evidence persistence (follow-up, deferred to PM3, 2026-06-30): the new `EvidenceBundle.to_dict/from_dict` froze a bundle to disk for the panel-vs-single experiment; wiring it into the live pipeline (persist each run's raw evidence) is the groundwork PM3 longitudinal + cheap re-FUSE will need. Deferred until PM3 is greenlit by its gate. — assigned: Sean
- fusion-discovery-council **Step B / PM4+D1 — ✅ DONE 2026-06-29** (branch `feat/discovery-pm4-d1-score-card`, PR pending): replaced the toy opportunity score with a research-grounded `composite = 100 × value × confidence` ([scoring.py](../../tools/llm-council/council/discovery/scoring.py)) + redesigned the PM card to PRD-grade (who · pain-in-their-words · evidence+corroboration · auditable size · why-now · heuristic proposed-bet via [bet.py](../../tools/llm-council/council/discovery/bet.py)); substack lens shares the score (DRY). Confidence is a RICE-style discount that separates independent-source corroboration from model-consensus (kills the single-source illusion). Grounded in a deep-research pass (`vault/20_projects/research/2026-06-29-opportunity-scoring-frameworks-research.md`). $0 API spend, gate untouched, 195 council tests pass. **REMAINING in Step B:** E3 MMR dedup + recency/reach decay · D4 whitespace-map-as-hero · D2 receipts UI. Then Step C gates (panel-vs-single-model — costs real $; PM3 longitudinal). — assigned: Sean
- fusion-discovery-council: **BACKFILL → agent-layer pivot — ✅ TASK 1 DONE 2026-06-28** (working tree, NOT yet committed — `tools/llm-council` + `.claude/skills/fusion-discovery-council`). Flipped CLI `--supplement` default OFF (kept as opt-in for the future headless/no-agent path, per Sean's confirm); documented agent-driven backfill (Claude Code WebSearch/WebFetch on the Anthropic subscription, $0 — replaces the in-CLI Exa/Brave deterministic version; vets relevance natively) as the standard flow across SKILL.md §2/§3/§4.1/§5/§6/§8; shipped a copy-paste `references/run-template.md`; added a code-enforced anti-fabrication backstop `python -m council.discovery.verify_supplement <ledger>` (re-fetches each supplement URL, confirms the quote is verbatim, demote-on-miss; 11 tests). Discovery suite 132 pass. **REMAINING = Task 2 roadmap** (master ledger `vault/20_projects/research/2026-06-27-fusion-discovery-council-improvement-idea-ledger.md`; audit-reordered E6✅→PM4→D4, E1 = defensibility track), one step per session: **Step A** harden (cost-recording typed-failure fix ✅ DONE on main via #99 + reviews/github query-length 422 clamp ✅ DONE via shared `textbudget.clamp_words_chars` + remaining: HN under `--no-native-web` · `--segment` operator-strip · Fusion request-shape watch + typed `failure_reason`) · **Step B** sharpen (PM4+D1 score+card · E3 MMR dedup · D4 whitespace-hero · D2 receipts) · **Step C** gates (panel-vs-single-model #4 — costs real $, run after the daily cap resets · PM3 longitudinal) · **Step D** E1 cost-model DECISION-with-Sean (local NLI vs OpenRouter judge vs subscription agent — do NOT default to paid per-claim) + E1/E2 · **Steps E/F** Phase 2-3. — assigned: Sean
- fusion-discovery-council BACKFILL cost-recording — **✅ RESOLVED 2026-06-28** (the fix landed on main as #99 `8b961e4` while this branch was in flight, then merged in here): post-fuse stage failures now thread the billed OpenRouter cost into a typed `DiscoveryFailed` (with `failed_stage`), so a crash after FUSE records real spend instead of $0 — exactly the ~$1.9-under-count bug. Crash-safety also ✅ FIXED (`ce6ffcc`, this branch — `_supplement_query` clamp + per-gap `collect_web` try/except). **Dashboard-reconciled 2026-06-28 (close enough):** OpenRouter shows Jun 28 all-in **$18.4** on the Claude-Second-Brain key, but that mixes discovery + council (shared Opus/GPT/Gemini lines), so the ~$1.9 discovery under-count isn't separable from that view and isn't worth penny-matching — #99 is the real remediation and is merged. No further action. — assigned: Sean
- fusion-discovery-council improvement roadmap (2026-06-27 brainstorm): ranked idea ledger + phased roadmap at `vault/20_projects/research/2026-06-27-fusion-discovery-council-improvement-idea-ledger.md` — harden→sharpen→workflow→map-to-paid. Next action = Phase 0 (E2 fix panel self-preference: judge≠panelist family + blind; H harden hygiene incl. the reviews fan-out ticket below). P0 moat win = E1 entailment verification gate v2 (replace verify.py substring with atomic-claim+NLI, below-SOTA today). ✅ E6 web-supplement/BACKFILL SHIPPED 2026-06-28 (b127aed, PR #97). ✅ Week-0 conversion audit DONE (~88% generous conversion across 16 runs — red-team #1 refuted, proceed). Next build per ledger §5: panel-independent GATHER hygiene (reviews fan-out, HN, --segment escaping), then the panel-vs-single-model test before E2/panel-config; E1 entailment gate is the moat track. — assigned: Sean
- ✅ DONE (PR #110, 2026-06-30): discovery review-sites collector under-yields (Phase-4 follow-up, LOW): the live 2026-06-21 standard+deep runs both returned only 3 review records. Root cause is Brave collapsing the OR'd multi-`site:` query in `_review_query` ([tools/llm-council/council/discovery/gather/reviews.py](tools/llm-council/council/discovery/gather/reviews.py)) — Brave treats `site:` as a single filter hint, not a Boolean. Records that DO return are real + gate-valid (correctness fine); this is yield tuning only. Fix when convenient: fan out to N single-`site:` queries (one per REVIEW_DOMAIN) instead of one OR'd query, or feed query-expansion. Surfaced by the Phase-4 final whole-branch review (M2). — assigned: Sean
- ✅ CORRECTED 2026-06-21 (close-out) — STALE TICKET, NOT a HIGH blocker: the bug is already fixed (Phase 3 shipped `_decode_payload` SSE-strip + `FusionError.cost` + failure-path `record_spend`) AND validated documented-correct by OpenRouter research (`: OPENROUTER PROCESSING` is a documented SSE keep-alive comment; ignoring `:`-prefixed lines is the spec-correct handling `_strip_sse_padding` already does). The "two runs failed" were Phase-2, pre-fix. Residual is confidence only (a few live runs incl. deep). See council/docs/2026-06-21-fusion-discovery-council-phase5-field-report.md §7a. ORIGINAL (stale) — discovery Fusion response robustness (Phase-3, HIGH — live discovery intermittently fails today): two live `quick`-tier e2e runs on 2026-06-20 both failed at the Fusion parse step — run 1 `FusionError` "did not return parseable", run 2 bare `JSONDecodeError: Expecting value: line 181 column 1 (char 990)`. Root cause: OpenRouter streams `: OPENROUTER PROCESSING` SSE keep-alive comment lines as padding on slow Fusion calls (panel + web tool calls), so `fuse()`'s unguarded `payload = resp.json()` in [tools/llm-council/council/discovery/fusion.py](tools/llm-council/council/discovery/fusion.py) chokes on the non-JSON prefix. Intermittent (Phase 1 run #2 succeeded when fast). Fix: strip leading `: ` comment lines / extract the first balanced `{…}` before json.loads, harden `_parse`. ALSO: failed Fusion calls bill OpenRouter but record $0 locally (`record_spend` is post-success only in [__main__.py](tools/llm-council/council/discovery/__main__.py)) — record usage.cost on failure too. Full write-up: council/docs/2026-06-20-fusion-discovery-council-phase2-field-report.md §5/§6. — assigned: Sean
- ✅ RESOLVED 2026-06-21 (close-out, commit 85b1a63) — fixed durably in our repo: `gather/last30.py`'s `_last30_env()` forces `INCLUDE_SOURCES=reddit,hackernews` (keyless) in the subprocess env, which wins in the plugin's `os.environ`-first loader. Live-verified: crash gone, reddit yields records. (HN errored under `--no-native-web` — degrades safely; LOW follow-up below.) ORIGINAL — discovery last30 live-blocked by upstream plugin bug: `~/.claude/plugins/marketplaces/last30days-skill/scripts/last30days.py` crashes before emitting JSON — `config.get('INCLUDE_SOURCES', '').split(',')` raises `AttributeError: 'NoneType' object has no attribute 'split'` because `INCLUDE_SOURCES` is set to an explicit null. Our fusion-discovery-council collector ([tools/llm-council/council/discovery/gather/last30.py](tools/llm-council/council/discovery/gather/last30.py)) degrades safely to `[]`, so the pipeline never crashes, but last30 contributes zero live evidence until fixed. Fix is config/upstream: set a non-null `INCLUDE_SOURCES` in `~/.config/last30days/.env` (e.g. `reddit,hackernews`) or patch/report the plugin. Surfaced during fusion-discovery-council Phase 2 (see council/docs/2026-06-20-fusion-discovery-council-phase2-field-report.md §4). — assigned: Sean
- llm-council transcript render crashes when any council model returns null content: `_render_markdown` appends `r["content"]` (None) to `lines`, so `"\n".join(lines)` raises `TypeError: sequence item N: expected str instance, NoneType found` and the whole transcript fails to write even though the run + spend succeeded (hit 2026-06-16 on the 16BitFit 4Q run, gemini-pro returned null; recovered by reconstructing the .md from the session JSON archive). One-line fix at [tools/llm-council/council/cli.py](tools/llm-council/council/cli.py) line 40: change `lines.append(r["content"])` to `lines.append(r["content"] or "_(no response: model returned null)_")`. Optional follow-on: guard chairman content (line 52) + ranking reasoning (line 47) the same way. — assigned: Sean
- voice-pass program COMPLETE 2026-06-11 (Cowork): the LinkedIn drafts, Loom scripts, pinned-repo READMEs, and the 5 highest-traffic 4Q EXPLANATIONs referenced by the publishing tickets below (vault-knowledge Loom+LinkedIn, judge Steps 8-9, MCP security, Discovery PRD, Control Architecture, AP Agent Spec) are all drafted and run through writing-voice-modes → writing-critique → writing-humanity-pass at recruiter dials. Paste-ready before→after docs in job-hunt-2026-roadmap/2026-06-11-{linkedin-posts,loom-scripts,readmes,explanations}-voice-final.md + the sequenced publish-sprint playbook 2026-06-11-publish-sprint-playbook.md. REMAINING is execution-only (commit / record / post — Sean checks off as he goes) plus 6 more EXPLANATIONs queued (System Card, Data Readiness, Vault Scorecard, Phase D, Phase 6, vault-synth eval) and reconciling the duplicate Phase-D / knowledge-loop 4Q files. Next-priority menu in 2026-06-11-continuation-prompt.md — assigned: Sean
- substack-post-6: build the Agent-or-Automation Advisor MCP (sibling to intent-engineering; `advise_task` + `audit_fleet`; deterministic rubric + pass^k reliability engine; local/no-key; npm + registry under `com.seanwinslow/*`). Spec ready: `vault/20_projects/prj-job-hunt-2026/onwards-and-upwards-5-4-26/job-hunt-2026-roadmap/2026-06-17-agent-or-automation-advisor-build-spec.md`. Build in a fresh Claude Code session (a weekend) before drafting Post 6 (due Thu 6/25) — assigned: Sean
- substack-post-6 proof: run `audit_fleet` on the real 14-agent fleet, capture the results table (= the post's self-dogfood proof + the relead opening) — assigned: Sean
- recruiter-cleanup: delete the pre-rewrite mirror backup `~/code-brain-backup-2026-06-10.git` (holds ALL scrubbed private history) + `/tmp/code-brain-restore` + `~/code-brain-scrub-paths*.txt` once confident in the rewrite (suggest after 1-2 weeks of clean operation) — assigned: Sean
- recruiter-cleanup: file GitHub Support ticket to purge unreachable objects (old commits remain SHA-fetchable server-side until GC); flip repo public only after Support confirms or after accepting the residual-while-private risk — assigned: Sean
- fleet privacy filter: nightly synthesizer/flush still write job-hunt-derived concepts into the PUBLIC vault/knowledge/ + tickets.md over time — add a privacy-aware exclusion (route career/personal-derived concepts to vault/knowledge/private/) or schedule a periodic prune pass — assigned: Sean
- fleet venv durable fix: the 2026-06-10 13:31 Homebrew python@3.13 reinstall (3.13.11→3.13.13_1) changed the interpreter cdhash, which invalidated launchd's cached LWCR for 5 jobs (daily-morning, meta-agent, vault-indexer, vault-synthesizer, deep-researcher) — every fire on 2026-06-11 was kernel-killed with OS_REASON_CODESIGNING (no daily note, no overnight knowledge loop). Recovered manually 2026-06-11 09:15 via `launchctl bootout`+`bootstrap` of the 5 jobs (binary itself was valid on disk; only launchd's cache was stale). RECURS on every `brew upgrade python@3.13`. Durable fix still open: either (a) uv-managed pinned interpreter under the repo so brew can't move the binary, or (b) a post-`brew upgrade` hook that re-bootstraps the com.sean.agent.* jobs — assigned: Sean
- recruiter-cleanup deferred polish: docs/ top level still has loose dirs (NotebookLM Synthesis Cleanup, Design-Enhancement-Docs, Additional Skills…); consider folding into docs/ subtaxonomy in a later pass — assigned: Sean

- enterprise-ap-agent-spec: repo public + `/transactions/enterprise-ap-agent-spec/` route LIVE (branch merged 2026-06-08). Last open item from the Task 26 gate: ≥1 substantive LinkedIn comment from an enterprise AI PM (post ~5–7 days pre-ship) — assigned: Sean
- Substack series "Make It Sound Like Me" (working name): rewrite the post-1 draft (the-confident-stranger) against the 2026-06-05 upgraded skill — new Pi-anchored thesis ("there are many Claudes; this one writes like me" / the humbling that you can't one-shot your own brain into an LLM), grittier title, dial Sedaris way down, references rationed — assigned: Sean
- Substack value engine: build the reader-facing tool — ship a Cheese-Gauntlet + reference-universe-interview prompt kit (copy-paste, Nate-style) with post 1 [DONE — kit shipped]; design the full "interview-the-reader → emit their personal voice-mode SKILL.md" Cowork plugin as a later flagship post [SPEC DELIVERED 2026-06-07: job-hunt-2026-roadmap/2026-06-07-voiceprint-plugin-build-spec.md, working name VoicePrint, MVP ~4-6 sessions; 4 open decisions: name / MVP boundary / bundle-the-chain / public-vs-dogfood-first; BUILD not started pending Sean's go] — assigned: Sean
- Substack: lock series name (candidates: "Make It Sound Like Me", "This One's Mine", "Writing With Taste", "Raising Claude") + per-post title convention (blunt/bodily over clever) — assigned: Sean
- writing-voice-modes: send MORE raw stories to deepen the Raw Stories grit anchor in voice-samples.md (the more full pre-AI narratives, the better the register holds) — assigned: Sean
- writing-voice-modes: evaluate Bukowski as a sixth author influence (beauty-in-squalor register, distinct from Thompson's mania) — stated influence in his own NBAGT script ("The Laughing Heart" recited as thesis); evidence in script-mining report §10.1 — assigned: Sean
- writing-humanity-pass: add an over-polish guard — Sean's looser constructions are voice data (typos/run-ons consistent across 7 years of scripts + raw interviews); the pass should fix rhythm and word choice, never sand the looseness (script-mining report §10.8) — assigned: Sean
- writing-voice-modes: re-run the eval baseline (evals.yaml) after the 2026-06-04 WHAT-layer + script-corpus integration — current scores pre-date reference-universe.md, cheese-bank.md, and the 9 new/promoted moves — assigned: Sean
- writing-voice-modes: close remaining elicitation gaps — games beyond Smash (R2-3), NY sports beyond Giants (R4-3), D-series text-register samples; plus watch-list promotions needing instances: Rhetorical Catechism (1 more prose instance), Post-Chaos Inventory Shot (1 more) — assigned: Sean
- vault-critic AG hang — network theory REFUTED by 6/04 probe; hang is INSIDE gemini's request path, not the network. The 6/04 03:30 run fired the IP-family probe AT the 120s-timeout moment: BOTH Google hosts (cloudcode-pa + play.googleapis) connected in ~15-26ms on IPv4 AND IPv6, and the api.openai.com control connected too — i.e. the network was fully up while gemini's authenticated model request hung to 120s and returned 0 tokens. The 6/03 play.googleapis.com connect-timeouts were a red herring (telemetry endpoint, intermittent; the actual model host cloudcode-pa was reachable). So eliminated now: MCP, OAuth-refresh, machine-asleep, AND raw network reachability. What remains is the authenticated Code Assist streaming call silently hanging (server-side throttle-as-hang on the preview model, or a CLI stream bug) — but note: curl-reachable ≠ authed-call-works, and the CLI swallows the real error, so further --debug captures won't reveal more. RECOMMENDATION: stop chasing the gemini hang — the circuit breaker now contains it ($0 impact) and the June-18 oauth EOL obsoletes the whole path. Collapse this into the migration decision below — assigned: Sean
- vault-critic AG — DECISION (6/04, Sean): Codex-only for now (`antigravity_enabled = false` shipped); no Gemini API spend. POST-JUNE-18 TASK: re-enable AG via the **Antigravity CLI** (uses the paid Google-One subscription, NOT the metered API) and A/B test whether the second opinion is worth keeping — flip `antigravity_enabled = true` (or run once with `--antigravity`), watch a few nightlies, compare expansion quality vs Codex-only + duration. If the Antigravity CLI hits the same silent hang, add a hard <120s stream timeout or drop AG for good. gemini-cli oauth-personal EOL is 6/18 (flag 45740200) — don't test the old gemini-cli path after that, go straight to Antigravity CLI — assigned: Sean
- vault-critic budget waste — RESOLVED + VERIFIED on the 6/04 nightly. AG circuit-breaker tripped after 1 failure exactly as designed: manifest shows antigravity_calls=1, antigravity_skipped=7, codex_calls=8, articles=3 fresh + 5 backfill = 8 (was 5 on 6/03), codex_tokens=260K (was 145K), duration=323s (was 610s). Throughput +60%, budget headroom restored. With ~277s spare now, could raise backfill_max_per_run (currently 5) to drain the orphan backlog faster. Raise threshold to 2 (or 0=off) once AG is healthy/migrated — assigned: Sean
- Remove `VAULT_CRITIC_AG_DEBUG=1` from com.sean.agent.vault-critic.plist (and the run-once connectivity probe is gated behind it) once the 6/04 probe evidence pins the sub-cause — it appends gemini --debug noise + a ~10s probe to every nightly AG timeout — assigned: Sean
- Obsidian-Git stale `.git/index.lock` recurs (fixed 5/31 and 6/03) — root cause is git commit crashing mid-write when Modern Standby cuts power during the 60-min auto-commit cycle. Permanent fix instead of repeated manual `rm .git/index.lock`: either widen the commit interval, gate commits on machine-awake, or add a pre-commit stale-lock sweep that only deletes a >5-min-old EMPTY index.lock with no live git process — assigned: Sean
- Re-add fleet-memory to daily_driver via cheap read-only inject (drop MCP bridge + context-management beta that doubled cost to $0.97 and tripped the cap 5/29); reuse inject_memories_into_prompt() like vault_synthesizer — assigned: Sean
- Build a $0/run local summarizer (gemma4:e4b / qwen3 on Mac Mini) that curates daily_driver's fleet-memory namespace, so the Opus agent only reads, never writes — assigned: Sean
- Wire SDK fleet agents to surface/read open tickets from tickets.md (mirror the interactive session-start-inject-tickets.sh hook) — GATE: only do this once agents have real tasks beyond vault upkeep (e.g. substack-drafter); the current vault-upkeep loop agents don't need it — assigned: Sean
- Build a $0/run local critic-synthesizer (gemma4:e4b / qwen3 on Mac Mini) that reads the nightly critic-manifest-{date}.json after the 03:30 run, ranks+dedupes vault_critic suggestions, and writes a top-N shortlist to a SEPARATE suggestions lane (NOT the hand-curated Manual Todo) for Sean to review and promote — propose-don't-auto-promote, human-in-the-loop — GATE: only build after ~2 weeks of clean vault_critic nightly runs (recent runs show status=partial / ag_fail=5; don't triage noise) — assigned: Sean
- vault-knowledge-mcp launch comms: record the 90-sec Loom from docs/LOOM-SCRIPT.md (all 3 tools, lead on find_contradictions), drop npm + GitHub links into the LinkedIn first-comment, then post docs/LINKEDIN-DRAFT.md tagging Anthropic — assigned: Sean
- Task 12 judge layer Steps 8-9 (Sean-host): run the full pytest suite + validate.py per 2026-05-31-task-12-day-6-handoff.md; record the 90-sec --demo-injection Loom; post the LinkedIn draft tagging Anthropic + FDE-Boston JD; commit + tag judge-layer-v0.1.0 (paste-ready CHANGELOG block in the handoff doc) — assigned: Sean
- Task 12 judge layer: add the /transactions/judge-layer ledger row in sw-ai-pm-portfolio (surface: control-plane) — GATE: Gap-Fill 3 personal-site deploy must land first so the page resolves — assigned: Sean
- Task 25 LDR Grounding-Collapse: create the standalone repo from the 5 staged scratchpad files (mkdir ~/Code-Brain/ldr-grounding-collapse, copy README.md + the-failure.md + the-diagnosis.md + the-fix.md + eval-case.yaml, git init, gh repo create --public, push) — the standalone path isn't a mounted folder so it must be created host-side — assigned: Sean
- Task 25 LDR Grounding-Collapse: run writing-voice-modes on substack-drafts/2026-05-29-ldr-grounding-collapse.md, then publish Substack Post 1 (gates Posts 2-3 + the Task 13 manifesto + the Looms); confirm npm run build in sw-ai-pm-portfolio resolves /transactions/ldr-grounding-collapse/; swap the repo URL placeholder → live in both the Substack draft and the ledger row once pushed — assigned: Sean
- meta-agent monitoring blind spot: it reported "all healthy" through the vault_critic Anti-Gravity 5/5 nightly failures (5/26→6/1) because critic status=partial is treated as tolerable. Add an alert when a CLI's failures == calls (100% failure) OR tokens_total==0 with calls>0, even if overall status is partial — so a fully-dead sub-CLI surfaces. Now that the manifest carries antigravity_last_error/codex_last_error, surface that text in the alert — assigned: Sean
- job-feed: add the web3.career API token to Keychain so the web3career feed adapter stops returning [] (`python3 agents-sdk/lib/keychain.py set web3career_token <token>`; free token from web3.career/api). Keychain secret — Sean-only. (Split out 2026-06-18 from the now-resolved ATS-watchlist-decay ticket; see Done.) — assigned: Sean
- Task 23 MCP Security Audit (v0.1.1 already published to npm + registry): run npm run build in sw-ai-pm-portfolio to confirm /transactions/mcp-security-audit/ resolves, then commit the new mcp-security-audit.mdx ledger row; post sw-mcp-intent-engineering/docs/LINKEDIN-DRAFT.md (links in first comment, tag Anthropic) and optionally commit that draft file — assigned: Sean
- vault-critic AG hang — read tomorrow's 03:30 capture: check vault/90_system/agent-logs/ag-timeout-*.log + critic-manifest-2026-06-03.json for 429/RESOURCE_EXHAUSTED/Quota/"Request cancelled." If confirmed (high-confidence hypothesis: gemini-cli #22648 swallows a preview-model 429 at the 00:30-Pacific quota-reset boundary), migrate the nightly off oauth-personal to GEMINI_API_KEY (also forced by oauth-personal-via-gemini-cli EOL 2026-06-18), then REMOVE the temporary VAULT_CRITIC_AG_DEBUG=1 from schedules/com.sean.agent.vault-critic.plist + reload. Context: agents-sdk/CONTINUATION-2026-06-02-vault-critic-antigravity.md Session 3 — assigned: Sean
- daily-driver morning cost cap breach: 2026-06-02 run cost $0.6848 vs $0.60 cap (second breach after 5/29 hit $0.97) yet meta-agent reported "healthy". Either raise the cap deliberately or investigate the overrun; also fold cost>cap into the meta-agent alert blind-spot ticket — assigned: Sean
- Task 24 Discovery PRD (content-complete + council-revised; ledger row validated clean via scripts/validate_content.mjs): run `npm run build` in sw-ai-pm-portfolio to confirm /transactions/discovery-prd-content-workflow/ resolves, then commit the new discovery-prd-content-workflow.mdx (surface: product) by hand; optional LinkedIn teaser on the cross-functional-translation angle — assigned: Sean
- Add the writing-humanity-pass skill to an export-group manifest (export-groups/*/playground.json) + a preset if it should ship via the installer; until then it is correctly listed in README as not-in-any-group — assigned: Sean
- Re-verify sean-anchor + claude-mascot Bibles' Pass-3 on a PINNED model — the 2026-06-02 provenance forensics found both were verified by the Antigravity backend-default gemini-3.5-flash (agy `-p` with no `-m`), NOT the "Gemini 3.1 Pro" their docs claimed. Cy's Pass-3 now routes through the Gemini API transport (gemini-3.5-flash, pinned by ID, served model read back from resp.model_version) per the A2 hardening PR; this ticket is the COSTED re-run of the two locked Bibles on that pinned path, scheduled AFTER the hardening PR merges. Decide Flash-as-shipped vs a deliberate gemini-3.1-pro upgrade (re-baseline whichever is chosen). See docs/2026-06-02-em-provenance-and-hardening-kickoff.md §A4 — assigned: Sean
  - ESCALATION (2026-06-03, B1b DINOv2 spike): this is BIGGER than "Flash-verified." sean-anchor's BODY turnarounds are **proportion-OFF** — head reads ~1:5.3 (profile-right), ~1:4.6 (back), ~1:4-4.5 (walk-cycle) instead of the 1:7 target — and were baked into the LOCKED Bible anyway because the Pass-2.5 similarity gate is record-only (it never measured proportion). So a re-verify on a pinned model isn't enough; the body plates need a proportion **re-bake** to clean 1:7, then re-lock. Evidence: anima PR #20 + evals/vision_critic/traces/dinov2-separation-2026-06-03.md.
  - SUB-ITEM (gates on the re-bake): the Em vision_critic eval fixtures are **contaminated** — the proportion-defect fixtures (evals/vision_critic/fixtures/frames/defect_body_*.png) are SHA-identical to the drifted Bible plates (verified byte-for-byte, 2026-06-03). Once the Bible body plates are re-baked clean, those fixtures must be RE-DERIVED (or the eval is testing against drifted ground truth), and the cases.yaml labels RE-RATIFIED against the clean fixtures (Sean's call — locked-label rule). — assigned: Sean

- Publish "Raising Claude" post 1 (You Can't Prompt Taste Into a Machine): (1) create a public GitHub gist from substack-drafts/raising-claude-cheese-gauntlet-kit-PUBLIC.md; (2) swap the gist URL into the canonical post (substack-drafts/2026-06-05-raising-claude-post1.md — placeholder `https://gist.github.com/REPLACE-WITH-YOUR-GIST-URL`); (3) paste post into Substack — Title field = "You Can't Prompt Taste Into a Machine", Subtitle = "Raising Claude · Post 1"; (4) after publish, swap the live Substack post URL back into the gist's backlink (placeholder `https://REPLACE-WITH-YOUR-SUBSTACK-POST-URL`) — assigned: Sean

- writing-voice-modes eval re-baseline — RUN ON YOUR MAC (prep DONE 2026-06-06; harness can't run in Cowork). Generation now routes through your Claude SUBSCRIPTION via the Agent SDK (OAuth), NOT the Anthropic API key — _SubscriptionClient passes env={} (never the key) and warns if one is set. Prereqs: `claude login` valid, NO ANTHROPIC_API_KEY in env (unset it to be safe), Mac Mini Ollama up at localhost:5050 with qwen3-14b-research. Steps already done by Claude: stylometry baseline rebuilt (em-dash 1.41→0.08, de-contaminated) + threshold recomputed offline (=9.64, reuse-existing, zero API). FIRST RUN FAILED 2026-06-07 (SDK exit-1, swallowed) — root-caused to the invalid model string `claude-opus-4-7`; FIXED 2026-06-07 (switched to the `opus`/`sonnet` Agent SDK aliases, dropped empty-valued `--setting-sources=`/`--tools` flags, wired the stderr callback so any next failure prints `[claude-cli] ...`). RE-RUN now on the Mac: `cd agents-sdk && PYTHONPATH=. .venv/bin/python -m agents.skill_optimizer --score-only` (5 train + 2 holdout × 15 = 105 Opus gens via subscription + local Qwen judge; writes a score-only-<date> row). SECOND RUN ALSO FAILED 2026-06-07 (same generic exit-1; stderr callback produced NO output, so the CLI started, accepted the query, and failed mid-run reporting a generic error over the control channel — not the model alias). Can't diagnose remotely (SDK hides it). DIAGNOSTIC LADDER on the Mac: (1) `claude --version` then `claude --print "say hello in five words"` then `claude --print --model opus "say hello in five words"` — isolates CLI/auth/model from our SDK usage; (2) a `--debug` flag was added 2026-06-07: `PYTHONPATH=. .venv/bin/python -m agents.skill_optimizer --score-only --debug 2>&1 | grep '\[claude-cli\]' | head -40` surfaces the CLI's real error. Interpret: `--print` fails -> auth (`claude login`) or CLI broken; `--print` works but `--model opus` fails -> model access on plan (try `--model sonnet` or a full ID in config.toml generator_model); both bare work but SDK fails -> our option set, paste the [claude-cli] debug lines. Paste whichever output back. DELEGATED 2026-06-07 to Claude Code (it has the SDK/CLI/OAuth/Ollama locally): handoff prompt at job-hunt-2026-roadmap/2026-06-07-claude-code-eval-fix-prompt.md — instructs it to diagnose from real output, fix forward (incl. a `claude --print` CLI-subprocess transport as an allowed subscription-billed fallback), run --score-only to completion, and report vs 0.7267. Hard constraint repeated: subscription/OAuth only, never an API key. Compare train_score to the 2026-05-10 baseline 0.7267 — but read it knowing the eval is blunt (see next ticket). If you ever re-run the FULL calibration with fresh AI samples, that path still needs an API key (`calibrate_stylometry_threshold.py` without --reuse-existing) — prefer `--reuse-existing` to stay key-free — assigned: Sean
- writing-voice-modes eval is TOO BLUNT to detect the grit/90-10 overhaul: `sounds_like_sean` was pinned at 1.00 in the only prior run, so the criterion that should register a voice improvement has no headroom; the criteria with headroom are format-mechanics + the (now-fixed) stylometry signal. Redesign the eval to discriminate: make sounds_like_sean harder (graded/sub-checks), add a cheese-bank NEGATIVE criterion (reject MEANING_OVER_ACCESS-register output), add a grit-register check. Until then a re-baseline number won't tell you if the overhaul worked — assigned: Sean
- writing-voice-modes stylometry: the em-dash density feature is now a DEAD feature post-ban (≈0 in both corpus and compliant output); harmless (zero-stdev → guard sets std=1.0 → contributes 0) but consider dropping it from FEATURE_KEYS and re-tuning, so the 4 live features carry the signal — assigned: Sean
- writing-voice-modes skill_optimizer: cost_usd is logged as 0.0000 even on real Opus runs (results.tsv iteration 1 had real scores + duration 6303s but $0) — wire actual token-based cost from msg.usage so the $50 soft / $200 hard caps in config.toml actually engage — assigned: Sean
- Task 14 Control Architecture (BUILD COMPLETE 2026-06-07; in-session verification green): host close-outs — (1) run the LIVE demo `cd tools/governance-demo && PUSHOVER_USER_KEY=… PUSHOVER_API_TOKEN=… python3 replay_budget_breach.py --fixture over_budget` to fire a real Pushover ping + confirm exit 7 (sandbox can't page); (2) record the 90-sec Loom (split-pane: the over_budget run + `tail -f` the demo ledger + phone showing the push); (3) post the LinkedIn draft tagging the Anthropic FDE Boston JD; (4) commit code-brain by hand (`agents-sdk/docs/CONTROL_ARCHITECTURE.md` + `agents-sdk/docs/EXPLANATION.md` + `agents-sdk/config/authority.example.yaml` + `tools/governance-demo/` + CHANGELOG/CLAUDE/README; suggested: `feat(docs): ship control-architecture trinity reframe — authority + recovery + audit (Council Gap-Fill 4)`) and the portfolio `control-architecture.mdx` (`content(transactions): add control-architecture ledger row, surface infra`) — assigned: Sean
- Task 14 Council: DONE 2026-06-07 (premium, $0.4856; transcript `agents-sdk/docs/critiques/control-architecture-council-2026-06-07.md`). Convergent fixes folded into CONTROL_ARCHITECTURE.md. ONE open decision for Sean: the council (Gemini + chairman) advised dropping the internal "Task 7 STOP-DOING" label from the routing paragraph as fourth-wall-breaking; I rephrased it to a generic "deliberate, standing decision" to honor BOTH the council and your locked "explicit deferral" decision — if you want the verbatim "Task 7 STOP-DOING" link restored, say so. (Mermaid was KEPT per your locked decision despite the council's cut suggestion.) — assigned: Sean
- Task 13 voice-pass-2 (applied 2026-06-07): commit the de-cheesed access-over-meaning across both repos — code-brain `docs/MEANING_OVER_ACCESS.md` (`docs(essay): voice-pass-2 on access-over-meaning — de-cheese + cut layoff + recruiter-dialed grit`) and portfolio `meaning-over-access.mdx` (live/PUBLISHED, so the commit is a deploy: `content(essay): sync access-over-meaning voice-pass-2; lastRevised 2026-06-07`). ALSO propagate the same voice-pass-2 into the Substack cross-post `substack-drafts/2026-06-19-meaning-over-access-substack-cross.md` (still carries the old cheese prose) before the ~6/19 publish — assigned: Sean
- Task 26 AP-agent eval-suite hardening (when the suite grows past ~30 cases): add confusion-matrix output to run_evals.py — emit precision / recall / false-pass on the should-escalate class (not just per-case pass/fail), so the suite reports detection quality the way a real eval does. Files at job-hunt-2026-roadmap/task-26-ap-agent-build/ (move to the enterprise-ap-agent-spec repo when created) — assigned: Sean
- Task 26 AP-agent eval-suite hardening: expand from 1 to 3-4 cases per class (happy / edge / adversarial / controller / precision) so a single stub change can't pass by coincidence; balance the classes and keep the bite + xfail markers. Pairs with the confusion-matrix ticket above — assigned: Sean
- Task 26 Enterprise AP Agent Spec — Step 10 critique (sole remaining verification-gate item; repo + ledger live at github.com/seanwinslow28/enterprise-ap-agent-spec + /transactions/enterprise-ap-agent-spec). PREREQ DONE 2026-06-11 (Sean, via Claude Code): the ⏱ time-sensitive numbers (g5.12xlarge hourly rate + APQC $5.83 labor median in cost-model.md) re-pulled + the OpenAI vendor certs (HIPAA BAA + SAML/SCIM in build-vs-buy-memo.md) re-confirmed. REMAINING: run the hardened writing-voice-modes pass on the LinkedIn draft, then post tagging ~3 enterprise AI PMs ~5–7 days before any push to land ≥1 substantive comment — assigned: Sean
- writing-voice-modes skill_optimizer score-only (2026-06-08): the new score-only row used a LOCAL `qwen3.5:27b` judge substitute because the canonical `qwen3-14b-research` judge (Mac Mini :5050) was asleep/unreachable — the 3 structural criteria are baseline-comparable but the 3 LLM-judge criteria are NOT. Re-run with `--judge-model qwen3-14b-research:latest --ollama-base-url http://localhost:5050` once the Mac Mini is back for a comparable LLM-judge number; AND capture that model's `/no_think` Modelfile into the repo (it lived only on the Mac Mini) so the judge is reproducible without that host — assigned: Sean
- anima SF03 sean-anchor body-turnaround re-bake + re-lock — DECIDED 2026-06-08 (Sean): bundle with the **next heads-tall character authoring**, no standalone costed run. SF03 proportion gate now enforces 1:7 at Bible-lock going forward (merged #33); sean-anchor's locked body plates predate constrained generation so they read `indeterminate` (honest non-measurement, not off-proportion). Trigger: the next authoring session that already spins up the Approach-A feeder re-bakes the five body turnarounds born-1:7 + re-locks → converts `indeterminate` to a certified pass. Resolves the re-bake half of the line-48 escalation ticket; the Em fixture-contamination sub-item there is already closed by the 2026-06-03 eval-foundation reset (50→52 clean Flow-authored fixtures). Recorded in anima CLAUDE.md SF03 "Open:" note + docs/2026-06-08-em-eval-suite-completion-plan.md item #9 — assigned: Sean
- last30days X/Twitter broken by incomplete vendored dep — `.claude/skills/last30days/scripts/lib/vendor/bird-search/node_modules/@steipete/sweet-cookie/` shipped with only package.json+README+LICENSE; its `dist/` (the actual code) was never vendored, so every X search crashed with `ERR_MODULE_NOT_FOUND` regardless of auth. FROM_BROWSER=auto silently failed for the same reason. 2026-06-08 hotfix: pulled `dist/` from `npm pack @steipete/sweet-cookie@0.1.0` and dropped it into the vendored folder (verified — X returned 19 posts). FOLLOW-UPS: (1) [DONE 2026-06-08] ran `sync.sh` — dist/ propagated to ~/.claude / ~/.agents / ~/.codex, all 3 import-check OK + module resolves; (2) [MOOT] can't commit — `node_modules/` is gitignored in code-brain (.gitignore:40), so the fix is local-only and won't survive a fresh clone or `npm` clean; sync.sh re-propagates from the source copy, but the dist/ in the SOURCE copy is itself gitignored — so a fresh code-brain clone reintroduces the bug. PERMANENT FIX is upstream only; (3) [OPEN] report upstream to mvanhorn/last30days-skill — published skill vendors @steipete/sweet-cookie without its dist/ (external action, needs Sean). Also: AUTH_TOKEN/CT0 now persisted in ~/.config/last30days/.env (X session cookies, will expire) — assigned: Sean
- VoicePrint: real-human dogfood before public launch — run 2-3 non-Sean people through the full pipeline per [`creative-studio/voiceprint/docs/dogfood-runbook.md`](creative-studio/voiceprint/docs/dogfood-runbook.md); confirm each bundle sounds like them (not a template), `/voiceprint-proof` lands `closer_to: reader`, and no bundle leaks another person's content. The in-session dogfood was a simulated persona (Priya). DECISION 2026-06-11: Sean shipped the public install with Post 2 AHEAD of this, so it is now a high-priority FAST-FOLLOW (run ASAP post-launch), not a pre-launch gate. RUN-LOCATION CAVEAT (2026-06-11): run the dogfood in a folder OUTSIDE ~/Code-Brain (no CLAUDE.md, no .claude/skills in the directory tree) — launching Claude Code inside Code-Brain loads Sean's canonical writing-voice-modes/cheese-bank/reference-universe skills into scope, which can contaminate the leakage test (a stranger's bundle looks right while quietly borrowing Sean's calibration). Only the plugin's de-Sean'd bundled skills should be in play — assigned: Sean
- VoicePrint: ship as a `claude-plugins-community` GitHub marketplace repo (add `.claude-plugin/marketplace.json`) in addition to the `.plugin` file — the marketplace path (`/plugin marketplace add <repo>`) is how Claude Code users discover/install; the `.plugin` is only the Cowork rich-preview install. Per `market-read.md` §7 distribution — assigned: Sean
- [SHIPPED 2026-06-15 · https://seanpwins.substack.com/p/i-built-a-machine-to-sound-like-you] VoicePrint: Post 2 launch (the "Raising Claude" flagship) — write the Substack post leading with the differentiation the research validated (reject-based gauntlet + works-with-no-corpus + honest hours-not-one-shot loop + local/free), NOT "interview" or "build a skill" (Grill Me + Skill Creator own those). Include a one-screen before/after demo (the format that wins per the research) + the copy-paste install command. Use the verbatim demand vocabulary ("AI slop", "taste", "sound like you") from `market-read.md` §5 — assigned: Sean
- VoicePrint: deferred add-ons from the build (plan-time decision, may revisit) — a `/voiceprint-status` quick text readout (lighter sibling of the dashboard) and any further "voice report card" surface beyond `/voiceprint-proof`. Not built; `/voiceprint-proof` + the dashboard cover the core proof need — assigned: Sean
- anima Em (Tier 2) — view-correctness fix-rate 0.00: in the 2026-06-08 G6.9 Gate-3 baseline Em's declared-view-correction clauses cleared **0/6** on the no-regen label-side path (golden 3/6; all six classes discriminative, overall lift 0.667). The next constructive-axis improvement target. Investigate WHY her view diffs don't move the label-side re-critique (re-critique the same fixture under a corrected declared view); decide if fixable (tighten the view-diff content / view-rule grounding) or inherent to the no-regen path. Improvement work, not eval-completeness. Source: docs/anima-test-runs/2026-06-08-em-g6.9-gate3-fixrate.md §5 — assigned: Sean
- anima Em (Tier 3) — Gate-3 fuller 30×5 run: tighten the small-N bands from the 12×3 baseline (6 outcomes/class/arm, per-class stderr≈0.33; the per-class +2.0 lifts ride on tiny golden−null denominators, directional not precise). Same `patch_efficacy --arm both+null` machinery, `--sample 30 --rerolls 5`, ~$31.50 costed under fleet-ops. DEFER — the 12×3 already clears "axis measured + discriminative on all 6 classes"; only run when a specific per-class number needs defending — assigned: Sean
- anima Em (Tier 3) — mascot eval corpus: Em is corpus-validated only on sean-anchor. Build a claude-mascot defect/clean corpus (the box-creature register) + Sean-ratify it so Em's verdict/citation/constructive axes are measured on the second character too. Fold in the cosmetic `_criteria_block` nit (the hardcoded `IR.sean.*` example shows for non-sean frames) during mascot validation. DEFER until mascot work — assigned: Sean
- anima Em (Tier 3) — identity-mode corpus (hair/jaw/eye): coverage extension consciously deferred at G6 — add identity-drift sub-modes to the verdict corpus so SF02-style drift is measured at sub-mode granularity. DEFER — assigned: Sean
- anima evals (horizon) — next-agent eval suite: Em is now the reference template for fully evaluating a fleet agent (verdict + citation + constructive, all measured/ratified). Next highest-leverage target per the eval handbook per-agent matrix (docs/research/2026-05-31-ai-evals-best-practices-and-anima-eval-strategy.md): Maya planner / Cy character-designer / Mo museum-writer / the T3 council. Horizon marker — scope only once Em's Tier 1 close-out lands — assigned: Sean

- Substack folder reorg (2026-06-09): the substack-drafts/ folder was restructured into per-post folders (01–07) + _ideas/_archive/_experiments/_assets + a README index that is now the single source of truth (series, dated queue, status legend, image+tool maps, conventions). Queue runs 2–3×/week Mon/Thu: Post 2 (Thu 6/11) → Post 6 (Mon 6/15) → evals bonus (Thu 6/18) → Post 3 (Mon 6/22) → Post 4 (Thu 6/25) → Post 5 (Mon 6/29) → Post 7 capstone (Thu 7/2). See substack-drafts/README.md — assigned: Sean
- Substack hero images — GENERATE the 5 missing heroes (Posts 1, 2, 4, 5, 6; Posts 3/7/bonus already have one). Priority: Post 2 by Thu 6/11. Use gemini-image-gen / gemini-pencil-animation-image-gen with the refs in substack-drafts/_assets/references/; house style = cheesy-caption + grotesquely-detailed-image satire (cheese-bank format). Drop each into its post's images/hero.png — assigned: Sean
- Substack tools — build the 3 missing ships-with tools in report build-order: (1) Agent-or-Automation Advisor for Post 6 (cheapest, a weekend, loudest hook); (2) Intent Card brief-mode tools on intent-engineering MCP for Post 3 (audit_brief/scaffold_brief/assess_brief_readiness); (3) On-Brand Gate for Posts 4+5 (generalize writing-critique → pass/fail + violated clauses + slop-risk). Each unblocks its post's publish date — assigned: Sean
- Substack Post 6 hook pivot: the 06-stop-building-agents/post.md body is still the LDR "Day My Research Agent Invented Microsoft" draft (labeled Post 1 in stale frontmatter — now Post 6). Reframe the open around the agent-vs-automation hook ("automations with LLM nodes," 1,556 upvotes) so it ships with the Advisor, then run the voice chain. Note: the existing line-38 ticket calling this "Post 1" is superseded by this reorg — assigned: Sean
- Substack drafts to write: Posts 3, 4, 5 (scaffolds with pain-point + ships_with + raw-material pointers are in each folder's post.md). Post 3 is draftable now (MCP already shipped); Posts 4/5 are tool-gated on the On-Brand Gate. All three need the voice chain before publish — assigned: Sean
- Substack cleanup: delete 4 empty legacy dirs left in substack-drafts/ (images/, archive/, experiments/, substack-image-generation-references/) — the Cowork session couldn't remove them (sandbox EPERM); they're empty so git won't commit them, but tidy them in Finder — assigned: Sean

- Substack image house style v2 (2026-06-10): replaced the deprecated Steadman/voice-mode image design with the pencil-test portfolio look (cream paper, graphite, ONE accent color, blob-baby characters + amber thread, minimal watercolor bloom behind the linework). Spec: docs/substack-image-generation-design-2026-05-23.md (v2). Drop the 3 ChatGPT portfolio illustrations (desk-kid, spear-warrior, trader) into substack-drafts/_assets/style-anchors/ on the Mac so they're reusable reference anchors — they didn't transfer as files in the Cowork session — assigned: Sean
- Substack heroes — regenerate all in the v2 pencil-test style (Post 1 first, then 2/4/5/6) via the openai-image-gen skill (GPT Image 2, OPENAI_API_KEY in code-brain/.env; pass a style anchor with --reference, --quality high, --aspect-ratio 16:9) or ChatGPT, per the v2 doc's master recipe. Accent per post: amber for human posts (1/2/5/bonus), teal for technical (3/4/6/7). Post 1's pencil-test prompt is ready at 01-cant-prompt-taste/images/hero-prompt.txt (the "confident stranger rising from the laptop" concept). The grotesque NB2 path is retired — assigned: Sean

- anima Flo (Phase 5) — self-hosted FLUX + Shakker sketch-LoRA for pencil in-betweens (the $0-ongoing future). Flo-B (2026-06-10) disqualified both fal.ai in-between candidates: Seedream morphs faces through the tween (the Reve failure DINOv2 misses), Qwen-plus reframes/degrades. In-betweens pivoted to NB2 (winner) / NB Pro (backup). API edit models hope-and-pray on grain+identity; self-hosted FLUX + sketch-LoRA controls the aesthetic at generation time. Re-evaluate by dropping FLUX in as a variant in the retained re-runnable bake-off (evals/bakeoffs/2026-06-10-flo-inbetween-pencil-pilot/, run the full 4×14). NOTE: this ticket was claimed-filed in the Flo-B field report + CLAUDE.md but never actually landed here until the 2026-06-10 Cowork verification caught the gap. Until then NB2/NB Pro own in-betweens — assigned: Sean
- anima Flo (Phase 5) — NB Pro routes render SQUARE, fail HF01 (pre-flight before any end-to-end run). The nb2/standard path (pipeline/generate.py generate_frame) passes --aspect-ratio 16:9, but the nb_pro path (invoke_image_edit / nb_pro_runner) passes NO aspect ratio → renders 1024²/2048² square, which fails the HF01 16:9 gate (audit.py, 2% tolerance). So Flo's hero_keyframe + in_between_mid routes (both nb_pro) currently produce HF01-failing frames. Never surfaced in the pencil-test (Act 1 used the 16:9 skill path); will bite the first end-to-end run that routes a hero frame to NB Pro. Fix: plumb aspect_ratio through invoke_image_edit (or have frame_router's nb_pro branch request 16:9). Surfaced 2026-06-10 (Flo-B field report flagged it as a "framing confound"; it's a production gap) — assigned: Sean- anima Em CC01 (seam #10, EVAL-GATED): Em's absolute left/right stylus-hand verdict is noisy in a screen-right profile — in the first integrated run it PASSED F01 then FAILED F02/F04/F05 on the identical near-hand-draws config. CC01 is better judged frame-to-frame than per-frame-absolute (continuity_audit.py already has CC01-CC08). Fix = a frame-to-frame continuity check, NOT a per-frame-absolute verdict. EVAL-GATED: any change to Em's verdicts requires re-running the vision_critic baseline + re-ratifying — must NOT touch evals/vision_critic/ (md5 2af75906502f1caf8857e18828ceb2e4) without a deliberate re-baseline. Field report: anima docs/anima-test-runs/2026-06-11-first-integrated-run.md seam #10 — assigned: Sean
- anima museum #14 / Slice 3: the seam-#12 JPEG-as-PNG fix is forward-looking — it normalizes future candidates at FloNode generation time, but the already-shipped "Spark, Shared" frames in runs/2026-06-11-spark-shared-first-integrated/approved/ are still JPEG-data-as-.png (they assembled fine via the hand path). When museum capture (#14) copies those existing frames as thumbnails, run pipeline/agents/image_utils.normalize_to_png on them first or the museum inherits the mislabel. Fold into the Slice 3 museum-capture build — assigned: Sean
- anima FLEET HARDENING CAMPAIGN — before the costed run / production use, each agent needs more tests/evals/hardening passes to be ROCK-SOLID. Current eval coverage (uneven): Em has a full scored suite (the ratified 0.97/1.00/0.00 verdict baseline + Gate-2/Gate-3 axes); Maya + Cy have evals/{agent}/cases.yaml + pytest runners; Flo, Mo, T3 council, and the orchestrator have NO scored eval suite yet; Sam + Bea aren't built. Per the eval handbook (anima docs/research/2026-05-31-ai-evals-best-practices-and-anima-eval-strategy.md) per-agent matrix, build error-analysis-first / binary / ships-red eval suites + targeted hardening per agent. Rough priority once the fleet is complete: (a) orchestrator drift + restart/retry-rate eval over N real runs (deterministic state/JSON discipline, promote to Opus if restart >10%); (b) Flo route-correctness + the self-hosted-FLUX in-between future; (c) T3 panel-value-add (PoLL κ-delta) + chairman-synthesis fidelity (Sage-tier bake-off #2); (d) Mo never-invent fidelity. SEQUENCE: after Sam + Bea land (complete the fleet first), then a hardening sweep. — assigned: Sean
- anima orchestrator COSTED VALIDATION RUN (HELD by Sean 2026-06-11) — the first real end-to-end CLI run. Pre-flight: `python -m pipeline.run --brief briefs/2026-06-10-spark-shared --stub` (now $0, ANIMA_FORCE_STUB-gated, walks to DONE — Cowork-verified 2026-06-12) to confirm the chain; THEN drop --stub for the real ~$0.66 run from a PLAIN TERMINAL (not inside a Claude Code session — nested-SDK throttle, seam #4), under fleet-ops §0. The 5-frame shots.yaml reproduces the 5-key cut; the shipped 6-key SS_F03b grin-peak insert stays a hand edit to sequence.txt. The Slice 2.1 brief snapshot means the run won't dirty the committed brief. — assigned: Sean
- animation-pipeline upgrade — feed the Miyazaki/visual findings from the screenplay-modes calibration into the `animation-pipeline` skill (the calibration flagged these for both skills). Carry over: object-economy / Room-as-Biography / the Loaded Object, Haptic Visuality, and Metaphor-Compression-in-an-empty-frame as Sean's door into wordless *ache* (the feeling lands on a symbolic object in an empty frame AFTER the character leaves — never a held close-up on a grieving face). Load-bearing coaching note: Sean CANNOT sit in a contemplative hold — he converts stillness into a goal/errand (installs a ticking clock + a deadline that weren't asked for), so the Miyazaki Micro-Ma / contemplative stillness must be a deliberately-coached muscle, not assumed; his wordless muscle is visual-narrative (objects/rooms), not contemplative. Sources: `.claude/skills/screenwriting-modes/references/screenplay-calibration-notes.md` §2 (Miyazaki SPLIT), §3 (wordless = visual-narrative muscle), and the script-mining report §2 (room-as-biography exemplars). This is also the Sam↔Bea handoff surface in anima (Sam writes the loaded object; Bea boards it). — assigned: Sean
- intent-engineering MCP Workstream B / Intent Card — brainstorm DONE 2026-06-18; decided build-brief at sw-mcp-intent-engineering/docs/2026-06-18-intent-card-build-brief.md (grounded by docs/2026-06-18-brief-quality-research.md, a 5-agent deep-research synthesis of creative-brief craft). LOCKED: one product (brief mode = optional `mode` field on the existing 3 tools, NOT 6 tools / separate server), self-contained v1 (brand assets deferred to the On-Brand Gate), a research-validated 10-section brief rubric + focus / anti-bloat / completeness≠correctness cross-cutting checks + a documented-failure-cited anti-pattern catalog (the rational proposition + insight stay OPTIONAL = a deliberate contested stance to own in the README). OPEN: (1) `mode` default explicit-vs-auto-detect; (2) Q2 canonical dogfood brief + stand-in brand (candidate: Sean's own Substack brand); (3) Q5 naming — Intent Card vs `audit_intent_spec` in the post/README. BUILD = #2 after the Advisor, NOT started; §9 CHANGELOG approval gates the `mode` schema change before code — assigned: Sean
- discovery GATHER-stage spend invariant (Phase-3 review follow-up, LOW): the new `except DiscoveryFailed` cost-recording path in [tools/llm-council/council/discovery/__main__.py](tools/llm-council/council/discovery/__main__.py) only covers FUSE-stage spend; the generic `except Exception` after it assumes "no spend incurred pre-fuse." That holds today because FUSE (OpenRouter) is the only billable stage, but GATHER calls Perplexity Sonar + web search — if a Sonar/web call is ever instrumented to bill (or already incurs cost not surfaced as an exception attribute), a gather-stage failure would silently record $0, re-opening the exact cost-integrity bug Phase 3 closed one stage upstream. Flagged by the Task-3 code-quality review. Fix when GATHER billing is confirmed: thread gather-stage incurred cost into a typed failure the same way `FusionError.cost` → `DiscoveryFailed.cost_usd` does. Until then it's a documented invariant, not a live bug. — assigned: Sean
- ✅ DONE 2026-06-28 (Step-A hygiene, branch `feat/discovery-step-a-hygiene`): `_normalize_segment` in [pipeline.py](tools/llm-council/council/discovery/pipeline.py) now strips `:`/parens + collapses whitespace (incl. whitespace-only → "") once at the `run_discovery` boundary, so every collector gets the cleaned segment. ORIGINAL — discovery `--segment` free-text into operator-bearing queries (Phase-5 follow-up, LOW): the Phase-5 `--segment` qualifier composes raw free text into the github (`f"{subject} in:title,body is:issue"`, [tools/llm-council/council/discovery/gather/github.py](tools/llm-council/council/discovery/gather/github.py)) and reviews (`({weak}) ({sites})` grouping, [tools/llm-council/council/discovery/gather/reviews.py](tools/llm-council/council/discovery/gather/reviews.py)) queries unescaped. A `--segment` carrying operator tokens (`is:pr`, an unbalanced `)`, `site:foo`) alters query semantics rather than just narrowing the audience — a self-inflicted yield bug, NOT a security/fabrication issue (read-only public search APIs; the VERIFY gate still governs every record downstream). Surfaced by the Phase-5 final whole-branch review. Fix if `--segment` ever becomes non-interactive/automated input: strip query-operator chars (`:`, parens) + collapse whitespace via a single `segment = segment.strip()` normalization at the CLI/pipeline boundary (also closes the whitespace-only-segment edge in the sonar prompt), and/or tighten the `--segment` help to "plain keywords only." Until then it's Sean's own arg against read-only APIs. — assigned: Sean
- discovery Fusion request-shape compatibility watch (Phase-5 close-out research, LOW): `_build_body` in [tools/llm-council/council/discovery/fusion.py](tools/llm-council/council/discovery/fusion.py) sends the Fusion config as a TOP-LEVEL `"fusion"` block + top-level `model`, but current OpenRouter docs (https://openrouter.ai/docs/guides/features/server-tools/fusion) show it nested under `tools[0].parameters` with the judge as `model` inside. Ours is live-verified working (FUSION_SCHEMA.md), and no test would catch a regression if OpenRouter ever drops the top-level form. Fix-if-it-breaks: move config into `tools[0].parameters`. Add a periodic live re-verify (e.g., a quick run after any OpenRouter Fusion changelog). Surfaced by the 2026-06-21 close-out research dig. — assigned: Sean
- ✅ DONE 2026-06-28 (Step-A hygiene, branch `feat/discovery-step-a-hygiene`): `fuse()` now runs `_find_failure_reason` (a defensive scan, since FUSION_SCHEMA.md captured only the success shape) over the last 200 payload and appends any typed `failure_reason` to the FusionError message. ORIGINAL — discovery surface Fusion typed failure_reason (Phase-5 close-out research, LOW diagnosability): the Fusion server tool hard-fails with `status:"error"` + a typed `failure_reason` (all_panels_failed / insufficient_credits / rate_limited / fusion_invocation_capped / unexpected_error). Because `fuse()` reads the outer judge's `message.content` rather than the raw tool result, those reach us as a vague "did not return parseable" + one retry → `FusionError`. Safe (degrades correctly) but loses the real reason. Enhancement: detect `status`/`failure_reason` in the response (if exposed at our layer) and surface it in the FusionError message for diagnosability. Surfaced by the 2026-06-21 close-out research. — assigned: Sean
- discovery last30 HackerNews errors under --no-native-web (Phase-5 close-out, LOW): the 2026-06-21 last30 verification run yielded reddit (3 records) but HN emitted a `hackernews_error` field (0 HN records). Likely the `--no-native-web` flag our collector passes ([tools/llm-council/council/discovery/gather/last30.py](tools/llm-council/council/discovery/gather/last30.py) `_subprocess_runner`) disables a path HN needs. Degrades safely (parser handles the empty hackernews list). Investigate when convenient: try dropping `--no-native-web` for HN, or confirm HN needs native web; weigh against latency/cost. reddit alone already restores meaningful social evidence. **DECISION 2026-06-28 (Step-A): keep `--no-native-web` — it's a deliberate speed/cost choice (paired with `--quick`) applied to all sources; HN needs the native-web path so it degrades safely to empty while reddit (API) yields. Removing it would re-enable native web for everything (slower/heavier) just to recover HN, and it's an untested change against the external last30days plugin. Not worth it — revisit only if HN evidence becomes load-bearing.** — assigned: Sean
- discovery per-run cost cap can be exceeded by actual spend (Phase-5 close-out live finding, MEDIUM): the per-run cap is enforced ONLY on the pre-flight ESTIMATE (`estimated = max_cost_per_run * 0.6`) in [tools/llm-council/council/discovery/__main__.py](tools/llm-council/council/discovery/__main__.py), never against the actual `usage.cost`. A 2026-06-21 standard run on "Claude plugins and skills for 2D animation pipelines" cost **$2.74** vs the $1.50 standard cap (1.8x over); cost is web-tool-call-dominated and high-variance by topic (a deep run the same day was only $0.99). The daily cap ($10) IS checked against actual accumulated spend and remains the real guardrail (close-out testing hit $7.31/$10). Not a correctness/fabrication issue — a budget-predictability one. Options if tighter per-run control is wanted: (a) raise the estimate factor (0.6 → ~1.0) so the pre-flight gate is less optimistic; (b) print a post-run warning when actual > per_run_cap; (c) bound evidence-bundle size / `max_tool_calls` more aggressively per tier; (d) accept it and lean on the daily cap (current design). Surfaced by the close-out confidence runs. — assigned: Sean
- substack-studio (migrated 2026-06-22): decide whether it becomes the LIVING drafting home or stays a curated public snapshot. Today `substack_drafter.py:529` + `agents-sdk/config.toml:556` still write to the gitignored source under `prj-job-hunt-2026/onwards-and-upwards-5-4-26/substack-drafts/`, so new drafts do NOT land in substack-studio. Do NOT repoint `output_dir` to the public folder until a scrub-gate exists — raw drafter output can carry un-suppressed Do-Not-Promote framing that would leak into the public path. See vault/20_projects/substack-studio/MIGRATION-REPORT.md §Path-coupling. — assigned: Sean
- ⚠️ pre-existing public leak (NOT from the substack-studio migration): the already-tracked `vault/20_projects/research/2026-06-02-topic-28-addictive-storytelling-and-reader-value-mechanics-for-substack.md:106` still contains an un-scrubbed Do-Not-Promote illustrative example (the "ask lands sideways" sample sentence) in the PUBLIC repo today (the substack-studio copy was scrubbed). Recommend a source-side scrub. Also LOW: the two `vault/30_domains/creative-studio/2026-06-09-ai-*-last30days.md` reports carry mild hiring-signal framing that's already public — decide whether to neutralize at source. — assigned: Sean
- crunchbase-prep / signal-scanner (deferred build — Sean curious, NOT greenlit, 2026-06-23): repurpose the fusion-discovery-council GATHER stage (last30days + Sonar + Exa/Brave + GitHub Issues collectors) + the OpenRouter client + budget spine into a small "leading-signal scanner" = Path A from the Crunchbase prediction-mechanics discussion. Pick a ~50-company watchlist, collect time-stamped leading indicators (hiring velocity, GitHub velocity, news/Reddit mentions), and have the panel emit an evidence-attached SIGNAL-STRENGTH read — NOT a fabricated future-event prediction (the verification gate structurally can't cite a future URL; that's the key insight). It's the honest qualitative version of what Crunchbase's predictions engine does, and would double as a Crunchbase-interview portfolio artifact + possible Substack post. Path B (real ML: LLM extracts features → classical classifier over point-in-time snapshots + accrued outcome labels) needs snapshot-collection-over-time + label accrual, so it can't be bootstrapped from a single scrape (cold-start). Claude offered to spec Path A as a signal-scanner skill; pick back up when ready. — assigned: Sean
- **Two Claude-output tells Sean flagged 2026-07-30 (VoicePrint refine round 1) — decide where they get enforced:** (1) **colons and semicolons** — "I'm noticing a lot of colons in the Claude outputs lately. I had it write an EXPLAIN.md for a job application take home assignment and we counted 13 colons and semi colons. They should be used sparingly or not at all." (2) **spelled-out words instead of contractions** — "It sounds very robotic/alien when you write everything out word for word." Both are cross-cutting (the EXPLAIN.md instance was a job take-home, outside substack-studio), so they're wider than the VoicePrint bundle, where they're already recorded as round-1 lessons in `~/Documents/voiceprint/my-voice/references/voice-samples.md`. Natural home is [.claude/skills/writing-humanity-pass/](.claude/skills/writing-humanity-pass/), which already enforces the no-em-dash rule mechanically — add a colon/semicolon density check and a contraction pass. **RESOLVED in-session for VoicePrint:** `pile-state.json` `preferences.em_dashes` set `keep` → `drop` per Sean ("I also don't want em dashes"), plus `colons: sparing` and `contractions: always`. **Interaction to fix when implementing:** `writing-humanity-pass` replaces a dropped em dash in the order period, comma, **colon**, parentheses — that third step trades an em-dash tell for a colon tell. Skip it for Sean. — assigned: Sean
- **Pencil & Prompt's core premise is disproven by Sean's own test — decide whether the partner arc survives (surfaced 2026-07-30):** Sean's words: "One of the reasons why I was having trouble with this creative substack was because the models continue to get better and a simple prompt with a reference can get you interesting results, so what I was preaching wasn't true at all. It was even disproven with one of the tests I tried using ChatGPT image 2. It nailed most, if not all of the simple prompts I gave it. That's why I abandoned this Substack... trying to rewrite something I don't believe in feels stupid and like a waste of time." This is counter-evidence from his own hand against the load-bearing Itch in [01-make-the-agents-taste-yours/build-sheet.md](vault/20_projects/substack-studio/01-make-the-agents-taste-yours/build-sheet.md) ("I typed a simple creative ask cold and got the average of everyone's nostalgia") and against its Part 0 pre-flight, which cites mode collapse + [Science Advances on AI creative homogenization](https://www.science.org/doi/10.1126/sciadv.adn5290) as the spine. **substack-studio's own value gate (CLAUDE.md §3) blocks on this** — the Itch must be genuinely Sean's, and he no longer believes it. Blast radius is wider than post 1: [SOUL.md](vault/20_projects/substack-studio/SOUL.md) §6's *Steal Like an Artist* ("references do not stop revert-to-average") and the cascade pieces rest on the same premise, as does the masthead's "you quit one step early." Options, none chosen: (a) retire the publication; (b) re-aim at what his evidence actually supports (the models got good at one-shot *rendering*, so the un-commoditized part moves to sustained multi-shot consistency, direction across a sequence, and pipeline-level judgment — closer to the anima/system arc than the partner arc); (c) run a fresh `fusion-discovery-council --lens substack` against 2026 reality before rebuilding any lineup. Worth re-verifying the counter-test deliberately first (same reference, same asks, documented) so the decision rests on captured evidence rather than one recollection — and that capture would itself clear the gate as a real artifact. No post drafts exist for 01 or 05, so nothing is lost by pausing. — assigned: Sean
- **`writing-voice-modes` emits a pompous comedic construction Sean rejects (surfaced 2026-07-30, VoicePrint refine round 1, decision E3):** the family `which is <someone>'s way of saying <punchline>` and its variants. His words: "I'm also noticing that phrasing happening a lot within the writing voice mode skill. I think it's coming from the David Sedaris calibration or something. That kind of sentence opening keeps happening when trying to add comedy to a description. 'Which is her way of saying I smell like a foot' or 'Which is their way of thinking it was a catastrophe'. It sounds pompous and presumptuous. 'Oh, yes, I know EXACTLY what they're trying to say'. Just constantly adding those to sentences reaching for a silly bit. I don't really like it that much and it doesn't sound like me." The construction claims access to another party's interior then performs the translation, which is the opposite posture from his under-reaction comedy. Job: audit [.claude/skills/writing-voice-modes/](.claude/skills/writing-voice-modes/) (especially the Sedaris / Domestic Observer calibration and any references that model comedic description) for where this is being taught or exemplified, and add it as a named anti-pattern. Already recorded as a round-1 lesson in `~/Documents/voiceprint/my-voice/references/voice-samples.md`. — assigned: Sean
- **anima front-door reason-capture: write the volunteered-reason rule into the two skill files (surfaced 2026-08-03, live-test finding 1b — needs Sean's explicit approval; two-file change contract still holds):** the live test found the one unspecified case — at 8 of 17 locks Sean volunteered the reason WITH the decision, and re-asking "Why that one?" would have been interrogation theater. The orchestrator improvised correctly (record the volunteered words verbatim, flag in-chat that they're being taken as the reason); make it a rule: one line under the four rules in `anima/.claude/skills/brainstorm-front-door/SKILL.md` (+ optionally a clause in `references/session-sidecar-contract.md`) — a reason volunteered with the decision satisfies the ask; record it verbatim, don't re-ask. Watch-item to fold in if it ever misfires: excerpt-selection when the reason is embedded in a longer message (finding 2b). Also carry forward: three paths never triggered in the live test and remain design-only — the skip path, the one-guess-on-category-answer path (incl. bare-agreement-records-nothing), and SUPERSEDES — observe on future sessions rather than manufacturing tests. The T27c partner-skill build should include the volunteered-reason rule from day one. Full findings: [docs/plans/2026-08-03-anima-frontdoor-reason-capture-livetest-findings.md](../../docs/plans/2026-08-03-anima-frontdoor-reason-capture-livetest-findings.md). — assigned: Sean
- **about-me-short ("How to Solve a Problem") — production next steps (surfaced 2026-08-03, front-door session close; brief emitted + validated at `anima/briefs/2026-08-02-about-me-short/`):** stress verdict was `proceed` with five Launch-Blocking residuals, in cheapness order: (1) ✅ **DONE 2026-08-03 (writers-room session)** — the two thesis lines are LOCKED in `anima/briefs/2026-08-02-about-me-short/lines-v1.md` (6 question + 9 button candidates run): the question = "Can you show me what happened the last time you tried to check out?" (Sean: "the warm and right way to ask a user without making them feel stupid"); the button = narrator cut off mid-word — "Which brings us back to where every problem begins: a quiet—" [PROBLEM! alarm, smash to black] (Sean steered: open-ended, next problem inevitable, crew ready; B1 "cheesy and on the nose"). Cold-read **PASSED 2026-08-04 (corrected record)**: the "anonymous working PM" of the first log was Sean himself reading his own picks (non-blind — kept as his PM read, not as fresh eyes); external-human gate then **waived by owner** as stakes-inappropriate ("Not a fun About Me cartoon") and replaced with a **blinded harness read: 3/3 fresh-context PM-persona subagents picked the winner** (shuffled orders, no file access, zero tool use) reasoning from method ("behavior instead of testimony"); button beat landed 3/3, surfacing two production notes for residuals (2)/(3): smash-to-black must be instant, and the team's smile must read genuine so the sting plays as cyclicality not cynicism. Full corrected log in lines-v1.md §Cold-read. R1 fully closed; (2) stopwatch table-read of the beat sheet ×2 — Movement-3 fit and the step-back+question landing by second 60 — **next recommended session**, and it must absorb the F3 delta: a third alarm STING as punchline-coda cutting the button line (brief says "alarm fires twice"; the sting is a coda, not a cycle — see lines-v1.md §Delta); (3) silent-read pass speccing visual twins for the two audio-paired payoffs (SHIP IT bell hollow→earned, alarm-1→alarm-2) — plus a third twin for the F3 sting (caption dies on the em-dash + red flood); (4) 30s scratch narration on the existing mic, judged blind, fallback path named in writing; (5) the FIRST production stub is a 2-second pit-stop slice — the money shot is the most pipeline-hostile shot. Then the Cy lift: 4 unregistered characters need Bibles (codex-cloud, gemini-star, grok-gremlin, user) per `manifest_gap_report.md`. Fast-Follow: make the title card a gag / test the quiet 15s open on a staging page. Track: Grok-tone sensitivity read at boards; no-deadline drift watch. — assigned: Sean
- **Route B (1950s Goofy-short gouache cel) — future short in that style (Sean call-out 2026-08-03, art-viz exploration):** Sean: "We'll definitely use the Route B art style for a future short or shorts. It looks great." Best specimen: `anima/briefs/2026-08-02-about-me-short/art-viz/route-b--gpt-ref.png` (GPT Image 2 + cast lineup ref — teal/mustard mid-century palette, checkered floor, painted gouache texture, on-model cast). The register is OUTSIDE anima's closed vocabulary — when a piece is greenlit in it, run the style-register authoring playbook (`docs/architecture/style-register-authoring-playbook.md`, R→S→B arc) rather than extending inline; prompts that produced the specimens are at `art-viz/prompts/route-b-*.txt`. — assigned: Sean
- **Mac Mini satellite setup for creative-partner sidecars (surfaced 2026-08-03, partner-skill build session, Sean-requested):** MacBook Pro `~/.creative-harness/` stays the single canonical harness home; the Mini runs partner sessions as a satellite (skill arrives via repo pull; sessions write to the Mini's local `$CREATIVE_HARNESS_HOME/partner-sessions/`), consolidated over LAN into the canonical home via a small rsync helper — to be written when the Mini actually starts running sessions. Registry duplicate/loud-error rules make consolidation safe by design. Sean's reason, verbatim: "It solves the problem without overcomplicating anything or costing me money. If it gets to a point where purchasing or going through a free cloud provider, I'm willing to do that, but it can wait until further down the line" — any future cloud step is a separate decision (the harness root stays sync-free by ratified design). — assigned: Sean
- **Pencil & Prompt masthead reconcile: the origin confession is retired (surfaced 2026-08-09, Rung 0 shape session — needs a partner-session reconvene, NOT a silent doc edit):** Sean reversed the confession premise mid-session. His finding: the taste-context block was never wrong, the *running order* was — the old prompt pack delivered the block at round 3, before any exploring, so it solved the problem instantly and the remaining rounds had nothing to do, which read as the premise failing. Second reason, decisive: "Not a confession about a Substack that no one even saw." Taste blocks return as divergence mechanism #1; the series is experimentation and learning, no confession posts ever. **Docs that now disagree:** [SOUL.md](../20_projects/substack-studio/SOUL.md) §3 ("the origin confession is the flagship") and §8 (taste-transfer retired outright); [REVAMP-2026-08-05-SESSION-MAP.md](../20_projects/substack-studio/REVAMP-2026-08-05-SESSION-MAP.md) S4 (the confession brief); `pages/start-here.md` + `pages/about.md` `open_items` referencing "the live origin-confession URL" (label-only). **Confirmed unaffected:** the sameness masthead, Building the Ladder, the verdict/Graveyard policy, both-tiers, and Start Here's existing NO-confession lock. Replacement shape spec: [docs/superpowers/specs/2026-08-09-rung-0-taste-experiment-design.md](../../docs/superpowers/specs/2026-08-09-rung-0-taste-experiment-design.md). — assigned: Sean
- **Raising Agents episode candidate: the model will draw you, it just won't flatter or insult you (surfaced 2026-08-09, Rung 0 capture):** two reproducible GPT Image 2 refusal findings from the same sitting, both documented with attempt counts in [rung-0-taste-experiment/capture/prompts.md](../20_projects/substack-studio/rung-0-taste-experiment/capture/prompts.md). (1) Enhancement language aimed at an image containing a child blocks 3/3 ("more vibrant, more detail, really make it pop"), while a benign compositional edit on the same image passes — it's the prompt-plus-subject combination, not the image. (2) With a *described* likeness of Sean in the chain (words only, no reference photo), the model refuses to make him prettier (blocked 3/3) AND refuses to make him uglier (blocked 2/2), while "make the room darker" and "draw it by hand" sail through. The face is fenced; everything around it is free. Carries a real navigation lesson about where the guardrails actually sit. Also a Notes candidate on its own. — assigned: Sean
- **Pencil & Prompt Rung 1 candidate: capture the taste interview (surfaced 2026-08-09, promised in Rung 0 beat 7):** Rung 0 uses a taste-context block Sean hand-wrote in June and promises the interview as the next piece. The mechanism: the model interviews your taste out of you and emits the block, so the reader never writes eight fields by hand. Needs a live captured interview session (Surface 1 of the old build sheet is the quarry: `_archive/back-catalog/01-make-the-agents-taste-yours/build-sheet.md`), plus the both-tiers ship per CLAUDE.md §4 (copy-paste interview prompt + installable skill). Sean's larger idea to sequence after it: taste blocks for *different styles you like*, run against one scene to show the range. — assigned: Sean
- **WS3 home page collided with itself from 768px to 1366px — FIXED, and the coarse half had never been looked at** (2026-08-17): Found while chasing the segerman audit's "type-scale inversion" to its cause; the inversion was the **symptom**. On the **home page only**, the headline ran underneath the project tiles, the Projects index ran across them, and the statement and footer overlapped them — violating DESIGN §5's hard rule **L26/1** ("the headline never runs under the media column"), which had been stated since the type scale was authored and **never had a test behind it**. **Measured range**: broken at every width **768–1099 with a fine pointer** and **768–1366 with a coarse one** — every tablet in landscape. Clean at every width ≥1100 fine. Sean's phone is ≤767 and stacks correctly, which is why a real device ran this site end to end without ever showing it. **Scope is home alone, and the reason is structural**: home is the only page laid out with **absolute positioning**, so it cannot reflow, and its single-column escape hatch was scoped to `max-width: 767px`; project and About are CSS grid and were clean at every cell measured. **Root cause in one sentence: the layout and the type scale disagreed about where a band ends.** The root font-size switches at 1099 and at `(pointer: coarse) and (max-width: 1366px)`; the layout switched only at 767, so the inflated root grew the **rem-based** hero column (`--col-hero: 69rem` = 942px at coarse 1366) while the **viewport-percentage** media column (`--x-media: 46%` = 628px) stayed put. **Fix: one media query**, mirroring the root's own bands — the stacked composition already existed, already in reading order, and had simply been unreachable above 767px. `--col-hero: 100%` moved to the same band so the two rules cannot drift apart again. **Coverage: page check 18** (sweeps 360–1920 in 40px steps across all three page types, cross-checking resize against full reloads because a sweep whose two modes disagree measures nothing) **and a new CDP check** for the coarse-pointer band, which no page harness can emulate. **Both were watched failing on the real bug — no fault had to be induced, because the fault was live** — check 18 red on `main` at run `f82stk` (`widths=120 collisions=8`, 800→1080, `resizeVsReload=agree`), the CDP check red at all four tablet widths with `coarse=true` and roots 10.24 / 11.8 / 12.8 / 13.66 — and check 18 flips under `--selftest` (run `fth4tl`, `collisions=19`) by re-applying absolute positioning in the live document rather than faking its own failure. Both were then re-reddened together by hand-reverting the media query (run `fnz0ar`) before the fix was restored. **Gate is now 17 page + 9 CDP, green in Chrome `ferhtr`, Safari 26.5.2 `f5m9rz` and Firefox `fibko7`**; 72 unit tests, `astro check` 0 errors, 11 pages built. **This corrects the audit**: `diff.md` row T2 and DESIGN §5.1 graded the 1080→1120 inversion *deliberate (inherited)* because the reference has identical cliffs at identical widths — true but incomplete, and the obvious remedy (bridging the desktop band up to meet the tablet band) **would have propagated the collision to every desktop width**. **The lesson worth keeping: the audit measured type sizes and box widths and never asked whether two boxes overlap.** Measuring for collision is what found it. **Left deliberately open**: the stacked block's `gap`/`padding` were tuned for a 390px phone and have not been re-judged at 1000–1366 — a green collision check says nothing about whether it *looks* right, so that is Sean's feel gate, not a regression.
- **WS3 display scale DECIDED and shipped — and executing it found the same disease twice more, incl. a live phone defect nobody had seen** (2026-08-17): The audit's largest single ratio, closed. Inner-page display size was `calc(var(--fs-hero) * 0.52)` repeated in `work/[slug].astro` and `info.astro`, absent from DESIGN.md entirely — at 1280 our About title was **3.95× smaller** than the reference's and our project title **2.16×**, while our home matched his to **3%**. Now **`--fs-title-about` 1.15× / `--fs-title-project` 0.85×**, named in `tokens.css` with the reason beside them: measured on the built site at **67.3704px** and **91.1481px**, inside 0.1px of spec. **The order About > home > project diverges from the reference (About > project > home) deliberately** — Sean's call that on a project page the work hits first, so the title reads second. **Both ratios sit below his on purpose**: his display face is weight 400 at +0.74 tracking, ours is Anybody 900 at −0.79. **A test pins the architecture, not just the values** — it fails if any page component derives a display size with its own `calc()`, the exact shape of the original drift. **Renames**: Agent Fleet Observability → **Agent Dash**, Vault Synthesizer Eval Suite → **Vault Evals**, Intent Engineering MCP → **Intent MCP**; longest title 28 chars → 11; slugs untouched and asserted, since they carry URLs, asset filenames, motion-fit keys and `view-transition-name`. **THE SPEC WAS DEFECTIVE AND MEASUREMENT CAUGHT IT.** Its "Verified before speccing" section measured **one width, 1280**, and generalised with a sentence true only *within* a band. Across all three bands the ratios break **every width below 1100**: project **105.5%** of its column at 768–1099, About **149.5%** with the *T* hidden behind the portrait, and on a phone project **140%** plus a sideways-scrolling page. **Neither cause was the ratio.** (1) **768–1099 is the home-collision defect on the other two pages** — both inner pages carry a stacked composition scoped to `max-width: 767px`, the identical structure found on home, while the root switches bands at 1099; `.work`'s head column is crushed to ~63% of the viewport and `.info`'s three rem columns (`52rem + 32rem + auto` + gaps + edges) over-subscribe it by ~112% so `minmax(0, …)` collapses the head to ~28%. Fixed with **the same one-line media query, now on all three pages**; both stacked grids also moved `1fr` → `minmax(0, 1fr)`, because bare `1fr` has a min-content floor and an unbreakable word inflates its own column, hiding its overflow from any element-level check. (2) **≤767 was ALREADY BROKEN before this session, on home, and nobody had ever looked** — "MANAGER" is one unbreakable word **354px wide inside a 308px column at 390px**, running off the right edge of **every phone from 360 to 767** and scrolling the page sideways. **Verified against the pre-session build (`3e2048e`) in a throwaway worktree rather than assumed**, so "pre-existing" is measured, not claimed. Sean saw it in a screenshot of his own phone width. **His call, taken against rendered specimens of the real face at 390px in a picker console: condense rather than shrink** — `--wdth-hero` **95 → 82 on phones only**, so the headline keeps its full 69px and the letters pull in, using an axis the display face already carries and the site had only ever set once; `--fs-title-project` follows at **0.70×** because "Groundwork" is the longest title and sets the ceiling. Options A (smaller type) and C (both, gently) were rendered and rejected. **New coverage: floors check 19** — display text against its own column **plus** a document-level sideways-scroll assertion, because when a box stretches to an unbreakable word the element-level test reads clean (`/work/groundwork/` at 390 reported `clientWidth === scrollWidth === 431` inside a 471px document). Watched failing first (`fynhmg`, `overflow=27 sideways=22`) and flips under `--selftest` (`fzorhh`, `overflow=112 sideways=49`) by doubling the display token in the live document. **Gate is now 18 page + 9 CDP, green in Chrome `f9n65d`, Safari `fwe8xh`, Firefox `fofne4`**; **78 unit tests** (the plan predicted 77; a sixth assertion pins the phone band), `astro check` 0 errors, 11 pages built. **The lesson, and it is the third instance of one disease**: L26/1's box collision, the inner titles below 1100, and the phone headline are all the same disagreement — a **rem** type token driven by the root against a layout column that does **not** move with it. Check 18 measures box-vs-box and is structurally blind to a headline overflowing its own column, because that collides with nothing; check 19 is its sibling. **Non-goal 4 of the spec ("no second step is added") is overturned, by fit rather than taste.** **Left open**: the stacked band's gaps/padding at 768–1366 were tuned for a 390px phone and now apply to project and About as well as home — Sean's feel gate, not a regression.
- **WS3 spec 2 (the three layouts) DESIGNED, SPECCED, PLANNED and handed off — plus the twist that turned out to be the content, not the layout** (2026-08-17): Third of the audit's four decisions. **Not executed** — [spec](../../../seanwinslow.com/docs/plans/2026-08-18-three-layouts-spec.md) `1a4cce0`, [plan](../../../seanwinslow.com/docs/plans/2026-08-18-three-layouts-plan.md) `d5e56c6`+`56f99a2`, [kickoff for a fresh inline session](../../../seanwinslow.com/docs/prompts/2026-08-19-execute-three-layouts-kickoff.md) `3e9f44a`. **What it builds**: home becomes a film strip (tile gap `6rem` → **`2.4rem`**, the reference's own number, captions off); the project page takes his composition (**title full-width → media band → description and metas in one bottom row**, sticky Projects index removed, `← Projects` affordance replacing it); About becomes **four quadrants** (title top-left, portrait right, prose bottom-left, metas bottom-right) with metas **Tools / Record / Elsewhere / Available** — where the reference lists Awards we list receipts, each with its measurement date, and Available moves up out of the fixed footer. Markup and CSS only: no JS, no dependency, **no new media by design**. Gate 18+9 → **19+9**. **THE SESSION'S REAL FINDING IS THE TWIST.** Sean picked the "drop the Projects list" option and then reframed the whole thing: *"the twist is the actual content we're presenting and how we present it… segerman's showing screen recordings of the websites he built… We should give each project their own personality and do the same thing, but SHOW the projects through the visuals… One of the biggest things we have to get across is my judgement and reasoning behind my decision making."* **His premise was researched before it was designed against, and it survived two independent sources** ([research doc](../../../seanwinslow.com/docs/plans/2026-08-18-judgment-layer-research.md), `a46aab8`, everything in it marked READ rather than measured). Nate Jones' Executive Circle archive opens on a reader with Sean's exact problem — *"the work they were proudest of was the work they could not show"* — and lands on *"the work sample is no longer enough; **the thinking layer has to travel with the work**"*. Public hiring commentary states Sean's premise almost verbatim from the employer side: *"building is dramatically cheaper with AI, which shifts the bottleneck from execution to judgment — when shipping is no longer the hard part, the question becomes whether you knew what to ship and why."* **The usable output is Nate's four questions, adopted as a house grammar for the media band**: situation → **the fork (what was rejected)** → the catch (the risk, because *"a lot of good judgment looks like nothing happened"*) → what changed, with a real number and its measurement date. Beat 2 is the one no portfolio shows and is exactly what a finished artifact hides; beat 3 is *why* these projects resist a screen recording at all. **The content largely already exists in code-brain's own CLAUDE.md** — $1.16/file vs $0, eight of ten agents disabled after an audit found two producing value, `fallback = "none"` so an off-hours miss never silently bills the paid API. **Sean's assumption that we were already looping the film strip was WRONG and the correction matters**: the loop is DESIGN §5.1 **decision 4**, recorded as open, never built — his home is 800px and does not scroll at all (wheel never moves `scrollY`; at 4,800px of wheel the first image is back), ours is a 2,191px document with 1,391px of scroll. **The tight gap is downstream of the loop, not a substitute for it** — which is why 2.4rem felt obviously right to him and read as risky to me. **Carried into spec 3 as the sharp hazard: a loop that clones tiles clones their `view-transition-name`, and a duplicate silently kills the morph** — precisely what floors check 16 exists to catch. **Dropping the captions superseded a recorded reason** (§5 named the plate rather than the `<figure>` *because the figure carried a caption on home and none on a work page*); it now has two better ones, Sean's leading — a strip that loops cannot stutter the same seven captions past you every 4,800px, and `view-transition-name` permanently makes an element a stacking context and backdrop root, which on the plate is confined to the picture and on the `<figure>` would constrain three separate compositions. **Two defects were found in my own plan during self-review against the real source and fixed before handoff** (`56f99a2`): check 20's index selector double-counted the component and its page-side wrapper, so `/work/` would have reported 2 against a plan predicting 1 with no way to tell a wrong selector from a failed removal; and a step said to add rules "beside the existing `.chrome__name` rules" when there are none. **Left open for Sean at the feel gate**: the strip at 2.4rem is specced for a reel spec 3 has not built yet (contact-sheet is the plausible failure, one-number fix); whether `Contact` becomes a fifth meta if the character signature reads orphaned in the footer; and the stacked tile gap at 768–1366, deliberately left at `5rem` to be judged on a device rather than derived.
- **WS3 spec 3 (the looping film strip + the warp) SPECCED — the last of the audit's four decisions, and it ships with its own exit ramp** (2026-08-17): [spec](../../../seanwinslow.com/docs/plans/2026-08-19-loop-and-warp-spec.md) `73569af`. **Needs a plan, not a brainstorm — the decisions are taken.** DESIGN §5.1 **decision 4**, the one the audit explicitly flagged *"do not decide it in a hurry"*, plus **decision 3** (route transition 520ms → **1200ms**) folded in because it is the same moment and would otherwise mean building the velocity signal twice. **Depends on spec 2 having shipped**: it needs the `2.4rem` gap and the removed captions, because a caption repeating every cycle is the tell that turns a reel back into a list. **THE SCROLL MODEL WAS THE WHOLE DECISION, and Sean chose B.** Three options were put to him: (A) his one-screen fixed shell — `html` fixed, wheel captured, genuinely infinite, and by far the most invasive change this site could make; (B) **a `position: sticky` pinned section inside a document that still scrolls natively**; (C) a recycling column with no pinning. **B was chosen so the accessibility floors keep working without rescue** — keyboard paging, screen-reader virtual cursors, find-in-page and `prefers-reduced-motion` all work today precisely because nothing is hijacked. **A is staged rather than rejected**: the accumulator, reel and wrap are the same machinery A needs, which is what makes the retreat a shell swap instead of a rewrite. **The velocity signal is ported, not invented** — Session A recovered the whole chain from his shipped bundles: wheel clamped to ±100, ×0.6 into an unbounded accumulator, lerp 0.07/frame, ×7.5, a second delta-corrected filter at base 0.3, and **absolute speed under 0.05 snapping to zero** (that last one is what stops the warp shimmering at rest and must be ported with the rest). **Lenis is NOT adopted** — on his site it is only the desktop input normalizer and he owns the accumulator himself; DESIGN §5's Lenis bullet is a licence, not an obligation. **THE WARP IS A LADDER, because his warp is a bend and a CSS transform cannot bend**: his shader scales each vertex's x-position by squared distance from centre × `0.2 × uSpeed`, so edges travel further than the middle and it reverses with direction, and DESIGN §5 already records the ceiling (*"a linear CSS skew past ~1.5° reads as a cheap italic rather than a bend"*) — written before anyone had his numbers. Rungs: **1** `perspective` + `rotateY`, which is *projective* and therefore non-linear in the same direction his is, and which DESIGN never considered because it predates the measurements; **2** SVG `feDisplacementMap`, a true per-pixel bend; **3** canvas in vertical slices, literally a software vertex shader; **4** WebGL. **Rung 4 is named as a CLIFF, not a step**: his WebGL meshes are precisely why he needs barba, so moving our tiles into WebGL re-opens the route-transition architecture DESIGN §5 settled deliberately, the zero-JS work-page payload, and the four floors checks built around them. It is a decision to be taken at the top of a session with the cost stated, never the next thing tried when rung 3 disappoints. **Also recorded: his home and project pages warp on DIFFERENT axes** — home bends the sides (`5e-5`, squared distance from centre, applied to x), project bends top and bottom (`2e-5` → ×200 → ×0.6 = effective `0.0024`, squared vertical position). Two effects, not one. **SEAN'S OWN REQUIREMENT SHAPED THE SPEC**: *"if I play with it and find the ending or the warp feels off, we should take a step back and figure out how to properly set up the project so we don't keep running into issues because we went down a different path."* So the spec names **five failure conditions in measurable terms before any code**, each with a pre-agreed move: F1 warp reads as italic → climb the ladder, keep the loop; F2 p95 frame time crosses **16.7ms** (real baseline to fail against — this site measures p95 9.9/10.2/10.2ms through the handoff and a locked p50 17/p95 17 over 645 frames on his iPhone) → drop a rung or restrict the warp to the hovered tile; F3 visible seam → a bug, not a pivot; F4 the ending reads badly → **escalate B to A**; F5 any accessibility floor breaks → stop and fix, no floor is traded for an effect. **Budget: +12 KB gzip on home, hard** (home ships 9,762 B today); work pages stay byte-unchanged at 7,680 B with zero external JS, because the reel is home-only. **Three things flagged as NOT verified rather than assumed**, and they are the plan's first instrumentation: `position: sticky` inside the intro's transformed `<main>` (the armed state parks it at `translateY(100vh)`, and a transformed ancestor changes how sticky resolves); `view-transition-name` on an element inside a sticky, translated subtree; and whether rung 1 is enough, which nobody has seen. **New coverage the plan must add**: check 16 green **mid-wrap** (the clone hazard), check 15 clicking a tile **after the reel has travelled**, no warp under reduced motion or keyboard scrolling, and frame budget under scroll at 1× and 4× throttle.
- **WS3 floors check 15 is INTERMITTENTLY RED and nobody knows why — the harness's own credibility is the thing at risk** (2026-08-17): Found while reviewing Codex's spec 2 branch: `npm run floors` reported `check=15 verdict=BAD` (`run=fow8db`) claiming the tile→hero morph did not happen — *"no `::view-transition-group(scene-…)` animated, so the root crossfaded and the tile did NOT morph"*, with all seven `::view-transition-old(scene-*)` present and **no `new` or `group`** for the clicked pair. **It is NOT a regression and NOT caused by spec 2.** Established by four independent measurements: the `view-transition-name` is present and unique on both sides (`grep` of `dist`), floors **check 16 was green in the same run** (`/ names=7 | /work/code-brain/ names=1`), a **live-transition probe hooking `pagereveal` on the destination** shows `::view-transition-group(scene-code-brain)` **and** `::view-transition-new(scene-code-brain)` both present at **1280×800, 820×540, 820×900 and 1280×540** (`MORPHED=yes` at all four), and the branch then ran **green twice in a row** (`fskwee`, `f01wbh`). **`main` flakes at the same rate — 1 red in 3 runs** — so it predates spec 2 entirely. **Why this matters more than a flaky test usually does**: this repo's standing lesson is *"a green check is a claim"*, and check 15 exists precisely because **four distinct faults all look like an ordinary successful navigation** and none of them throws or logs. An intermittently-red check inverts that protection — the next genuine breakage gets waved through as "just the flake", which is the exact failure mode the harness was built to prevent. **Also observed the same day and also unexplained**: one `npm run floors -- --firefox` run reported `cdp=RED` while its page suite was green, not reproduced in three subsequent runs, failing check name not captured. **Two intermittents in one day in a harness whose whole value is trustworthiness.** **Suspected mechanism, untested**: check 15 clicks a tile *while its hover clip is playing* (`clipPlaying=true` in every run) and reads `document.getAnimations()` at `ready`; a race between the still↔clip crossfade (`--dur-mode`) and the snapshot would produce exactly this signature — the outgoing document captures fine, the destination contributes nothing. Spec 2 plausibly *widens* the window without causing it, since the work page's media band went full-width and the destination snapshot is now 663px tall against 307px before. **Job**: reproduce deliberately (loop `npm run floors -- --no-cdp` 10× and count), then either fix the race or make check 15 retry-once-and-report rather than fail — but **NOT** silently, since a check that retries until green is worse than one that flakes. Until then, spec 3's plan tells its implementer to re-run once before investigating a check-15 red, which is a workaround and is recorded as one.
- **AI PM System Design Thinking program restarted from scratch — research done, map drafted, four things deliberately left open** (2026-08-17): The 2026-08-16 program (`prj-ai-pm-system-thinking-strategy`, 7 modules, 28 artifacts, notebook `0abf9bb0`) was **consumed at M1 and rejected by Sean** — too abstract (its architecture module taught RAG as "retrieval quality is measurable", which does not let you draw a pipeline), no design-thinking half despite the seed doc's thesis, and every exercise pointed at his own fleet so nothing transferred. **Retired, NOT deleted** — Sean explicitly wants notebook `0abf9bb0` preserved; it holds $4.12 of paid research. New home: `vault/20_projects/prj-ai-pm-system-design-thinking/`, notebook `bcb4e6aa`. **The calibration correction is the session's most important finding and must survive**: Sean is NOT a novice. He runs a 12-agent production fleet with circuit breakers, fail-closed cost guards, hybrid routing, budget caps and grounding gates — **all built by instinct, none by name**. The gap is vocabulary, structure and articulation, not knowledge. Teaching consequence: never explain what an agent is; do give canonical names and provenance; **never reference a company or paper without a one-sentence setup** (that was M1's actual defect). **Research spend $9.80 of $15–25**: Gemini DR $2.80 (failure frameworks + taxonomies + expertise research + assessment) and DR Max $7.00 (four literatures → an ordered decision procedure). **CORRECTION on caps, made in-session:** the operative `[gemini.budget]` values in `agents-sdk/config.toml` are **$50/month and $20/day**, not the $20/$10 code defaults the skill doc quotes — so month-to-date is **$21.00 of $50** and today **$12.60 of $20**, with headroom. An earlier claim that Gemini was exhausted until September was wrong. **Also flagged: a sixth August ledger entry (2026-08-17 12:02 EDT, $2.80, DR tier) was NOT run by this program** — its query is Golden Loop Phase B offline-evaluation/holdout-discipline research matching the partner session's L7 budget line, so it is almost certainly a parallel session, but Sean should confirm rather than assume, since the alternative is something running unattended. **THE EVIDENCE IS THREE-LAYERED AND NO SINGLE SOURCE WAS SUFFICIENT**: DR Max = what to decide (Intent/Ground/Contact/Consequence, from CRISP-ML(Q), RE4AI, Amershi, Sculley); DR #1 = what breaks (ML-FMEA / ASTRIDE / STPA, the ACL TrustNLP 33-mode RAG taxonomy, Microsoft's agentic taxonomy v2.0); **Nate B Jones' Executive Circle = what to build**, and it exposed **13 gaps the academic literature could not**, eight of them one missing module (the harness: tool registry, permission trust tiers, session persistence, **workflow state vs conversation state + idempotency**, pre-turn budget checks, stop-reason taxonomy, provenance-aware context assembly, day-one invariant tests). Sean's instinct to add Nate was correct and is worth repeating on future curricula. **AUDIT DISCIPLINE HELD AND CAUGHT A REAL PROBLEM**: DR #1 graded 31%A/56%C/8%D but the split is **wildly uneven by section** — §1–3 are peer-reviewed and trustworthy; **§4 (what interviews assess) is built almost entirely on interview-prep vendor SEO** (fonzi.ai, tryexponent, prepfully, interviewnoodle, four Medium posts) and enters the program as a hypothesis, never as fact. This is CLAUDE.md's own "market-shaped questions pull vendor marketing" rule reproducing itself as a clean natural experiment inside one report. **`last30days` was then run to remediate it and FAILED** — 25 Reddit threads returned r/UXDesign career-malaise, top YouTube hit was a 2022 Fireship comedy sketch; there is no 30-day public discourse at this specificity. WebSearch hit the same vendor wall. **So the interview-assessment question has no good source and remains the program's one soft spot** — the honest remedy is real job descriptions from Sean's target companies plus his own interview notes, and that is deferred work. **Verified by hand rather than trusted**: Amershi CHI 2019 (18 guidelines, validated with 49 practitioners against 20 products), Sculley NeurIPS 2015 (CACE, glue code at 95%+ of a mature ML codebase), the ACL TrustNLP RAG taxonomy (**12 of 33 modes have no peer-reviewed evidence and all 8 agentic modes are among them — an evidence desert in the fastest-growing pattern**), Microsoft's taxonomy v2.0 (12 months of red-teaming deployed systems), "Stochastic Tax" (real — arXiv 2605.27320, Hydari/Iqbal/Ramasubbu, debt is a stock and tax is a flow, but a fresh preprint not settled canon), **ASTRIDE (real — arXiv 2512.04785, Dec 2025, but it is a *platform* paper using fine-tuned VLMs; the durable teachable content is STRIDE plus the agentic threat category, and STRIDE-AI at arXiv 2605.17163 is a competing extension, so this area is unsettled and should be taught as such)**. **"Intent Debt" could NOT be verified and was dropped rather than taught as canon.** **Open, in order**: (1) the council pre-mortem's findings need folding into the map before it locks — draft at `curriculum/curriculum-map-draft.md`, critique landing at `curriculum/2026-08-17-council-premortem-curriculum-map.md`; (2) ~~Golden Loop migration~~ **DONE 2026-08-17 (`25359b3`)** — and it was not two stale docs: a parallel session had shipped Phase B that morning, so `product/`, `posts/`, the falsification verdict and five competitor teardowns all moved by `git mv` with history intact (`git log --follow` verified). The retired curriculum stayed behind **on purpose** — `m7-lesson-evals` is still the product's domain-vocabulary source — so five references point at the old project deliberately and are now labelled as retired reference rather than live spec. `00-strategy.md` carries a superseded banner. **Three facts in `kickoff-prompt-phase-c-wayfinder.md` were wrong and would have mis-planned a fresh session**: the curriculum is 5 modules with no M7 (evals is M5, week 6–7); Golden Loop ships weeks 9–12, not week 8; and the $8–15 Phase D council reserve no longer exists (two workstreams, one $15–25 approval, $17.58 spent, ~$7 left). **The subtle one worth remembering: a bulk path rewrite silently repointed "curriculum/curriculum-map.md" at the NEW five-module map while the surrounding sentence still described the old seven-module one** — the kind of drift that survives a find-and-replace and reads as correct. Also fixed a pre-existing broken path (pain evidence pointed inside the project's own `research/`; the file has only ever lived in the shared `vault/20_projects/research/`). 19 repo-local refs verified resolving, 0 broken; (3) M1 notebook sources need hand-curating and tier-labelling into `bcb4e6aa` — **no bulk research imports**, since that is exactly what graded the old notebook 17%A/60%C/20%D; (4) the verbal-training ramp needs a transcription path decided — macOS dictation works today at zero setup, and whether a Whisper path exists on this machine has **not** been checked. **The scope tension Sean should own rather than inherit**: eight modules now, four-and-four agreed (4 weeks curriculum, 4 weeks build), and over-scoping is precisely what killed the last program — the council was asked to attack this specifically.

- **README export-group count table is stale in every row (surfaced 2026-08-10, wince code review):** `README.md:117` presents per-group skill counts as authoritative in a public repo and **not one row is correct** — core-features says 12/actual 11, pm-workflows 15/16, creative-projects 8/12, advanced-techniques 7/11, technical-stack 9/10, master-designer 9/10, community-resources 6/5, domain-specific 5/6, life-optimization 8/9. code-brain CLAUDE.md's "When Modifying" rule requires updating count tables on every new skill and it has been silently skipped for a long time. Two-part fix: correct the table once, then add a `scripts/validate.py` check comparing each manifest's `skills` array length to the number in the table so the drift can't return. The validator already walks every manifest, so this is a few lines in `validate_export_groups()`. — assigned: Sean

- **`wince` follow-ups from the two dogfood gates (surfaced 2026-08-10, both gates PASSED; none of these blocked the ship):** (1) **Neither gate exercised a single refusal branch.** Persona A shot empty night streets, persona B shot a cosmetics jar, and both are about as safe as subjects get, so `references/degraded-paths.md` (refusals, sanitized returns, transport failures, words-only) has never met a real content filter. That file was written from real measured data on 2026-08-09 but its handling is unproven. The cheapest real test is a persona whose subject is people. (2) **The copy-paste kit only carries the drawing-column phrasing** for MEDIUM / SUBSTRATE, THE HAND, and COLOR / LIGHT. The installed skill branches three ways after gate A found the drawing wording asked a video editor what made those marks; the portable tier never got the fix, so it is silent on camera work and synthetic work generally. That is the tier a stranger actually uses. (3) **Both gates ran self-play, which biases every dig toward success**, and both returned zero thin fields. Both testers flagged this themselves and said not to read 0/10 as evidence the interview is that good. A real run with a real person is the only thing that settles it. (4) Smaller: `references/examples/*.md` carry headers above their fence, which violates `library.md`'s "the file opens with the fence, nothing above it, ever" rule; correct as-is since `examples/` is not a library directory, but a model reading both could infer the header is part of the emitted shape. And the leakage grep used by the gates matches `lo.?fi`, which fires on the sibling example's *filename*, so any future cross-reference between the two example files is a false positive. — assigned: Sean

## In Progress

## Done

- **WS3 route transitions SHIPPED — the tile becomes the project, at zero JS; research was the gate and it moved a line in §5** (2026-08-16): DESIGN §5's second build-order item, and the thing that delivers the project-page reveals Sean flagged on segerman.dev. Pure-CSS cross-document `@view-transition`, no JavaScript, no dependency, gated behind `prefers-reduced-motion: no-preference`. **Research ran as a gate, not a step, and earned it.** §5's line "Firefox gets today's instant navigation" is still TRUE — but "Firefox does not support view transitions" is now FALSE: Firefox **144 shipped the same-document half** (`view-transition-name`, `startViewTransition`, the pseudo-elements, `view-transition-class`, `:active-view-transition`) and none of the cross-document half; Mozilla's meta bug 1860854 was `NEW` with no milestone as of 2026-08-04. Stopping at "Firefox 144" in the compat table would have marked a true sentence stale and re-opened a settled architecture. Every feature used has the same floor, **Safari 18.2**, so there is no tier between all of it and none of it. **Three decisions, each made from a measurement rather than an adjective.** (1) *The name goes on the PLATE, not the `<figure>`* — the figure carries a `<figcaption>` on home and none on a work page, so naming it would name two boxes of different aspect ratios, and these snapshots are bitmaps stretched to their group with `object-fit: fill` by default. (2) *The six non-morphing tiles are the decision, and the default is not "nothing"* — naming an element pulls it out of the root snapshot into its own top-layer group, so left alone six tiles hang over the destination page for 520ms, **350ms after the page they belong to has gone**. They now leave with the page, discriminated in pure CSS by `:only-child` (an unpaired capture is the only child of its image pair), proved by giving those six a 999ms duration and reading it back off `getAnimations()`. (3) *The page does not crossfade* — it leaves, bare paper shows for 150ms, the next page arrives; a plain crossfade puts two 107px display headlines on screen together and the double exposure is visible in stepped frames. **No new easing curve**: the plate travels on `--ease-sweep` (minted in §5 for exactly this shape of move) and the two pictures hand over on `--dur-mode`, which tokens.css already defines as "the tile still-to-motion crossfade" — a tile clicked with a fine pointer *is* showing its clip. `--ease-pop` untouched. **Every navigation path has a decided answer**: home tile → hero morphs; a Projects-column entry morphs the corresponding tile, including one 2030px below the fold (measured: decelerates into frame at ~40% of the duration); work → work is an in-place dissolve because both plates are `:only-child` in the *same box*, so the plate holds as a frame while the words change — better than a morph and needing no rule; `/info/` and 404 opt in for the root fade; a deep link has no origin document and correctly gets nothing. **Coverage 13→16 page checks and 7→8 CDP checks, all green in Chrome 151 AND real Safari 26.5.2**, plus 72 unit tests and a clean `astro check`. Four new checks rather than one because **four separate faults produce an identical, entirely normal-looking navigation** — no opt-in on either page, a duplicate `view-transition-name`, no engine support, reduced motion — and none of them throws or logs, and cross-document leaves no promise to reject. Check 15 asserts the only two things a live transition emits (a `ViewTransition` on `pagereveal`, a `::view-transition-group(scene-…)` alive at `ready`) and clicks the tile while its clip plays; check 16 reads computed names off every element of five documents; check 17 is the §9 tile-click contract nothing had ever tested; CDP 8 runs the same click with reduced motion on and off and demands both answers, because an off-only assertion passes against a completely broken build. **The harness needed a seam, not a driver** (same shape as `?nowebgl2`): `pagereveal` fires in the incoming document before first render and the parent cannot hook it, because a same-origin navigation keeps the **same WindowProxy** — the first version of check 15 reported `pagereveal=false` against a site where the transition was demonstrably running. `scripts/vt-probe.js` is served from outside `dist` and injected by the harness server on a **cookie**, since a real link click navigates to a bare URL and only the destination can answer. Settled first by experiment: cross-document transitions **do** run inside a same-origin iframe, which is what keeps these checks where real Safari can run them. **Check 17 found a fact about the INTRO nobody had named**: the armed state parks `<main>` at `translateY(100vh)`, so the tiles are not at any viewport coordinate at all — `elementFromPoint` returned null, which was the belt reporting itself. Three belts now measured, with which one holds when: geometry and the cover while loading, `inert` **alone** at `settled` where the cover takes `pointer-events:none`. **All four watched failing first**: `--selftest` flips all three page checks (16 induces a *real* duplicate rather than faking the result); check 15 also run against a build with the opt-in deleted (`viewTransition=false`, `sceneGroups=[]`); CDP 8 run against an inverted gate and failed in **both** directions. **Also measured and deliberately left alone**: the paper grain drops out for the length of a transition — 0.0517 vs 0.4889 high-frequency energy over bare paper, because the pseudo tree renders in the top layer above the fixed grain. Under half a grey level; recorded so it is not rediscovered. Commits `6c1a105` `206a5c0` `d12dc00`. **Open for Sean, neither blocking.** (a) *The feel gate* — production build served on the LAN at `http://192.168.68.54:8123/` with a stepped-frame console at the artifact link in that session; three calls are flippable in a minute (fade-through-paper vs crossfade, whether the other six tiles get their own exit, the 520ms duration). **Sean's two calls, taken this session and recorded in §5:** the work page gets **no arrival beat** with this (the morph ships alone; the headline write-on stays where the build order puts it, next — and the repo has no footage of the reference's project pages to judge sufficiency against, only the already-mined intro clip), and **Firefox stays unverified for now** (it was not installed on this Mac, so closing the gap meant installing a browser and that was not mine to do). **Sean then installed Firefox 153.0.4 the same evening**, and a reconnaissance pass of the page suite was run in it before the next session's prompt was written: **15 of 16 green, check 15 the only red, and it fails correctly** — Firefox has no cross-document view transitions so `pagereveal=false`, while the same run shows the navigation landing on `/work/code-brain/` with the right `<h1>`, which is §5's Firefox contract observed rather than assumed for the first time. The ticket is **not** closed: `npm run floors` has no `--firefox`, the CDP suite cannot reach Gecko, and one manual pass is not a gate. **Still unverified, unchanged in shape and larger by one**: Firefox has still never been run; touch is still untested (and a tap matters slightly more now, since a fall-through would be a navigation — three belts stop it, all tested, but in a desktop browser rather than under a thumb); reduced motion in WebKit is still unverified, which now also means the reduced-motion half of route transitions is checked in Chrome and **not** in Safari, since no page harness can set that media feature. **AMENDED 2026-08-16 (later): re-running the gate to verify these numbers rather than inherit them found a defect in the gate itself — commit `a12ab99`.** `npm run floors` reported *"the page suite never reported"* while the suite was, in the same moment, **green**. Third time in this harness the *instrument* rather than the site was at fault, and the first time in the **runner** rather than in a check. Mechanism: the standalone server that hands Sean's phone a URL is deliberately left on port 8123, so a second server there is normal workflow — `floors.mjs` spawned its own with `stdio:'ignore'` so the `EADDRINUSE` was invisible, its readiness probe was a `GET /` that the **foreign** server answered 200, and `intro-check-server.mjs` truncated its log *before* `.listen()`, leaving a one-line log that read exactly like a run which produced nothing. Every beacon went to the other server's log. **The quiet direction was the dangerous one:** `ROOT` is `cwd/dist` of whichever process won the bind, so a server left up in another checkout serves a *different site* and the run would have scored it as the one just built — §5's standing lesson arriving through the runner instead of a check. Fix: the server answers `/__whoami` with `{pid, root, log, port}`, truncates only after a successful bind, and fails loudly on stderr; the runner **adopts** a stranger provably serving the same `dist` (killing it would take down the URL Sean's phone is on) and **refuses** one that is not, or one that will not identify itself, naming both roots and the command that clears it. All three branches watched, not reasoned about: pre-handshake server → refused with no stray log; same dist → adopted pid 92618, `run=f5wv3d ALL checks=16 bad=0 verdict=GREEN`; different dist → refused printing both roots. **The generalisation is not about servers:** a readiness probe any listener can satisfy asks whether *something* is there, when the gate needs to know whether *the right thing* is there. **Re-verified after the fix, both engines: Chrome 151 `run=f5wv3d` 16/0 GREEN, Safari 26.5.2 `run=fbd8sq` 16/0 GREEN, CDP 8/0 GREEN, `FLOORS GREEN`, 72 unit tests, `astro check` 0 errors.** **Still the one open item: Sean's feel gate** — the production build is live at `http://localhost:8123/` and `http://192.168.68.54:8123/` (phone, same LAN), and the three flippable calls are unchanged: fade-through-paper vs plain crossfade, whether the six non-morphing tiles get a beat of their own, and the 520ms travel. **FEEL GATE CLOSED 2026-08-16 — ship as built.** Sean judged it on the production build (not a dev server); all three flippable calls put to him with their costs stated rather than their merits — the page hand-off (fade-through-paper vs plain crossfade, cost = the measured ~100ms double exposure of two 107px headlines), whether the six non-morphing tiles get a beat of their own, and the 520ms travel. **His call on all three: unchanged.** The choreography is now judged as well as measured, so nothing here is open. Moved to Done; the remaining gaps (Firefox in the gate, touch, WebKit reduced motion) are carried by the text-arrival-beats ticket and DESIGN §5, not by this one.
- **creative-partner skill: add a plain-language gate** (surfaced 2026-08-05, P&P reconvene session — Sean: "I'm truly having trouble following what it is that's being proposed and why"): the skill's loop enforces reason-capture discipline but nothing forces the orchestrator to translate proposals into plain terms before asking for a decision; the round-3 fates presentation buried the decision under research jargon (precision scores, effect sizes, verification-move names). One-session job: edit [.claude/skills/creative-partner/SKILL.md](../../.claude/skills/creative-partner/SKILL.md) to add a rule in the loop (candidate shape: "every proposals presentation states each option in one plain-language sentence a non-specialist can rule on; evidence detail follows the plain statement, never replaces it"), consistent with the existing memory `explain-jargon-before-decisions`. Skill-edit session per the skill's own house rule (procedure changes never happen mid-session). Note the four skill-file sha256 stamps change → future sidecars stamp the new version. — assigned: Sean **DONE 2026-08-05 — landed as loop rule 3.** "Plain language before the decision": every option stated in one plain-language sentence a non-specialist can rule on BEFORE any evidence detail; jargon/metrics/method names may follow the plain statement, never replace it; every term of art defined on first appearance. Applied in a dedicated skill-edit session per the skill's own house rule; loop renumbered (ask-one-question is now rule 4). Four-file sha256 stamps change with this commit — future sidecars stamp the new version. Same commit also recorded the live-validated divergence isolation mechanism (headless `claude -p --tools ""`; in-session Agent subagents could not fully deny tools) in divergence-stage.md.
- **Codex/Sol adversarial review of the new `creative-partner` skill (surfaced 2026-08-03, partner-skill build session close):** the drafted skill at [.claude/skills/creative-partner/](../../.claude/skills/creative-partner/SKILL.md) (+ its three references) gets the same external adversarial-review pattern as the prior map and design-contract reviews before heavy use. Sean launches it separately; findings triage with Sean, amendments land as targeted edits. A fresh-context subagent compliance run already passed in-build (volunteered reason, category answer, bare agreement, wave-off, stall→offer, repo-placement refusal all correct); the external pass is the second, independent gate. — assigned: Sean **DONE 2026-08-04 — review run, 12/12 findings accepted, amendments landed.** Codex verdict "revise" (2 dangerously-wrong, 9 structural, 1 minor) at [docs/plans/2026-08-04-codex-review-creative-partner-skill.md](../../docs/plans/2026-08-04-codex-review-creative-partner-skill.md); triaged with Sean, all accepted (f5 as modified: deterministic sidecar structure without full JSON payload encoding — human-first markdown preserved). Headline fixes: location preflight + git-tree refusal, fail-stop write failures, exclusive create/collision/resume procedure, enforced divergence isolation with no verbatim-reason leakage into sub-agent payloads, compaction recovery, generator failure-slot accounting, full four-file hash stamps, reason-before-lock single mutation, tightened triggers, out-of-domain frame mapping, differentiated pre-mortem/inversion. Re-verified by a second subagent pressure run over ten failure junctures — all held; its two cross-file contradictions + two silences closed as follow-up edits. CHANGELOG amendment entry added. The skill is ready for its first live partner session.
- **Build the generalized creative-partner session skill (surfaced 2026-07-30, creative-harness T27c — provenance only, details stay in-lane):** a new public skill (home: `.claude/skills/`, name TBD) generalizing the anima front-door session pattern beyond anima — session sidecar with PROPOSALS LOG + LOCKED DECISIONS, one question at a time, every question ships a recommendation, generic-answer detector, a one-line *why* captured at every lock; no anima emit, the sidecar is the deliverable. Its divergence stage adapts the public MIT `adhd` repo's loop (isolated parallel takes wearing ordinary-persona frames, separate critic pass with traps flagged and reasons, frame selection logged) — author Sean-domain frames rather than the stock engineering-biased 15, quarrying the technique catalogs preserved in `vault/60_archive/sw-creative-toolkit-bmad/` (archived same day; Sean: "I don't want them completely deleted, but in an archive folder is perfect for them"). Domain-agnostic by design, per Sean: "It's meant to help me think differently and creatively and make me better for various topics and tasks." **Build-spec requirements added 2026-07-31 (external adversarial review, Sean-ratified):** (1) the producer skill and any compiled preference pack it pairs with get distinct stable names, a defined invocation/precedence contract, and both artifact hashes stamped into the session sidecar and install manifests; (2) the divergence stage is an explicit mode — default OFF, invoked by Sean or a stated threshold, with a call/token budget and a recorded mode tag (the adapted loop costs ~5-10x a single answer; that spend is a choice, never a default); (3) the build depends on a long-session honesty-rule reassertion mechanism (in-context instructions are documented to drift over long sessions) — either inherited from the harness or a bounded-session rule of the skill's own. **Gate + scope notes added 2026-08-02 (creative-harness T28 grilling, Sean-ratified — provenance only):** the upstream design gate this build waited on ratified 2026-08-02, so the build may proceed on its own schedule; two items were explicitly routed INTO this build's spec rather than decided upstream — the sidecar harvest registry's record layout (paths, defaults, cursors, loud-error duplicate rules) and the block-to-move splitting procedure (how one multi-axis proposal block becomes N discrete moves) — both live here so they can evolve per sidecar format without reopening ratified schema. — assigned: Sean **DONE 2026-08-03 — BUILT, verified, committed.** Grilled with Sean (7 open choices, all Sean-ratified, reasons captured verbatim; session sidecar — the first specimen of the new convention — at `~/.creative-harness/partner-sessions/2026-08-03-creative-partner-build.md`). Shipped: [.claude/skills/creative-partner/](../../.claude/skills/creative-partner/SKILL.md) + `references/{sidecar-contract,frame-deck,divergence-stage}.md` — names locked (`creative-partner` skill / `partner-pack` pack, identity stamps in every sidecar header); 12 frame cards across Sean's four daily lenses + 2 wildcards (pm-skills methods adopted as provenance only, zero dependency); divergence default OFF (invoke-or-offer, 5-call budget, one axis/run, frames logged, critic traps `machine_fate_hypothesis` only); 5-lock honesty checkpoint with sidecar audit traces + ~25-lock soft bound; volunteered-reason rule written in from day one; sidecars live only under `$CREATIVE_HARNESS_HOME/partner-sessions/`, never in a repo tree. Registry record layout + block-to-move splitting spec landed as a private-lane asset (referenced by name only). Verified by a fresh-context subagent pressure run — all load-bearing rules held; 5 surfaced ambiguities closed as targeted edits (incl. the late-volunteered-reason append rule and the procedure-vs-creative-lock boundary). Gitignored `references/private/` slot reserved; README counts + CHANGELOG updated; validator PASSED. Follow-ups filed as their own Todo bullets: Codex/Sol adversarial review (Sean launches), Mac Mini satellite setup.
- **Live-test the anima front door's reason capture before T27c generalizes it (surfaced 2026-08-02):** **DONE 2026-08-03 — PASSED.** Run as a real full-chain session on a spark Sean cares about: the portfolio About-Me short "How to Solve a Problem" (Sean + the four AI mascots; judgement as the product). Chain ran spark → micro-expand → 5 contested-axis workshops → INTERROGATE → ART-VIZ (9 Higgsfield renders, 38 credits, Sean-authorized; anchor `route-c--gpt-ref.png`) → STRESS-TEST (fresh-context reviewer; verdict `proceed`, 5 residuals) → SYNTHESIZE (emitted + `validate_brief_dir` clean). Specimen sidecar with 18 locks at `anima/briefs/2026-08-02-about-me-short/frontdoor-session.md`; findings note at [docs/plans/2026-08-03-anima-frontdoor-reason-capture-livetest-findings.md](../../docs/plans/2026-08-03-anima-frontdoor-reason-capture-livetest-findings.md). The six findings: (1) asked/captured at every lock, no drift — 17/17 whys recorded, with one design gap (the volunteered-reason case, 8/17 locks — new Todo ticket); (2) verbatim held exactly, typos preserved; (3) skip path UNTESTED (never waved off); (4) one-guess mechanism UNTESTED (no category-grade answer ever occurred — the design's motivating case didn't arise); (5) SUPERSEDES UNTESTED (no lock was superseded; the pose reversal amended a proposal, honestly reported, not manufactured); (6) cadence PASS in Sean's words: "the sidecar questions haven't bothered me at all. It's really made me think deeper about WHY I want those choices and even made me think of other routes to go. Definitely want that moving forward." Bonus validation: the L4 why-answer was autobiographical and flipped the story's protagonist design (steady-conductor → caught-in-the-chaos) — the question generated creative material, which is exactly the "get the brain churning" hypothesis. No skill edits made in-session; the one prose fix is the new Todo ticket. — assigned: Sean
- **anima carries ~4 sessions of uncommitted FIRST LICKS work (surfaced 2026-08-02 during the front-door reason-capture edit — incidental find, not part of that ticket):** the anima working tree on branch `first-licks-story-artdept-close` holds 6 modified + 8 untracked files dated 2026-07-23 → 07-26 that were never committed, spanning Phase-1 story formalization through Phase-3 wave-1 pack drafting. **Modified:** `CHANGELOG.md` (5 dated entries already written, +40 lines), `CLAUDE.md` (+2 — the per-animation-project convention that every `briefs/{project}/` dir carries a project `CLAUDE.md` + `STORYBOARD.md` built from `docs/templates/`), `briefs/2026-07-02-grandmaster/concept.md` (+60/-16, DR #33–36), both ai-guru brief files (small), `prompts/seedance-template-v4.md` (+2). **Untracked (~96K):** the grandmaster project `CLAUDE.md`, `STORYBOARD.md`, `beat-sheet-v1.md`, `beat-sheet-v2.md`, `beats.json`, `script-v1.md`, `CONTINUATION-2026-07-24-phase2-census.md`, and `docs/templates/` (the two template files). **One concrete breakage:** the committed-pending `CLAUDE.md` convention links to `docs/templates/`, which is untracked, so that pointer resolves only on this machine. **Not in scope:** `runs/` is gitignored by design (`.gitignore:2`), so the generated wave-1 plates, keeper set, and census stay local-only and are correctly excluded. **COMMITTED 2026-08-02** as `1da37bf` (15 files — `docs/templates/` was two files, not one) on branch `first-licks-story-artdept-close`, kept as its own commit separate from the front-door skill edit. **CLOSED 2026-08-02 — merged to `main`.** Pushed 12:40 EDT by Sean, then shipped via [anima PR #121](https://github.com/seanwinslow28/anima/pull/121), squash-merged 12:57 EDT as `454b766`. Local `main` fast-forwarded and verified in sync with origin, working tree clean, content spot-checked on disk (FIRST LICKS files present, the DR #33o one-shot pull-back mirror ending intact, the front-door `why (verbatim)` rules present in both skill files). Branch auto-deleted on merge. **Opening the PR required an unplanned ancestry repair worth remembering:** PRs #119 and #120 had been squash-merged, which discards ancestry, so a PR off this branch showed 67 changed files instead of the real 17 and was **not mergeable**. Fixed by merging `origin/main` back into the branch (`85b2b73`, no content change — it only repairs the merge base), resolving both conflicts toward the branch. `concept.md` mattered: `main` was still carrying the *superseded* FIRST LICKS canon (retired camcorder-cassette end card, old "phoenix is born" inscription, ~3:30–4:00 runtime, mid-montage second-compartment star discovery), all of which DR #33/#34 had killed. Taking the branch side restored canon rather than overwriting it. — assigned: Sean
- **anima front door: capture Sean's one-line reason at each lock (surfaced 2026-07-30, creative-harness T27a — provenance only, details stay in-lane):** Sean narrowly lifted the "Anima is read-only" rule for exactly this edit. Job: update `anima/.claude/skills/brainstorm-front-door/SKILL.md` (Step 0 lock discipline) and `references/session-sidecar-contract.md` so the orchestrator asks *why* in one line whenever it writes a LOCKED DECISION and records the answer in the lock entry — reason capture at lock time, nothing else; write-back stays out of scope. His reason for wanting the capture everywhere: "It's meant to help me think differently and creatively and make me better for various topics and tasks." **Change contract added 2026-07-31 (external adversarial review, Sean-ratified):** the allowed surface is exactly these two files — no art-department edits, no shared-dependency edits, diff kept to that allowlist; acceptance test = the only new persisted datum is a user-authored reason on a front-door lock. Anything broader is a new lift Sean must approve first. **Verbatim-marking requirement added 2026-08-02 (creative-harness T28 grilling, Sean-ratified — provenance only):** the captured reason must be recorded as Sean's own words and explicitly marked verbatim in the lock entry (the sidecar's existing `(verbatim)` convention, as SPARK lines already use) — an orchestrator paraphrase does not satisfy the requirement, and downstream consumers will treat unmarked reason text as paraphrase, which defeats the point of capturing at lock time. **Gate status: this edit is now unblocked** — the design gate it waited on ratified 2026-08-02. **DONE 2026-08-02** — applied to both allowlisted files committed as `290e1ee` and shipped to anima `main` via [PR #121](https://github.com/seanwinslow28/anima/pull/121) (squash-merged 2026-08-02 as `454b766`; both rule blocks verified present on `main`). Five design choices grilled and ratified in-session: (1) fixed ask wording "Why that one? One line."; (2) the reason lands as an indented `- why (verbatim): "…"` sub-line under the lock, reusing the sidecar's existing `(verbatim)` marker rather than inventing a second idiom; (3) on a category-grade answer the orchestrator offers **one** candidate reason as a thinking prompt (Sean's pick over open pushback — "it adds to the brainstorming aspect. I could like an option, but not really find the reason why. Having the orchestrator make a guess could help get the brain churning."); (4) a bare agreement with that guess records **nothing** — the guess is never promoted into the quote, which is what keeps the acceptance test intact; (5) `SUPERSEDES` entries take the sub-line too, alongside the entry line's existing prose why (orchestrator's account of what changed vs Sean's reason for changing it). Skip and silence record nothing in every path. Verified: `git -C anima diff --stat` touches only the two allowlisted files, no new emitted field or schema surface, no private-lane vocabulary in the diff. — assigned: Sean
- [closed 2026-06-28] fusion-discovery-council E6/BACKFILL E1-coupled cleanup — CLOSED by the agent-layer pivot (Task 1). The self-referential Stage-5 gate call (`quote_supported_at_url(cited_quote=r.quote, fetched_text=r.quote)` in [backfill.py](tools/llm-council/council/discovery/backfill.py)) now lives only in the OPT-IN in-CLI `--supplement` path; the DEFAULT backfill is agent-driven and vets relevance natively, with the new `verify_supplement` backstop re-checking verbatim-ness. So there is no deterministic Stage-5 relevance gate to upgrade, and E1's entailment work is now a CORE-VERIFY concern only — it no longer needs to flow to backfill. (If the opt-in in-CLI path is ever revived for relevance vetting, reopen.) — assigned: Sean
- [closed 2026-06-18] intent-engineering MCP Workstream A — SHIPPED v0.2.0 (A1 heading-vocab mapper + A2 schema-drift snapshot + A3 pagination test), merged to main, npm publish + MCP registry both DONE (Sean confirmed published + registered 6/18). This is the MCP Post 3 ships on. Only Workstream B (Intent Card) remains — parked/spec'd, build #2 after the Advisor, no deadline. — assigned: Sean
- [closed 2026-06-18] Publish "Raising Claude" Post 3 (Correct Was Never Defined) — PUBLISHED 6/18: https://seanpwins.substack.com/p/correct-was-never-defined. Live render QA'd clean (title / subtitle / hero / repo link / social card all correct); npm package name hyperlinked to npmjs by Sean. post.md frontmatter → status: published + publish_date 2026-06-18 + live_url; substack README queue row marked published. Ships with the shipped intent-engineering MCP @swins/intent-engineering-mcp (v0.2.0). — assigned: Sean
- DONE 2026-06-18 (Claude Code): job-feed ATS watchlist decay — failed pollers cut from **20 → 0** (verified via a live `fetch_all` run; postings 3876 → 4798). Probed every flagged slug against Greenhouse/Lever/Ashby: (1) recovered 3 via corrected slugs in `watchlist.yaml` — `scale`→`scaleai` (177 jobs), `glean`→`gleanwork` (171), `together-ai`→`togetherai`; (2) the 7 "ReadTimeouts" (elevenlabs, lovable, hubspot, klaviyo, webflow, cursor, together-ai) were valid-but-slow boards dying at the 10s budget, NOT dead — fixed by bumping `http_timeout_sec` 10→20 in [agents-sdk/config.toml](agents-sdk/config.toml) (committed); (3) 11 companies on unsupported ATSes (Workday/SmartRecruiters/custom — huggingface, replicate, tome, canva, framer, spline, beehiiv, fourthwall, draftkings, datarobot, circle) moved to a documented commented block in the watchlist (track manually in target-companies.md). NOTE: `watchlist.yaml` is under the gitignored job-hunt private layer, so that edit is **local-only / uncommitted** (the config.toml timeout bump is committed). The web3career_token sub-item was split into its own Todo (Keychain secret — Sean-only).
- DONE 2026-06-18 (Claude Code): llm-council transcript render no longer crashes on null model content. Guarded all three model-supplied appends in `_render_markdown` at [tools/llm-council/council/cli.py](tools/llm-council/council/cli.py) — response content (line 40), ranking reasoning (line 47), and chairman synthesis (line 52) — each now falls back to a `_(no response…)_` placeholder instead of appending `None` into `lines`. Added regression test `test_render_markdown_handles_null_content` in [tools/llm-council/tests/test_cli.py](tools/llm-council/tests/test_cli.py) that reproduces the exact `TypeError` (red before fix) and asserts the placeholder + real content survive. Verified: `pytest tests/` → 34 passed, 1 skipped. NOTE: code lives outside the vault and is uncommitted in the working tree — commit when ready.
- DONE 2026-06-18 (Claude Code, verify-and-close — already shipped): Tier C batch route wiring. All three asks are satisfied in-tree: the route `tier_c_batch_summarize = { model = "gemma4_26b-32k", machine = "alienware", fallback = "none" }` exists at [agents-sdk/config.toml](agents-sdk/config.toml) line 403 (fail-fast Pattern-E shape, no API fallback — the recommended choice), and the Tier C "production" note is in [CLAUDE.md](CLAUDE.md) line 170. No further work needed; closing the stale ticket.
- DONE 2026-06-18 (Claude Code): "test suite hang" on the Gemini DR call resolved — `pytest tests/` now completes (895 passed in ~13s, was hanging). Root cause was subtler than reported: `test_run_warn_called_with_mtd_plus_pred` in [agents-sdk/tests/test_gemini_dr.py](agents-sdk/tests/test_gemini_dr.py) pre-seeded the ledger with a hard-coded **2026-05-03** entry, but `run()` computes mtd/today against the live `date.today()`. Once real time passed into June the May entry stopped counting, the daily-cap refusal no longer fired, and execution fell through to the **real** `client.interactions.create()` (genai + get_credential were unpatched) — a live DR call that polls up to 3900s. Fix: dated the ledger entry to `date.today()` so the cap math engages (also restores the intended $12.80 warn assertion), and added hermetic `patch("scripts.gemini_dr.genai")` + `get_credential` guards plus `mock_genai.Client.assert_not_called()` so no test can ever reach the network again. NOTE: code lives outside the vault and is uncommitted in the working tree — commit when ready.
