# Lane manifests — placeholder

Seven lane manifests arrive here via the build map's "Lane manifest schema" ticket and the corpus build that follows it. Manifests are shelf labels that double as reading paths: title + pointer + one-line when-to-read into the private corpus, plus reading-path rows for listen-only titles that never enter the corpus. Never the books. See [../CLAUDE.md](../CLAUDE.md).
