## Obsidian Integration

Bidirectional Obsidian vault and Claude Code knowledge management. Requires the Obsidian MCP server.

### Conventions
- Vault path must be configured in MCP settings before use
- Use YAML frontmatter on all notes for machine-readability
- Atomic notes: one concept per note, linked via wiki-links
- MOC (Map of Content) pattern for navigation
